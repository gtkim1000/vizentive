# 비젠티브 홈페이지 작업 기록

최종 갱신: 2026-08-29 (히어로 가상공간에 핀치 확대/축소(카메라 전진·후진) 기능을 네트워크·도넛 양쪽에 추가하고, 그 과정에서 드러난 다수의 실측 기반 버그를 수정한 세션. **네트워크 공간 핀치 1차 구현**: 처음엔 `networkScene`에 `scale(zoom)`을 얹는 단순 확대/축소로 구현했으나, 사용자 지시로 **"확대/축소가 아니라 카메라 전진/후진"**으로 전면 교체 — `translateZ(cameraZ)`를 씬 트랜스폼에 추가해 실제 원근감이 반영되는 돌리(dolly)로 재구현(카드 자체는 네이티브 3D라 자동 반영되지만, 연결선은 별도 수동 2D 투영이라 `project()`의 깊이 항에 `cameraZ`를 더해 카드와 함께 움직이도록 동기화). 핀치는 포인터맵으로 2손가락을 추적해 기존 1손가락 회전 드래그와 분기, 손가락 하나만 떼도 매끄럽게 단일 드래그로 복귀. 이 핀치 구현 중 **"인식 공간 밖으로 나간 카드가 클릭 안 됨" 버그**를 발견 — 카메라 전진으로 카드가 `#networkShell` 박스 밖(예: `.intro-copy` 텍스트 위)으로 넘어가면, 클릭 이벤트가 실제로는 그 위에 그려진 다른 요소를 타깃으로 잡아 `networkShell`까지 버블링되지 않아 씹히는 문제였음(elementFromPoint 실측으로 확인) — `document` 레벨 보조 리스너를 추가해 클릭 지점이 shell 밖이면 좌표 기반 히트테스트(`networkNodeAtPoint`)로 직접 구제하도록 수정, 무관한 클릭은 전혀 건드리지 않음을 회귀 테스트로 확인. **동일 핀치를 도넛에도 이식**하며 도넛 전용 문제 다수 추가 발견: (1) 도넛도 같은 클릭 씹힘 버그가 있어 `torusCardAtPoint` 기반으로 동일 구제 로직 선제 적용. (2) 상담 신청 폼 서비스 체크박스에 누락돼 있던 **"홈페이지"** 항목 추가(주요 제작 범위 섹션엔 이미 있었음). **핀치 범위·왜곡·성능 3종 세트 재점검**: 사용자가 "핀치가 확대/축소와 다를 바 없다", "네트워크 전진 시 카드가 떨린다(fps 문제 아니냐)", "카드 데이터량이 작아 계단현상"을 동시에 지적 — 실측으로 하나씩 검증(추측 금지 원칙). fps 문제는 실제로 확인됨: 카메라 최대 전진 시 프레임 시간이 17ms→41ms로 급락했는데 JS 연산 자체는 0.2ms로 불변 — 요소를 하나씩 제거해가며 원인을 좁힌 결과 **`.network-node`의 `box-shadow`** 하나가 원인의 거의 전부(제거만으로 41ms→17ms 정상 복귀, 다른 무엇도 영향 없음)임을 확인 — 카메라 전진으로 카드가 400~900px까지 커지면 매 프레임 그 면적에 블러를 재계산하는 비용이 급증한 것. `dolly-heavy` 클래스를 신설해 카메라가 일정 수준(전진 한계의 40%) 이상이면 도넛·네트워크 양쪽 카드의 box-shadow를 끄도록 수정(기존 `.3s ease` 트랜지션 덕에 자연스럽게 페이드). 계단현상은 카드 이미지의 `sizes` 속성이 "정상 표시 크기(370~520px)" 기준으로 고정돼 있어 카메라 전진으로 실제 렌더 크기가 그보다 커져도 브라우저가 작은 srcset 파생본을 그대로 쓰기 때문 — 기존 포트폴리오 그리드 핀치 리사이즈에 쓰던 `measureAndApplyGridSizes`와 같은 기법으로 `refreshCardImageSizes()`를 신설해 핀치가 끝나는 시점에 화면의 모든 카드 이미지 `sizes`를 실제 렌더 폭으로 재계산(제스처 도중엔 요청하지 않음). **도넛 전용 원근 폭발 버그(가장 까다로웠던 문제)**: 도넛 카드는 자체적으로 이미 상당한 z-깊이를 갖고 있어, 네트워크와 "똑같은 비율(0.55)"을 그대로 썼더니 카메라 전진값을 더하는 순간 원근 나눗셈 분모가 0에 근접해 카드가 가로 1846×세로 2461px 같은 통제불능 크기로 폭발(실측으로 확인, 화면 상하단이 잘려나가는 것처럼 보였음). 1차 수정으로 "전면 카드 높이가 shell의 85%를 넘지 않도록" 실측 기반 역산 클램프를 도입했으나, 이 과정에서 보정 비율식의 부호가 반대로 들어간 실수(분모를 반대로 조정해 카메라를 더 전진시켜버림)와, 극단적 핀치가 안전 상한 없이 원근 특이점을 넘어가버리는 문제를 각각 실측 후 수정. 하지만 이후 **사용자가 방향을 재조정**: "그렇게 자기 공간 안으로 줄어들게 하지 말고, 네트워크처럼 화면 밖으로 나가도 다 보이게 해달라" — shell 높이 기준 축소 보정 로직을 완전히 삭제하고, 원근 특이점(퍼스펙티브의 90%)에 닿기 직전까지만 막는 순수 안전핀만 남기는 것으로 최종 정리(실측: 가로 2751×세로 3668px까지 커지며 화면을 한참 벗어나되 깨짐 없이 정상 렌더링, 그렇게 넘친 카드도 정상 클릭됨을 확인). **모바일에서 여전히 안 됨 재보고 → 진짜 원인 발견**: 데스크톱에선 정상 작동해 놓쳤는데, `.torus-shell`에 예전 세션("도넛 후면 카드 궤적과 모바일 안정성 개선")에서 넣어둔 **모바일 전용(640px 이하) `overflow:clip`+`contain` 규칙**이 있어 도넛만 모바일에서 자기 박스에 갇혀 있었음(`.network-shell`엔 애초에 이런 규칙이 없었음) — 이 규칙 자체는 원래 목적을 위해 유지하되, `dolly-heavy` 상태일 때만 예외적으로 풀리도록 스코핑, 실제 모바일 뷰포트(390×844)로 재현해 핀치 전엔 `overflow:clip`, 핀치 중엔 `overflow:visible`로 전환되며 카드가 원래 박스(390×390)를 훨씬 벗어나 화면을 가득 채움을 확인. **네트워크 공간의 상단 바로가기 메뉴 미반영 수정**: 포트폴리오 바로 위 "AI 모델·광고 소재·원페이지 릴스·카드 뉴스" 링크가 오직 도넛(`focusTorusGroup`)만 조작해 네트워크 모드에선 무반응이었던 것을, `focusNetworkGroup()`을 신설해 클릭 시 활성 모드를 보고 분기하도록 수정 — 네트워크가 하위 단계에 들어가 있어도 최상위로 복귀 후 해당 그룹에 포커스(형제 dim + 중앙 확대)됨을 확인, 도넛 쪽 기존 동작도 회귀 없음 확인. 이번 세션 전체에 걸쳐 Playwright로 실측(프레임 타이밍, 좌표, 클래스 상태, elementFromPoint)하여 추측이 아닌 증거로 원인을 좁히는 방식을 일관되게 사용, 매 수정마다 로컬 빌드(`npm run build`) 후 재검증.)

이전 갱신: 2026-08-28 (히어로 페이지("도넛/네트워크 가상공간") 저사양 폰 버벅임 원인을 코드 분석으로 5가지 특정해 전부 처리한 세션. **1) `#aiField` 배경 파티클 캔버스**: 전체 화면을 덮는 O(n²) 거리계산 애니메이션이 가시성 체크 없이 사이트 전체 수명 동안 항상 60fps로 돌고 있었음 — 타임스탬프 기반 스로틀로 24fps로 제한(요청에 따라 30→24 단계적 조정). **2) `spatialFrame()`(도넛 렌더 루프) 무한루프 버그**: 모드가 도넛이 아니거나 화면 밖이어도 매번 `requestAnimationFrame`으로 자기 자신을 재예약해 절대 멈추지 않던 것을 발견 — `networkFrame()`의 기존 "멈췄다 필요할 때 재시작" 패턴(`torusFrameRunning`+`startTorusFrame()`)을 대칭 적용해 실제로 정지·재개되도록 수정(수정 중 `torusFrameRunning` 변수를 선언보다 먼저 참조하는 TDZ 버그를 만들었다가 IDE 진단으로 즉시 발견해 수정). **3) 카드 이미지 항상 원본 해상도 문제(가장 큰 병목)**: 도넛·네트워크 카드가 화면 표시 크기(모바일 256px~데스크톱 520px)와 무관하게 항상 원본(최대 1792×2400px)을 그대로 받고 있었음(실측: 최대 59배 오버샘플링) — `tools/generate-card-images.js` 신규 작성해 원본 93장마다 320/640/1024px 파생본 279개 생성, `srcset`+`sizes`를 도넛·네트워크 양쪽에 공용 적용(둘이 수시로 전환되므로 더 큰 쪽인 도넛 기준 sizes로 통일하기로 결정). 구현 중 네트워크 모드가 애초에 `mediaMarkup()`을 안 거치고 `<img>`를 직접 만들고 있어서 새 `srcset`이 처음부터 적용된 적이 없었던 별개의 버그를 추가로 발견해 수정. 포트폴리오 그리드·데이터 월(둘 다 `art-NN.webp` 사용)에도 동일 기법 적용(`tools/generate-preview-images.js`, 320/640 2단계) — 데이터 월은 자체 지연로딩 큐 시스템이라 `data-srcset`을 심어뒀다가 실제 로드 시점에 `srcset`으로 전환하는 방식으로 별도 대응. 핀치로 열 개수가 실제로 바뀌는 순간에만(연속 스케일 미리보기 중엔 안 함) `sizes`를 다시 계산해 필요시 더 큰 파생본을 새로 받도록 `setupPinchColumns()`에 훅 추가. 실측: 모바일 65~81%, 최대 82% RAM 절감(단, 압축 포맷만 바꾸고 해상도를 유지하면 RAM 절감 효과가 없다는 걸 히어로 PNG 사례로 확인 — 파일 크기와 디코딩 메모리는 별개 축). **4) 히어로 배경 PNG(1.5MB)**: 실제 필요 해상도(1254×1254, 표시 크기 대비 과도하지 않음) 대비 무손실 PNG라 압축 자체가 안 돼있던 게 원인 — WebP quality75 재인코딩만으로 36KB로 42배 감소(해상도는 그대로라 RAM 절감은 없음, 별도 과제로 보류). **5) backdrop-filter 블러/체이싱 글로우**: 비용은 모든 기기에서 동일하게 발생하지만 증상(버벅임)은 저사양에서만 나타난다는 점에 착안, `navigator.hardwareConcurrency<=4`면 `perf-lite` 클래스를 부여해 도넛 카드 라벨의 `backdrop-filter:blur()`만 저사양에서 제거(체이싱 애니메이션은 이후 사용자 지시로 이 예외에서 제외 — 아래 참고). 이 다섯 항목의 상대 영향도를 캔버스=100 기준으로 추정(카드 해상도 ~60, 루프 버그 ~25, 필터 효과 ~20, PNG ~15)해 우선순위 근거로 제시. 추가로 이미지 데이터량 감소 필요성을 논의하며 대표적인 저사양 후보 기기로 언급된 "갤럭시 S32+"는 실재하지 않는 모델명이라 확인 질문이 있었으나 이후 논의 방향이 바뀌어 실제 조사는 진행하지 않음. **부수 발견·수정**: 헤더 햄버거 메뉴(`☰`)·닫기(`×`)·모달 화살표 등 텍스트 글리프로 표기되던 아이콘 7곳이 폰트/브라우저마다 렌더링이 미묘하게 달라 보이는 문제(사용자가 인스타그램 인앱 브라우저 스크린샷으로 제보) — 전부 인라인 SVG로 교체. 상담 신청 팝업의 "오픈 카톡 상담" 버튼을 "1:1 상담"+`pf.kakao.com` 채널 링크로 변경(빌드 스텝을 빠뜨려 로컬에 반영이 안 된 해프닝 있었음, 이후 항상 `npm run build`까지 마치기로 함). **도넛 카드 시각 효과 조정(세션 후반, 별도 요청 3건)**: (1) 카드 라벨 배경에 있던 검은 반투명 박스를 완전 투명으로 되돌림(원래도 투명이었는데 위 4번 저사양 대응 중 실수로 불투명 배경을 추가했던 것을 되돌림); (2) 앞면 카드의 번쩍임(`torusFrontFlashGlow`)+체이싱(`torusFrontChase`) 글로우를 `.torus-shell.auto-rotating` 상태로 스코핑해 유휴 자동회전 중에만 재생되고 수동 드래그·클릭 선택 중엔 재생되지 않도록 함 — 처음엔 체이싱만 사라지는 재보고를 받았는데(perf-lite가 이 효과까지 꺼버리고 있었음, 사용자 지시로 그 예외를 완전히 삭제해 자동회전 여부만이 유일한 조건이 되도록 함) 동시에 `transitionSpatialMode()`가 시작될 때 `auto-rotating` 클래스를 안 지우고 있어서 가상공간 전환 중에도 이 상태가 얼어붙어 남아있던 실제 버그를 발견해 전환 시작 시 항상 초기화하도록 수정; (3) 포트폴리오·데이터 월에 도넛의 사방 화살표와 같은 스타일의 좌우 전용(상하 없음) 드래그 힌트 화살표를 신규 추가 — 폭은 항상 화면 폭의 25%, 위치는 `position:fixed`로 뷰포트 중앙에 수평 고정하되 수직 위치는 스크롤에 따라 "섹션이 화면에 걸쳐 보이는 부분의 중앙"을 추적(섹션이 화면을 완전히 채우면 화면 정중앙, 절반만 보이면 그 절반의 중앙, 전혀 안 보이면 숨김)하도록 계산해 매 스크롤/리사이즈마다 rAF로 갱신 — 이후 재요청으로 추적 대상을 전체 섹션(제목·CTA 포함)에서 실제 카드 그리드(`#portfolioGrid`/`#archiveWall`)로 좁히고, 보이는 공간이 화살표 자체 크기보다 작으면 숨기도록 보완, 모바일 전용(핀치 안내 목적)으로 `innerWidth>640`이면 항상 숨기도록 제한. 세션 마지막에 전체를 `main`에 커밋·푸시(Vercel 자동 배포 트리거)하고 로컬 zip+git bundle, 원격 태그로 "베타5-최적화" 백업 생성.)

이전 갱신: 2026-08-27 (도넛 브라우저에 상하 틸트(피치) 기능 신규 추가 — 기존엔 좌우 회전(`torusRotation`)만 있었고 상하 입력은 아예 처리하지 않았음(모바일에서도 `touch-action:pan-y`로 세로 스와이프를 일부러 페이지 스크롤에 넘기고 있었음, 2026-08-25 확정 사항). **카드를 잡고 위아래로 드래그하면 도넛 전체(`#torusScene`)가 `rotateX`로 최대 ±45°까지 기울어지는 기능**을 추가: 드래그 시작 지점이 카드 위인지 여부로 동작을 분기 — 카드 위에서 시작한 드래그만 상하 이동량을 피치에 반영하고(`torusPitch`, 감도 `.28deg/px`, `torusPitchMax=45`로 clamp), 카드가 아닌 빈 영역에서 시작한 드래그는 기존처럼 세로는 페이지 스크롤, 가로는 회전으로 그대로 둔다. 구현은 CSS `touch-action`의 "가장 제한적인 값이 이긴다" 특성을 이용 — `.torus-shell`은 `pan-y`(기존 유지, 빈 영역은 계속 페이지 스크롤 허용), `.torus-card`에만 `touch-action:none`을 추가해 카드 위에서 터치가 시작되면 교차값이 `none`이 되어 브라우저가 스크롤을 가로채지 못하고 JS가 직접 처리하게 만들었다(PC 마우스와 모바일 터치 동일 코드 경로, `torusDownIndex`로 시작 지점 판정). 좌우 회전(`torusDragDelta`)과 상하 틸트(`torusDragDeltaY`)는 완전히 독립된 상태값이며 같은 배치 렌더 함수(`renderTorusDrag`)에서 각자 조건부로만 처리해 기존 회전 로직은 손대지 않았다. Playwright로 로컬 빌드(`localhost:8000`) 검증: 카드 위 드래그 시 실제 `rotateX` 값이 변하고 ±45°에서 정확히 clamp됨, 카드 위 드래그 후에도 좌우 회전(대표 카드 인덱스 변경)이 정상 동작함, 빈 영역에서 시작한 세로 드래그는 `torusScene` 트랜스폼이 전혀 변하지 않음(터치 스크롤 패스스루 유지)을 스크린샷과 함께 확인 — 콘솔 에러는 로컬 개발 환경에만 있는 사전 존재 404(`ppt-manifest.js`, 무관한 애셋 누락)뿐이었음. **후속 버그 신고 및 근본 수정**: 사용자가 "틸트로 드러난 뒤쪽 카드를 선택해도 앞으로 안 온다"고 재보고 — 처음엔 Playwright로 카드 하나를 골라 클릭 테스트해 성공했다고 "이미 된다"고 잘못 답했으나, 사용자가 재지적해 다시 조사. **진짜 원인**: 링 뒤쪽(로컬 z가 가장 음수인) 일부 카드는, 실제 화면에 정확히 보이는 그 자리(`getBoundingClientRect()`로 그린 디버그 테두리가 사진과 완벽히 일치)에서도 `document.elementFromPoint()`가 카드 대신 빈 부모(`#torusScene`)를 반환하는 크로미움의 네이티브 히트테스트 오류(복합 3D 트랜스폼 체인 + `backface-visibility:hidden` 조합에서 발생) — 26×26 격자로 박스 전체를 스캔해도 단 한 픽셀도 자기 자신에 히트하지 않아 "얇은 조각이라 표본 부족" 가설도 기각, 눈에는 멀쩡히 보이지만 네이티브 클릭 판정에서만 사라지는 것으로 확정. 이는 이 세션의 틸트 기능과 무관하게 원래부터 존재하던 문제(틸트가 카드를 극단적 각도로 더 많이 보내면서 노출 빈도만 높인 것)이며, **[[torus-network-mode]]에서 이미 한 번 겪고 고쳤던 것과 완전히 같은 종류의 버그**(극단적 회전 각도에서 네이티브 `closest()`가 실패, `networkNodeAtPoint()` 수동 폴백으로 해결)임을 확인. 동일 패턴으로 `torusCardAtPoint(x,y)`(전체 카드의 `getBoundingClientRect()`를 직접 대조해 z-index 최상단 후보를 고르는 수동 히트테스트) 폴백을 추가하고 `startDrag()`가 네이티브 판정 실패 시 이걸로 대체하도록 수정 — Playwright로 틸트를 5회 반복해 매번 네이티브 판정이 실패하는 카드를 찾아 클릭한 결과 5/5 모두 정상적으로 최전면 전환됨을 확인. **부수 발견**: `.torus-card`의 실제 렌더링 폭은 스타일시트 뒷부분의 스코프 없는 재정의 규칙 때문에 기본 규칙(185~260px)이 아니라 약 490px(`clamp(370px,34vw,520px)`)이었다 — 이 시스템을 다시 분석할 땐 최초로 매칭되는 규칙 텍스트가 아니라 반드시 실제 computed style을 확인할 것. 같은 날 사용자가 인터랙티브 네트워크 모드에도 동일한 "카드 위=원래 기능, 빈 배경=페이지 스크롤" 처리를 요청 — `.network-shell` touch-action을 `none`→`pan-y`로, `.network-node`에 `touch-action:none`을 추가하고, `dragNetwork()`의 피치(상하) 갱신만 `networkDownIndex!==null`(카드/허브에서 시작한 드래그) 조건으로 제한(요(좌우)는 도넛과 동일하게 무조건 반응, 원래도 문제 없었으므로 그대로 둠)해 동일 패턴으로 해결. **가상공간 전환 연출을 훨씬 다양하게 재설계**: 기존엔 모든 카드가 항상 "원점에서 바깥으로 방사형 이동 + XYZ 모두 무작위 회전"이라는 단일 패턴만 썼는데, 사용자가 "카드가 다양하게 들어갔다 나왔다 하는 걸 넣어달라, 위로 간다든지 아래로 간다든지 방으로 흩어진다든지, 회전도 XYZ축 다양하게"라고 요청 — 이동 방향 7종(`up`/`down`/`left`/`right`/`radial`/`toward`/`away`)과 회전 프로파일 4종(X축 위주/Y축 위주/Z축 위주/셋 다 큰 텀블)을 각각 카드마다 독립적으로 무작위 조합하도록 재작성(`randomScatterKind()`+`scatterVector()`+`randomTumble()`). 도넛은 카드 transform을 전체 교체하는 구조라 그대로 적용, 네트워크는 카드가 항상 카메라를 향하는 빌보드 회전(`rotateY(-yaw) rotateX(-pitch)`)을 유지해야 해서 기존 회전 뒤에 정규식으로 추가 회전만 갈아끼우는 방식을 그대로 쓰되 회전 폭을 rotateZ 하나에서 XYZ 전체로 확장(복귀 시 보간이 끊기지 않도록 자연 위치 쪽에도 `rotateX(0) rotateY(0) rotateZ(0)` 자리표시자를 같이 추가). Playwright로 전환 중 8장의 카드 transform을 실측해 이동 방향(위/아래/좌/우/카메라 쪽/카메라 반대쪽)과 회전 축 강조가 카드마다 뚜렷하게 달라짐을 확인, 스크린샷으로도 산만하지 않으면서 활기찬 흩어짐이 보임을 확인. **도넛 카드 선택 회전이 매끄럽지 않다는 재보고 → CSS 트랜지션 기반에서 JS 프레임 단위 트윈으로 전면 교체**: 기존엔 회전만 CSS `.snapping` 클래스의 `transition:transform`에 맡기고 z-index(깊이 순서)는 트랜지션 시작 시점에 단 한 번, 목표값으로 즉시 바꿔버렸다 — 그 결과 화면 위치는 480ms에 걸쳐 부드럽게 보간되는데 어떤 카드가 앞뒤인지(z-index)는 이미 최종값으로 순간 고정되어, 회전 도중 카드끼리 앞뒤 순서가 위치와 안 맞게 겹쳐 보이는 "재구성처럼 보이는" 현상이 있었다. `tweenTorusRotation()`을 새로 만들어 `torusRotation` 값 자체를 매 프레임 easing 함수로 보간하며 그때마다 `layoutTorus()`를 다시 불러 위치와 z-index를 항상 같은 순간의 값으로 재계산하도록 변경(`selectTorus`/`focusTorusGroup` 둘 다 적용, 드래그 시작 시 진행 중인 트윈은 취소). Playwright로 33프레임을 샘플링해 매 프레임 z-index가 항상 유효한 순열(중복 없음)임을 확인, 스크린샷 시퀀스로도 카드들이 한 덩어리로 매끄럽게 회전해 들어오는 모습을 확인. **말단 카드 원본보기 진입을 확대 트랜지션으로 재구성**: "말단 카드가 최전면에서 클릭 시 원본 보기로 확대되어 들어가는 것처럼, 나갈 때는 거꾸로" 요청 — 기존엔 모달이 `display:none↔flex`로 전환 트랜지션 없이 순간적으로 뜨고 사라졌다(포트폴리오 하단의 데이터 월은 이미 `growArchiveCard`/`shrinkArchiveCard`로 같은 효과가 있었지만 도넛/네트워크 가상공간에는 없었음). 데이터 월과 별개로 도넛·네트워크 전용의 `zoomCardIntoModal()`/`reverseZoomFromModal()`을 새로 작성 — 클릭된 카드의 실제 화면 좌표(`getBoundingClientRect()`)에서 시작해 화면 중앙 큰 영역(데스크톱 62×86%)까지 커지는 `position:fixed` "고스트" 오버레이를 만들고(원본 카드는 그 사이 opacity:0으로 숨김), 다 자란 시점에 실제 모달을 열고 고스트를 제거 — 비디오 말단 카드는 방금 재생 중이던 비디오를 그대로 복제하면 첫 프레임이 검게 나올 수 있어 항상 정지 이미지(포스터/미리보기)로 고스트를 만든다. 닫을 때는 반대로 모달의 현재 이미지 영역(`.modal-visual`)에서 시작해 원래 카드의 **그 시점 기준 실측 위치**로 줄어드는 고스트를 만든 뒤 원래 카드의 opacity를 복원 — 모달이 열려 있는 동안에도 배경 링/궤도 회전은 계속되므로(관련 기존 이슈) 열 때 좌표를 저장해두지 않고 닫는 시점에 항상 다시 실측해서, 오래 열어뒀다 닫아도 카드가 그 사이 돌아간 실제 위치로 정확히 줄어들도록 함. Playwright로 도넛·네트워크 각각 실제 클릭 흐름을 재현해 고스트가 등장→모달 오픈→고스트 제거, 그리고 닫기 시 고스트 재등장→축소→제거+카드 투명도 복원까지 전 구간을 스크린샷과 함께 확인, 두 모드 모두 동일한 이미지로 이어져 보임(예: 시니어 여성 AI 모델, 뷰티 광고 시술 사진)을 확인. **전환 속도 재조정**: 사용자가 "효과가 들어간 것 같은데 너무 빨리 지나가서 알 수가 없다, 반 정도 늦춰달라"고 요청 — `SPATIAL_SCATTER_OUT_MS`(420→840)와 `SPATIAL_SCATTER_IN_MS`(460→920) 둘 다 정확히 2배로 늘림(오버랩 유지시간·버튼 재활성 타이밍은 이 두 상수에서 자동 계산되므로 별도 수정 불필요). 800ms 지점에서도 카드가 아직 자기 궤도로 안착하기 전 단계로 보이는 것을 스크린샷으로 확인. **곧이어 재조정**: "속도를 50% 정도 빠르게 해주고(방금 늦춘 상태 기준), 방향은 다양해서 좋은데 비슷한 느낌이다, 팽그르르 돈다든지 춤춘다든지 착지할 때 탱그르르 돌고 탁 착지하는 것도 간혹 넣어달라" — 두 요청을 함께 반영: (1) 방금 2배로 늘렸던 `SPATIAL_SCATTER_OUT_MS`/`SPATIAL_SCATTER_IN_MS`(840/920)를 원본이 아니라 그 값 기준으로 1.5로 나눠 560/613으로 재조정. (2) `randomTumble()`에 16% 확률로 한 축이 1080~1800도 통돌이하는 "휘몰이" 분기를 추가하고, 착지(진입) 전용으로 카드별 25% 확률로 별도의 Web Animations API 애니메이션(`landingFlourish()`)을 추가 — 메인 CSS 트랜지션이 끝나 자연 위치에 도달한 직후, 그 자연 트랜스폼의 `scale(...)` 앞에 정규식으로 큰 회전+확대(오버슈트)를 끼워넣은 시작 프레임에서 시작해 정확히 원래 자연 트랜스폼으로 끝나는 3프레임 키프레임을 튕기는 이징(`cubic-bezier(.22,1,.36,1)`)으로 재생해 "빠르게 돌다가 탁 멈추는" 느낌을 구현(도넛의 전체 교체 방식과 네트워크의 빌보드 접미사 방식 둘 다 항상 `scale(...)`로 끝나므로 같은 정규식으로 양쪽 다 처리됨). 전역 CSS 룰이 아니라 요소별 WAAPI로 구현해 이전에 겪었던 "스코프 없는 `.torus-card` 재정의가 페이지 전체에 새는" 함정을 원천 차단. 12회 반복 실측: 휘몰이(900도 초과) 9/12회, 착지 플러리시 애니메이션 감지 11/12회로 카드 8장 중 최소 하나가 각각 16%/25% 확률에 걸릴 기대치(약 75%/90%)와 부합함을 확인. **"통통통 튀면서 들어오는" 착지 바운스 추가**: 회전 없이 순수 상하 이동(translate3d의 Y값)만으로 세 번 튕기며 진폭이 점점 줄어드는 `landingBounce()`를 신규 작성, 기존 스핀 플러리시(13%)와 함께 3지선다(스핀 13%/바운스 15%/없음 72%)로 확률 재배분 — 20회 반복 계측(양방향)으로 스핀·바운스 호출이 기대 비율대로 발생함을 확인, 스크린샷 3장으로 카드가 실제로 위로 튀었다 가라앉는 모습 확인. **치명적 버그 발견·수정**: 곧이어 사용자가 "회전 기능 넣고 나서 도넛은 일부 카드가 반응 안 하고, 네트워크는 일부가 정면을 안 본다, 전환 효과일 뿐이니 착지 후엔 원래 기능에 영향 없게 루틴을 분리해달라"고 지적 — 원인은 `landingFlourish`/`landingBounce`가 `fill:'forwards'`로 애니메이션을 재생했던 것: 이 옵션은 애니메이션이 끝난 뒤에도 그 결과(합성 효과)가 요소의 실제 인라인 `transform`을 계속 덮어쓰게 만드는데, 아무도 이걸 취소(cancel)하지 않아 플러리시가 한 번이라도 걸린 카드는 그 순간의 스냅샷에 영원히 고정돼버렸다 — 이후 도넛 드래그(`layoutTorus()`)나 네트워크 매 프레임 빌보드 보정(`applyNetworkBillboard()`)이 아무리 새 `transform`을 써도 죽은 애니메이션이 조용히 계속 그 값을 되돌려 씀. 두 함수 모두 `fill:'forwards'` 제거(기본값 `fill:'none'`으로 애니메이션 종료 즉시 제어권을 자연스럽게 반납, 마지막 키프레임이 항상 자연 상태값과 정확히 같으므로 시각적 끊김 없음) + 함수 시작 시 `el.getAnimations().forEach(a=>a.cancel())`로 방어적 정리 추가. 검증: 여러 차례 강제 왕복 후에도 카드/노드의 활성 애니메이션 수가 전부 0으로 정리됨, 도넛 드래그 시 8장 전부 정상 반응, 네트워크 카메라 관성이 완전히 멎은 뒤 8개 노드의 빌보드 회전값이 오차 없이 전부 일치함을 확인(직전의 약 2도 오차는 두 번의 개별 측정 사이 관성 감쇠로 인한 타이밍 오차였을 뿐, 실제 버그가 아니었음을 동일 틱 측정으로 재확인). **같은 요청에서 회전축 단순화**: "x축이나 y축, z축으로 회전해달라(혼합 금지)"에 따라 `randomTumble()`을 "주축 하나 + 나머지 두 축 약간의 노이즈"에서 "정확히 한 축만 값을 갖고 나머지 두 축은 완전히 0"으로 수정, `landingFlourish()`도 항상 `rotateZ`만 쓰던 것을 매번 X/Y/Z 중 무작위로 고른 회전 함수 하나만 쓰도록 일반화 — 200회 샘플링해 전부(200/200) 회전축이 정확히 하나뿐임을 확인. **바로 다음 메시지에서 아키텍처 자체를 재설계 요구**: "통통통 튀고 회전, 이런 건 전환 과정 중의 효과를 말하는 거다. 즉 튀면서 들어오거나 튀면서 나가거나 하는 거지, 이미 착지한 뒤에 튀거나 도는 건 하지 마라. 단 예외로 Y축 제자리에서 팽그르르 빨리 돌다 탁 멈추는 것만 허용" — 직전에 고친 `landingFlourish`/`landingBounce`는 여전히 "메인 전환이 끝나고 카드가 이미 멈춘 뒤에 별도로 재생되는" 구조였기 때문에 이 요구와 정면으로 배치됨. `landingBounce`를 완전히 삭제하고 **비행 중 바운스**로 재구현: `buildBounceKeyframes(fromT,toT,steps=14)`가 시작·끝 위치의 translate3d/scale을 수치로 파싱해 15개 중간 프레임을 생성하고, Y축에만 `sin(t*bounces*π)*(1-t)*진폭` 형태로 양 끝(t=0,1)에서 정확히 0이 되는 튕김을 얹어 실제 착지 지점을 조금도 벗어나지 않게 만든 뒤, 이 전체를 카드별 동일 지속시간(ms) 하나짜리 `el.animate()` 호출로 재생 — 도넛·네트워크의 진입(scatterIn)뿐 아니라 퇴장(scatterOut)에도 카드별 18% 확률로 동일하게 적용(양방향 모두 "튀면서 들어오고 튀면서 나간다"를 충족). 바운스 중엔 회전을 별도로 섞지 않고 도착지(natural)의 회전 문맥(도넛은 `rotateY(0deg)`, 네트워크는 그 순간의 빌보드 각도)을 프레임 내내 그대로 유지해 바운스와 XYZ 회전 텀블이 같은 카드에서 서로 안 겹치게 분리. `landingFlourish`는 사용자가 명시한 유일한 예외만 남기고 `landingSpinSnap`으로 이름을 바꿔 축을 Y로 고정, "빠르게 돌다 탁 멈추는" 느낌을 위해 0~85% 구간은 `linear`로 일정 속도 회전시키고 85~100%의 짧은 구간에서만 최종값으로 급격히 수렴하도록 키프레임 타이밍을 불균등하게 배치 — 여전히 착지(진입) 후에만, 바운스가 아닌 카드 중 15%에만 걸리도록 유지. 이 예외 애니메이션(460ms)은 전환의 오버랩 타이머(`transitionSpatialMode`)가 풀리는 시점 이후까지 재생 중일 수 있는데, `fill:'forwards'`를 쓰지 않으므로 그 사이에도 배경 렌더 루프가 실제 값을 계속 밑에서 갱신하고 있다가 애니메이션이 자연 종료되는 순간 그 값이 그대로 드러나는 구조라 문제 없음을 확인. 검증: 모드를 4회 연속 강제 왕복(바운스·스핀이 여러 번 걸리도록)한 뒤 (1) 예외 애니메이션 지속시간까지 충분히 기다리면 잔존 애니메이션 0개, (2) 도넛 카드 8장 전부 드래그에 정상 반응, (3) 네트워크 관성이 완전히 멎은 뒤 노드 8개의 빌보드 회전값이 동일 시점 측정으로 전부 정확히 일치함을 확인. **바운스 속도 별도 보정**: "통통통 튀는 게 안 보인다, 그건 예외적으로 느려도 되니 잘 보이게 해달라" — 바운스가 다른 카드와 같은 `ms`(당시 560/613)를 그대로 쓰다 보니 2~3번의 튕김이 0.6초 안에 압축돼 거의 안 보였던 것이 원인. 바운스 전용 `SPATIAL_BOUNCE_MS=1900` 상수를 새로 두고 도넛·네트워크의 진입·퇴장 4곳 모두에서 WAAPI 지속시간과 투명도(opacity) 전환 시간 둘 다 이 값으로 통일(퇴장 시 투명도가 바운스보다 먼저 끝나 사라져버리는 것 방지), 키프레임 수도 14→22로 늘려 늘어난 길이에서도 매끄럽게 보이도록 함 — 평범한 카드와 착지 스핀 예외는 기존 속도 그대로 유지해 바운스만 의도적으로 유독 느리게 만듦. **곧이어 전체 속도도 재차 절반으로**: "전체적으로 반 정도 늦춰달라, 들어가고 나오는 게 너무 빠르다" — `SPATIAL_SCATTER_OUT_MS`/`IN_MS`를 각각 560→1120, 613→1226으로 정확히 2배 재조정(바운스 전용 상수와는 별개로 독립 조정). 스크린샷 4장(300ms 간격)으로 카드들이 이전보다 훨씬 오래, 눈에 띄게 위치를 계속 바꾸는 것을 확인. **바운스가 여전히 안 보인 진짜 원인 발견**: 사용자가 "통통통 튀거나 사인파 궤적 그리는 것들이 여전히 안 보인다, 천천히 해달라"고 재차 지적 — 조사 결과 단순히 지속시간을 늘리는 것만으론 부족했음: `transitionSpatialMode()`가 두 씬을 동시에 보여주는 `.spatial-overlap`을 제거하는 타이머가 `SPATIAL_BOUNCE_MS`를 전혀 고려하지 않고 기존 기본 지속시간(약 1366ms)에서만 계산돼, 바운스로 퇴장 중인 카드가 자기 애니메이션(1900ms)이 끝나기도 전에 소속 씬 전체가 `display:none`으로 통째로 가려지고 있었음 — 바운스 시간을 아무리 늘려도 뒤쪽 절반은 항상 잘려나가는 구조적 버그. 이 조사가 끝나기도 전에 사용자가 요구사항을 하나 더 추가: "그런 것들이 합류가 안 됐어도 브라우저는 먼저 자동 동작하게 해주고 늦게 합류하게 해달라"→"반 이상 합류되면 동작하게, 나머지는 늦게 합류되는 대로. 속도가 달라도 상관없다." **아키텍처를 카운트 기반으로 전면 교체**: `settleTracker(total)`(개별 카드 완료를 세어 절반 시점(`half`)과 전체 완료 시점(`all`)을 각각 Promise로 알려주는 헬퍼)를 만들어 4개 scatter 함수 모두 자신의 카드/노드별 완료 시점에 `mark()`를 호출(바운스는 `onfinish`, 평범한 카드는 `setTimeout(ms)`)하고 트래커 객체를 반환하도록 재작성. `transitionSpatialMode()`는 진입 트래커의 `half`가 풀리는 즉시(대부분 기본 지속시간 근처, 바운스는 전체의 18%뿐이라 나머지 82%만으로도 절반을 넘김) 상호작용·자동회전을 재개하고, 퇴장·진입 두 트래커의 `all`이 모두 풀려야(즉 가장 늦게 끝나는 바운스까지 전부 끝나야) 비로소 `.spatial-overlap`을 제거해 감춘다 — 이제 지속시간을 얼마로 바꾸든 구조적으로 도중에 잘리지 않음. **이 분리가 새로 만든 위험**: 상호작용 재개가 은닉보다 먼저 일어나면서, 이미 떠난(퇴장 중인) 씬의 셸이 여전히 화면에 남아있는 동안 그 씬의 렌더 루프나 드래그가 실수로 재활성화될 수 있는 창이 생김 — `spatialFrame()`(도넛 루프)가 기존엔 `isOnScreen()`만 확인했던 걸 네트워크 루프처럼 `spatialModes[spatialModeIndex]==='torus'` 모드 확인도 추가하고, `startDrag`/`startNetworkDrag`에도 각각 자기 모드가 현재 활성 모드일 때만 동작하도록 동일한 가드 추가. 검증: 버튼 재활성 ~1543ms(기본 지속시간 근처, 2600ms 바운스를 기다리지 않음) vs `.spatial-overlap` 해제 ~2912ms(바운스가 실제로 끝날 때까지 기다림)로 두 타이밍이 의도대로 분리됨을 확인; 1000/1800/2400/2900ms 스크린샷 4장에서 카드가 2900ms까지 계속 눈에 띄게 이동함을 확인; 4회 강제 왕복 후 잔존 애니메이션 0, 도넛 8장 전부 드래그 반응, 네트워크 8개 전부 정면 일치; **스트레스 테스트**로 버튼이 재활성화되는 즉시(이전 전환의 바운스 잔여물이 아직 끝나지 않았을 시점) 6회 연속 재클릭해도 카드/노드 개수가 8/8로 정확히 유지되고 고아 요소가 전혀 쌓이지 않음을 확인. **진짜 범인은 타이밍이 아니라 z-index였음**: 사용자가 "늦게 합류되는 건 오히려 멋있다, 근데 바운스나 사인파로 들어오는 것 자체가 안 보인다"고 재차 지적 — `Element.prototype.animate`를 가로채 실제 바운스 요소를 특정한 뒤 `getBoundingClientRect()`+`elementFromPoint()`로 비행 전 구간을 추적한 결과, 아직 화면 왼쪽 멀리(예: `left`/`radial` 방향으로 크게 튕겨나간 지점)에 있는 카드가 왼쪽 히어로 카피(`.intro-copy` z-index:10)의 **뒤에 가려져 렌더링되고 있었음** — 도넛/네트워크 카드는 평소 한 자릿수 z-index만 쓰기 때문에, 이 문제는 사실 이번 세션 내내(심지어 맨 처음 빠른 속도일 때도) 항상 존재했으나 그땐 그 구간을 통과하는 시간이 너무 짧아 아무도 눈치 못 챘을 뿐 — 정확히 사용자가 요청해서 늦춘 속도 때문에 원래 있던 문제가 처음으로 눈에 보이게 된 것. 4개 scatter 함수(평범한 카드 포함) 모두, 애니메이션 시작 시 `z-index:150`(모달 120·확대 고스트 130보다도 높게, 헤더 80·인트로카피 10을 여유 있게 넘도록) 지정하고 그 카드만의 애니메이션이 끝나는 순간 정확히 해제해 평소의 `layoutTorus()` z-index 관리로 되돌리도록 수정. 2600ms 바운스의 10/30/50/70/90/100% 지점 스크린샷으로 전 구간에서 깜빡임 없이 계속 보임을 확인, 완전히 정착한 뒤엔 z-index:150에 갇힌 카드가 하나도 없고 드래그·빌보드 추적도 모두 정상임을 재확인. **같은 메시지에서 바운스·Y축 회전 비중 상향 요청**: 바운스 확률 18%→24%로, `randomTumble()`의 축 선택을 균등 1/3에서 Y축 40%/X축 30%/Z축 30% 가중치로 조정해 "동전처럼 도는" Y축 회전이 더 자주 나오도록 함. **늦게 합류하는 카드가 보정 중 탁 튀는 문제 수정**: 사용자가 "본진에 늦게 합류한 애들이 목적지가 안 맞아서 보정하다 탁 튄다, 매끄럽게 해달라"고 지적 — 원인은 바운스의 목적지(`naturals[i]`)가 전환 시작 시점의 스냅샷으로 고정돼 있는데, 이제 상호작용/자동회전이 절반 시점에 일찍 재개되므로 아직 바운스 중인(최대 2600ms) 카드 입장에선 그 사이 링이 계속 돌거나(도넛 유휴 자동회전) 궤도가 계속 진행(네트워크는 카메라와 무관하게 항상 궤도 진행)해서, 바운스가 끝나 굳어있던 옛 목적지값을 곧바로 이어지는 `layoutTorus()`/`applyNetworkBillboard()`가 트랜지션 없이 현재의 진짜 위치로 덮어써 순간 이동처럼 보였던 것 — 실제로 `torusRotation`에 1.4라디안을 강제 주입해 재현·확인함. `smoothLateJoin()`을 새로 만들어 바운스가 끝나는 순간 그 카드에만 `transform .5s ease-out` 트랜지션을 걸고 나서 위치 재계산 함수를 호출해 옛 위치에서 현재 진짜 위치로 부드럽게 미끄러지도록 하고, 520ms 후 트랜지션과 z-index 오버라이드를 정리 — 착지 스핀 예외(`landingSpinSnap`)에도 동일 처리 추가. 셸을 완전히 숨기는 타이머는 이 미끄러짐까지 끝난 뒤에야 완료로 치도록 정리 시점도 함께 미뤘음. 목적지 값이 실제로 달라짐(보정이 진짜로 작동함)과 트랜지션이 실제 걸려있음을 실측 확인, 정착 후 트랜지션·z-index가 남김없이 정리되고 드래그·빌보드 추적도 그대로 정상임을 재확인. **도넛 최전면 카드 테두리 효과 추가**: "최전면 카드는 테두리가 번쩍 빛나고, 빛이 사그라들면 테두리를 타고 빛이 빙 도는" 순수 CSS 2단계 효과를 `.torus-card.front`에 추가 — (1) box-shadow 키프레임으로 0.6초간 밝은 흰색 링이 확 커졌다 원래 그림자로 잦아드는 플래시, (2) 0.55초 지연 후 `conic-gradient`+마스크(xor/exclude) 기법으로 테두리를 따라 도는 얇은 빛줄기가 2.4초 주기로 무한 회전(회전을 부드럽게 보간하려면 `@property --torusFrontAngle` 등록이 필수). 별도 JS 없이 기존 `layoutTorus()`의 `.front` 클래스 토글에 그대로 올라타 카드가 바뀔 때마다 자동으로 재생됨. `overflow:hidden`에 잘리지 않도록 링을 카드 바깥으로 튀어나오지 않게(`inset:0`) 배치. 검증 중 짧은 창(0.6초)을 스크린샷으로 잡기 어려워 처음엔 "이미 끝난 상태"만 보였으나 `getAnimations()`의 실제 재생 시각을 대조해 애니메이션 자체는 정상 실행 중이고 단지 측정 타이밍이 늦었을 뿐임을 확인, 배포 파일은 건드리지 않고 임시로 지속시간만 늘린 스타일을 주입해 플래시가 카드 주위에 뚜렷한 보라/흰색 후광으로 나타나는 것을 스크린샷으로 최종 확인. **효과 강화·색상·속도 재조정**: "빛을 더 강하게, 테두리 빛도 더 강하게, 색은 푸른색 계통으로, 돌 때는 고슴도치 같은 빛이 앞장서고 꼬리를 물며 따라가게, 회전 속도는 반 정도 늦게" — 플래시의 box-shadow 확산/블러를 대폭 키우고 보라→파랑으로 재색상, 체이스 그라디언트를 25도짜리 짧은 호에서 250~360도의 긴 꼬리(뒤로 갈수록 옅어짐)+끝부분 356~359.3도 구간에 밝기를 촘촘히 교차시켜 까칠까칠한 "고슴도치" 머리처럼 보이게 재설계, `drop-shadow` 추가로 발광 보강, 회전 주기 2.4초→4.8초로 정확히 2배 늦춤. **곧이어 재보고: 늦게 합류하는 카드가 여전히 튐** — 직전 수정(`smoothLateJoin`)은 바운스가 "끝나는 순간"만 매끄럽게 보정했을 뿐, 바운스 자체가 여전히 전환 시작 시점에 고정된 목적지 하나를 향해 WAAPI 키프레임으로 미리 다 그려두는 구조라, 그 몇 초 동안 계속 쌓인 링 회전 오차가 결국 "보정"이라는 형태로 드러날 수밖에 없었음. **근본 해결**: 도넛·네트워크의 진입(scatterIn) 바운스를 WAAPI에서 `requestAnimationFrame` 기반 실시간 추적 방식(`driveBounceLive`)으로 전면 교체 — 매 프레임 `layoutTorus()`/`applyNetworkBillboard()`를 직접 호출해 "지금 이 순간 진짜 올바른 위치"를 새로 얻어와 그걸 향해 계속 보간하고, 그 위에 감쇠하는 통통 튀는 오프셋만 얹음 — 목적지 자체가 매 프레임 살아있는 값이라 애초에 "굳어서 나중에 고쳐야 할 스냅샷"이 존재하지 않음(z-index 150 고정도 매 프레임 재적용해 회전 재계산에 의해 풀리지 않게 함). 퇴장(scatterOut)은 모드 전환 즉시 해당 루프가 가드로 차단되어 애초에 어긋날 일이 없으므로 기존 방식 그대로 유지. 검증: 바운스 도중 회전을 강제로 크게 주입한 뒤 위치를 50ms 간격으로 전 구간 샘플링해 "가장 큰 단일 구간 이동량 ÷ 전체 이동량" 비율을 계산 — 3회 반복 모두 0.07~0.21(진짜 튐이라면 1에 근접해야 함)로, 보정이 한 순간에 몰리지 않고 여러 프레임에 고르게 분산됨을 확인. **네트워크 브라우저 사진 흐림 문제**: "중앙 사진이 흐릿한데 손가락 모양(호버) 될 때 살짝 눌리면서 청명해진다, 눌림 효과는 없어도 되니 항상 청명하게, 대신 호버 시 테두리가 빛나면 된다"는 지적에 이어 "사실 모든 카드 사진이 다 흐리다, 촛점 맞춰 청명한 게 최우선, 기억해"로 확장 — 원인은 데이터 월에서 이미 한 번 겪었던 것과 완전히 같은 GPU 합성 레이어 페인트 무효화 버그(이미지는 이미 다 로딩됐지만 3D 카드 스택 안에서 화면에 반영이 안 되다가, 호버 등 우연한 리페인트가 일어나야 그제서야 보임 — 코드 어디에도 의도적인 "눌러서 포커스" 효과는 없었고, 순전히 이 버그의 부작용이었음). 데이터 월에서 썼던 것과 동일한 해법(`img.decode()` 후 `display` 토글로 강제 동기 리페인트)을 `forceMediaClarity()`로 일반화해 네트워크 허브·모든 노드의 새로 생성되는 이미지에 적용. 호버 효과는 기존에 허브/현재 노드만 제외됐던 호버 테두리 규칙의 빈자리를 채워 보라색 계열 글로우로 새로 추가(도넛과 달리 파란색 요청은 없었으므로 네트워크 기존 색감 유지) — 다만 Playwright의 합성 마우스 호버가 매 프레임 transform이 갱신되는 요소에는 `:hover` 상태를 안정적으로 못 붙잡는 자동화 한계로 이 부분만은 코드 검토(기존에 이미 쓰던 동일 패턴과 동일 문법)로 정확성만 확인하고 실측 검증은 완료하지 못함.)

이전 갱신: 2026-08-26 (모바일 밀도 개선 요청으로 시작해 하루 종일 이어진 세션. **포트폴리오/데이터 월 핀치(두 손가락) 인터랙션을 네 번 재설계 끝에 확정**: 1열↔2열 단순 토글 → 1~3단계 이산 스텝+FLIP 스냅(문턱값 넘기 전엔 반응 없음, 사용자 피드백으로 폐기) → column-width/그리드 auto-fill 기반 "연속처럼 보이는" 방식(실은 브라우저가 항상 꽉 채워 늘리므로 열 개수 바뀌는 순간에만 크기가 변하고 사이 구간은 안 변함, 뒤늦게 발견) → **최종: 열 개수(column-count)는 정수로 고정하고 그 사이 구간엔 `transform:scale()`로 실시간 미리보기(오므리면 계속 작아지며 우측에 여백, 벌리면 계속 커지며 이웃과 겹침)를 보여주다가 정확히 한 열이 바뀔 만큼 크기가 변하는 순간 그 열 개수로 재배열**. 핀치 중인 이미지의 화면 위치를 앵커 카드 기준으로 고정(전체 컨테이너 높이 변화량 기준 보정은 2→1열 등 큰 변화에서 화면이 엉뚱하게 튕기는 부작용으로 폐기, 앵커 카드 1장의 위치만 보는 방식으로 교체 후 드리프트 0 확인). 간격은 매 제스처마다 실측한 비율로 이미지 폭에 비례. 포트폴리오는 1~4열(기본 2, 3열=제목만/4열=이미지만 텍스트 단계적 숨김), 데이터 월은 2~6열(기본 5→여러 차례 조정 끝에 최종 4, 5~6열에서 카테고리 라벨 숨김). 실기기에서 핀치가 안 먹던 문제는 `touch-action` 미설정으로 브라우저 기본 핀치줌과 충돌한 게 원인이었음(도넛 브라우저에 이미 쓰던 `pan-y`로 해결). **포트폴리오 원본보기 오매칭 버그 발견·수정**: 도넛 그룹 계층에 속하지 않는 포트폴리오 전용 항목(좌상단 "비젠티브 브랜드 키비주얼" 등) 클릭 시 엉뚱한 그룹의 첫 항목이 열리던 버그 — 그룹을 못 찾으면 클릭한 항목이 실제로 들어있는 컬렉션을 기준으로 열도록 수정, 전체 카드 스캔 검증. **데이터 월 확대(원본보기 진입) 위치 어긋남을 두 차례 수정**: (1) 모으기 단계에서 카드를 앞으로 튀어나오게 하는 깊이(`--mag-z`)가 확대 단계까지 남아있어 3D 원근 때문에 중앙 이동량이 부풀려지던 문제(확대 시작 시 `--mag-z` 리셋), (2) 핀치 코드가 카드의 `transform-origin`을 `top left`로 바꿔두고 복원을 안 해서, 이후 클릭 시 우하단으로 커지던 문제(스케일 1로 복귀 시 origin도 함께 제거하도록 수정). **데이터 월 썸네일이 "로딩은 됐는데 화면에 안 그려지는" 렌더링 버그를 다섯 차례 시도 끝에 해결** — 처음 네 번(재시도 로직, 동시 요청 6개 제한, `IntersectionObserver` 독립 스크롤 폴백, `will-change` 리페인트 힌트)은 전부 "로딩이 실패했을 것"이라는 검증 안 된 가정 위에서 만든 수정이었고, 매번 "확인했다"고 보고했지만 실제로는 사용자 화면에서 안 고쳐졌음 — 사용자의 직접적 지적("왜 문제없다면서 화면에서는 안보이나?")으로 방향을 바꿔, 사용자의 실제 깨진 브라우저에서 진단 스크립트를 직접 돌려본 결과(`naturalWidth>0`, 즉 이미 다 로딩됨)로 **진짜 원인이 로딩이 아니라 GPU 합성 레이어 페인트 무효화 문제**(3D 카드 스택 특성상 새로 디코딩된 이미지가 레이어에 반영 안 됨, 슬립모드가 카드를 건드리면 그제서야 그려짐)임을 확인 — `img.decode()`로 픽셀이 진짜 준비된 시점을 감지해 그 순간 `display` 토글로 강제 동기 리플로우를 일으키는 방식으로 최종 해결, 사용자 실제 브라우저에서 확인 완료. **기타**: 주요 제작 범위(`.scope-grid`)를 PC 4열/태블릿 3열/모바일 2열로 세분화하고 서체·여백을 줄여 제목·항목이 한 줄에 들어오게 함, "홈페이지 제작" 서비스 카드 신규 추가, "Ways to Work"(요금제) 섹션 전체 삭제. 세션 마지막에 로컬 zip+git bundle과 원격(origin) 태그로 "베타4-모바일최적화" 백업 생성.)

이전 갱신: 2026-08-25 (정전으로 끊긴 세션 재개; 인터랙티브 네트워크 모드를 하루 동안 여러 차례 재설계 끝에 최종 규칙으로 확정 — **"카드간의 위치 이동은 절대 없다. 처음이나 끝이나. 그룹 카드나 말단카드나. 모든 카드는 고유의 궤적을 회전하는 것이고, 선택된 카드로 카메라가 이동하는 것이다"**(사용자 직접 지시, 영구 아키텍처 규칙). 이 규칙이 확정되기까지: 처음엔 클릭한 카드가 중앙(허브 자리)으로 날아가 확대되는 방식이었다가 → 카메라도 함께 움직이도록 추가했다가(카드 이동 + 카메라 이동 동시 진행) → "다른 카드 선택시 카메라 각도가 리셋된다"는 피드백으로 카메라 이동 기능을 제거했다가(카드만 이동하는 원래 방식으로 복귀) → 그래도 "말단 카드 재선택시 카메라는 안 움직이고 카드끼리 자리를 바꾼다"는 재현 및 위 영구 규칙 지시로 **정반대로 재설계**: 이제 모든 카드(그룹·말단 불문)는 선택 여부와 무관하게 항상 자기 궤도를 계속 돈다(위치·크기 절대 불변), 카메라(시야각)만 선택된 카드 쪽을 보도록 매끄럽게 이동한다. 카메라 이동은 CSS 트랜지션이 아니라 매 프레임 JS로 보간해 카드의 실시간 반대회전(빌보드)과 항상 일치하도록 구현(불일치 시 카드가 일그러져 보이는 문제 방지). **선택 카메라 이동 방식은 이후 두 차례 더 발전**: 1차로 "카메라가 계속 따라다니게 해줘, 선택된 카드는 중앙에 정지, 주변은 지구에서 보는 달·태양처럼 움직이게" 요청에 따라 클릭 시점 한 번 조준 방식을 **매 프레임 실시간 재조준하는 지속 추적** 방식으로 바꿨으나, 이 방식(카메라 각도를 매 프레임 강제로 다시 맞추는 것)이 곧바로 새 문제를 낳았다 — "각도를 바꿀 때 바뀐 궤적을 유지하게 해줘. 자꾸 원래로 돌아간다"(드래그해서 각도를 바꿔도 손을 떼는 순간 다시 원래 조준 각도로 스르륵 되돌아감). **2차(최종) 수정**: 카메라 각도를 강제로 맞추는 방식 자체를 버리고, **렌더링 좌표계의 기준점(원점)을 선택된 카드 위치로 옮기는** 방식으로 교체 — 선택된 카드는 이제 항상 "로컬 원점"에 오도록 매 프레임 좌표를 재계산하고, 로컬 원점은 카메라 각도와 무관하게 항상 화면 중앙에 투영되는 기존 불변식(허브가 항상 중앙에 오던 것과 동일 원리)을 그대로 재사용했다. 그 결과 카메라 각도(요/피치)는 순수하게 드래그 조작에 의해서만 바뀌게 되어, 드래그로 바꾼 각도가 더 이상 되돌아가지 않고 그대로 유지된다. 동시에 "선택된 카드가 중앙에서 크게 보이게 해줘" 요청도 반영 — 선택된 카드가 이제 허브와 같은 위치(중앙)를 차지하는 김에 크기도 허브와 같은 확대 배율로 부드럽게 커지도록(사라질 때도 부드럽게 원래 크기로) 만들었다. Playwright로 검증: 포커스된 카드가 화면 중앙에 서브픽셀 단위로 고정되고 실제 크기도 약 1.85배로 커짐을 확인, 드래그 후 1.5초 대기해도 카메라 각도가 드래그 이전 값으로 전혀 되돌아가지 않고(관성에 의한 자연스러운 감속만 있을 뿐) 유지됨을 확인, 이웃 카드는 여전히 화면상 크게 움직여 "달·태양이 도는" 효과가 살아있음을 확인. 또한 "블러 처리 오류를 수정했다고 했는데도 여전히 카드 화면이 흐리다"는 재지적으로 **진짜 원인을 재조사**: 이전에 제거한 CSS 블러 효과와는 별개로, 네트워크 카드가 데이터 월 그리드용 저용량 압축 이미지(`art-XX.webp`)를 쓰고 있었던 것이 원인이었다(동일 해상도지만 압축률이 더 높아 확대 시 흐릿하게 보임) — 도넛 브라우저 카드가 원래 쓰던 고화질 원본 이미지(`original-XX.webp`)로 교체해 선명하게 만들었다. 이에 따라 기존의 "카드가 허브로 날아가 확대", "선택 시 나머지 카드가 뒤로 빠짐(recede)", "그룹 진입시 하부 카드가 허브에서 튀어나오는 스폰 애니메이션", "허브 접기시 카드가 제 자리로 이동" 등 위치를 바꾸던 애니메이션은 전부 제거되고 전부 "불투명도 페이드"로 대체됨 — 부수 효과로 뒤로가기 반복시 그리드가 사라지던 버그의 근본 매커니즘 자체가 사라짐(더 이상 요소별 상태를 이어받는 복잡한 재사용 로직이 없음). 카메라 각도는 이제 사용자가 손으로 맞춘 뒤에도 선택 때문에 리셋되지 않고 유지됨(Playwright로 재검증: 재선택 반복시 카메라 각도가 매번 실제로 변경되고, 카드 위치는 궤도상 정상적인 미세 이동만 있을 뿐 순간이동/교체가 없음을 확인). 그 외 이번 세션에 확정된 것들: 그리드를 여섯 번의 재설계 끝에 "카드와 완전히 분리된 평면 레이어에 그리는 2D 파이프 라인"으로 확정(preserve-3d 안에서 z-index가 무시되는 문제가 최종 원인이었음); 가상 모드 전환 구조를 순차 진행에서 두 장면이 겹쳐 보이는 진짜 크로스페이드로 재구조화; 모드 전환 버튼 아이콘을 3차례 개선 끝에 "3등분 화살표 원" 모양으로 확정; 전환이 평면적이라는 피드백으로 카드에 무작위 3D 텀블링·궤적 편차 추가; 배경(상위 링) 그룹 카드도 클릭 가능하게 만들어 선택 시 현재 그룹을 닫고 곧바로 선택된 그룹으로 진입; 그리드 두께를 여러 차례 요청에 따라 축소(최종 0.85~1.05px); 배경 카드 블러 제거(채도 감소만 유지); 좌상단/우상단 버튼을 콘텐츠 쪽으로 20%가량 이동 + 마우스 오버 시 "상위로 이동"/"가상공간 전환" 풍선 도움말 추가; **"모든 상태 변화는 과정이 보여야 한다"는 사용자 영구 지침을 메모리에 저장**; **선택 카드 크기/구분 표시를 한 차례 더 다듬음**: 포커스를 잃은 카드가 원래 크기로 순간 팍 줄어들던 버그 수정(카드마다 자기 크기를 계속 부드럽게 추적하도록 변경), 포커스가 다른 카드로 전환될 때도 화면이 순간 이동하지 않고 부드럽게 이어지도록 수정, 말단 카드 포커스 시에는 동급(같은 단계) 카드들을 더 이상 흐리게 하지 않고 선명하게 유지, 대신 그룹으로 이어지는 카드에는 항상 보라색 테두리를 넣어 구분(포커스와 무관하게 항상 유지); **그룹 카드(허브) 자체가 자기 안의 멤버가 선택돼도 계속 크게 남아있던 버그 수정** — 지금까지 그룹의 중심 카드(허브)는 항상 고정된 확대 배율로만 그려졌는데, 이제 그 안의 멤버 카드가 선택되면 허브도 함께 원래 크기로 부드럽게 작아지도록 해서, 항상 "선택된 카드 단 하나만" 커 보이도록 통일함; **굵은 테두리 표시 방식을 "선택/중앙" 기준에서 "마우스 오버" 기준으로 전면 교체** — 선택되어 중앙에 있는 카드에는 이제 굵은 테두리를 주지 않고(사실은 예전부터 있던 허브 고유의 은은한 테두리 효과가 "항상 굵은 테두리가 있다"는 오해를 낳았던 것으로 실측 확인), 대신 마우스를 올린(hover) 카드에만 굵고 빛나는 테두리가 순수 CSS로 나타나도록 바꿈; **곧이어 같은 문제가 재발 — 이번엔 진짜 다른 원인**: 카드를 클릭하면 마우스 커서는 그 자리에 그대로 있는데 방금 선택된 카드가 화면 중앙으로 커지면서 이동해오다 보니, 전환이 끝났을 때 커서가 우연히 그 커진 카드 위에 그대로 놓여 있는 경우가 잦았다 — 이게 진짜로 마우스가 그 카드를 hover하고 있는 상태라서 순수 CSS만으로는 "의도적으로 올린 것"과 "우연히 남아있는 것"을 구분할 수 없었다. 선택된(중앙) 카드는 실제 마우스 hover 여부와 무관하게 항상 굵은 테두리 효과에서 제외되도록 예외 처리를 추가해 완전히 해결; **세 가지 추가 수정**: (1) 그룹 카드의 가느다란 테두리를 요청대로 완전히 제거; (2) 가상공간2 첫 진입 시 카드들이 기울어진 채 움직이다 갑자기 정면을 보던 문제를 CSS 트랜스폼 함수 목록 불일치로 수정했으나 **재보고됨(여전히 있음) → 진짜 원인을 다시 찾음**: 아래 참고; (3) "하부 카드들이 그룹 카드로 모여 없어진 뒤 그룹 카드가 제자리를 찾아가는" 접기 애니메이션이 이전 재설계 때 사라진 것을 확인하고 복원 — "카드 위치 이동 금지" 영구 규칙에 대한 명시적 예외로, 없어질 하부 카드가 자신을 흡수할 그룹 카드 쪽으로 모여드는 것으로 한정. **뒤이어 (2)의 진짜 원인 발견**: 화면 회전을 담당하는 컨테이너 자체의 CSS 회전값이 첫 진입 시 한 번도 초기화되지 않고 있었음(드래그를 하거나 1.2초 무입력 후에야 처음 설정됨) — 그 전까지는 각 카드가 "컨테이너가 이만큼 돌아가 있을 것"이라고 가정하고 계산한 역회전이 실제로는 존재하지 않는 회전을 상쇄하려 해서 전부 어긋나 보였고, 1.2초 뒤 컨테이너 회전값이 처음 설정되는 순간 전부 갑자기 맞아떨어지며 "정면을 향하는" 스냅이 발생한 것 — 카드 렌더링을 담당하는 함수가 매번 컨테이너의 회전값도 함께 맞춰 쓰도록 해서 다시는 어긋날 수 없게 근본적으로 고침. **그룹 카드 선택 시 하부가 펼쳐지는 과정도 함께 복원**: 아래 새 항목 참고; **레벨 전환 애니메이션을 "매번 다 지우고 새로 그리기"에서 "살아남는 카드는 진짜로 같은 화면 요소를 재사용해 부드럽게 이동"하는 방식으로 전면 재설계** — 그룹 접기 시 방금까지 중심이었던 카드가 실제로 다른 그룹들 사이 제자리로 이동하는 과정이 눈에 보이게 됐고, 다른 그룹으로 옮겨갈 때도 남아있는 상위 카드들이 사라졌다 다시 나타나지 않고 그 자리에서 매끄럽게 재배치됨 — 아래 새 항목 참고; **홈페이지 첫 진입 시 가상 공간 모드(도넛 브라우저/인터랙티브 네트워크)를 랜덤하게 선택하도록 변경** — 새로고침마다 둘 중 하나가 무작위로 먼저 보이며, 네트워크 모드가 첫 화면으로 뜨는 경우도 새로 검증해 문제없음을 확인함; **곧이어 "그리드가 깨진다" 재보고 → 새 경로가 처음으로 밟은 미검증 코드 경로에서 카드/연결선 초기 투명도 설정이 누락돼 있던 버그를 발견해 수정**했으나, 이 검증은 "페이지를 새로 열 때"만 반복 테스트한 것이었고 **사용자가 직접 "띄우고 나서 전환할 때 깨지는 거다, 그냥 띄우기만 하면 안 깨진다"고 정확한 재현 경로를 짚어줘서 재조사** → **진짜 원인**: 가상 모드를 껐다 켤 때마다(토글 버튼) 화면을 다시 그리는 두 지점이 "지금 화면에 뭐가 떠 있는지" 스냅샷을 넘기지 않아 옛 카드·연결선을 정리하지 않고 매번 새 세트를 덧붙이기만 해서, 전환을 반복할수록 화면에 안 보이게 얼어붙은 유령 카드/연결선이 계속 쌓이던 것(왕복 1회만으로 실측 카드 24개 vs 실제 추적 8개) — 두 지점 모두 그룹 진입/뒤로가기와 동일하게 스냅샷을 넘기도록 수정해 근본 해결, 왕복 3회·계층 2단계 깊이에서의 왕복까지 반복 검증해 유령 요소 0개 확인)

이전 갱신: 2026-08-23 (데이터 월 슬립 모드 자동 이동 재설계; 도넛 브라우저 회전 속도 연동 반경/확대 효과 및 관성 조정을 여러 차례 거쳐 확정(반경 200%·확대 125%), 대분류/하위 단계 카드 크기 공식 통일; 두 번째 가상 모드 "인터랙티브 네트워크"를 여러 차례 재설계 끝에 **태양계 궤도 모델**(대표 카드=중심 항성, 하위 카드=계속 공전하는 행성, 클릭 시 그 카드가 새 항성이 되며 확대)로 확정; 가상 모드 간 전환은 화면 안에서 벌어지는 방식→최고 속도 시 자동 폭발 전환 도입→다시 원래(화면 밖으로 흩어져 사라졌다 재등장)로 되돌림, 강제 리플로우 배치 처리로 끊김 제거, **원본 뷰어가 열려 있는 동안은 모드 전환을 아예 막아 "카드 1장만 남는" 고질 버그 근본 해결**, 네트워크 모드 상하 회전 무제한 개방)

이 문서는 대화 기록이 사라져도 다음 작업에서 현재 상태와 사용자의 확정 요구사항을 이어가기 위한 프로젝트 메모다.

## 프로젝트 위치

- 작업 폴더: `G:\강의\AI 숏폼 자동화\홈페이지`
- 운영 주소: `https://www.vizentive.co.kr/`
- GitHub: `https://github.com/gtkim1000/vizentive.git`
- 기본 브랜치: `main`
- 배포: GitHub `main` 푸시 → Vercel 자동 배포 → 가비아 연결 도메인 반영

## 절대 변경하지 않을 확정 문구

- 회사명: `비젠티브 | VIZENTIVE`
- 슬로건: `브랜드의 상상에 시선을 더하다`
- 서비스 메뉴: `AI 모델`, `광고 소재`, `원페이지 릴스`, `카드 뉴스`
- AI 비디오는 서비스하지 않는다.

## 현재 홈 화면

- 왼쪽에 슬로건과 서비스 바로가기 메뉴가 있다.
- 서비스 바로가기 네 항목은 보라색이며 선택 시 해당 서비스 카드로 이동한다.
- `포트폴리오 보기`, `프로젝트 문의하기` 버튼은 서비스 바로가기 아래에 있다.
- 회전 안내 문구와 좌우 화살표 아이콘은 제거됐다.
- 홈 전용 배경 이미지는 아래 파일만 사용한다.
  - `public/hero/vizentive-home-bg.png`
- 홈 이미지는 데이터 월에 포함하지 않는다.
- 홈 이미지는 정사각형 1:1 비율, 잘림 없는 `object-fit: contain`, 투명도 50%다.
- 이미지 위치는 PC에서 위로 103px, 모바일에서 위로 30px 이동한다.

## 모바일 홈 및 도넛 배치

- 모바일 슬로건 크기: `2.8rem`
- 모바일 도넛은 고정 좌표로 배치하지 않는다.
- `포트폴리오 보기`, `프로젝트 문의하기` 버튼의 실제 하단을 측정한다.
- 버튼 하단에서 화면 높이의 3%만큼 간격을 둔 다음 도넛을 배치한다.
- 정보 패널과 대분류 이동 버튼도 도넛 위치에 연동한다.
- 관련 함수: `syncMobileTorusPosition()`
- 화면 크기 변경 및 웹폰트 로딩 완료 시 위치를 다시 계산한다.

## 도넛 브라우저 동작

- 마우스 드래그 및 모바일 스와이프로 회전한다.
- 전면이 아닌 이미지를 선택하면 전면으로만 이동한다.
- 최전면 대표 이미지를 선택하면 하부 이미지로 들어간다.
- 최전면 하부 이미지를 선택하면 고해상도 원본 보기로 들어간다.
- 최전면 이미지는 조금 더 크게 표시한다.
- 후면 이미지도 흐리게 만들지 않고 최대한 선명하게 표시한다.
- 대표 이미지 설명은 대분류 상태에서만 표시한다.
- **최상위(대분류)와 하위(하위그룹/작품) 단계의 카드 크기·움직임은 완전히 동일하다(2026-08-23 통일)**: 원래 `layoutTorus()`의 카드 크기 공식이 `torusMode==='groups'`일 때(`.66+depth*.4`, depth에 선형 비례)와 그 외 단계일 때(`.72+depth^8*.34`, 대부분 구간에서 거의 평평하다가 맨 앞에서만 급격히 커짐)로 서로 달랐다 — 이 때문에 하위 단계에서는 양 끝(옆면) 카드가 대분류 단계보다 눈에 띄게 작아 보였고, 사용자는 이를 "출렁임이 더 많다"고 체감했다(실제로는 파도 진폭 자체가 다른 게 아니라 카드가 작아서 상대적으로 더 흔들려 보이는 것이었다 — `waveY`/`baseY`/`emphasis` 등 다른 움직임 공식은 애초부터 모드 무관 공통이었음). 사용자 요청으로 하위 단계 공식(`.72+depth^8*.34`)으로 완전히 통일 — 이제 `torusMode` 조건 분기 자체가 없다. Playwright로 depth→scale 관계를 두 모드에서 각각 재구성해 오차 0으로 완전히 동일함을 확인.
- **회전 속도 연동 반경 확대 + 카드 확대, 최종 확정 동작(2026-08-23)**: 빠르게 드래그해서 돌릴수록 ① 카드들 사이의 **가로 간격**(원심력처럼, `rx`만)이 최대 **200%**(`torusSpinRadiusBoostMax=1`)까지 벌어지고, ② 카드 자체의 **확대(스케일)**도 별도로 최대 **125%**(`torusSpinScaleBoostMax=.25`, `.1`→`.2`→`.3`→`.25`로 재조정, `compositeScale`에 곱함)까지 커진다 — 이 둘은 서로 독립된 상태값(`torusRadiusBoost`/`torusScaleBoost`)이며 `layoutTorus()`에서 각각 적용된다. **단, 깊이(`rz`)는 절대 건드리지 않는다** — 한때 `rz`도 같이 키운 적이 있었는데, 카드가 카메라에 가까워지면서 고정된 CSS `perspective` 때문에 원근 효과로 카드가 화면을 덮을 만큼 확대되어 보이는 부작용이 있어 완전히 되돌렸다(스케일은 오직 명시적 `compositeScale` 곱셈으로만 조절, `rz`는 항상 기본값 고정). Playwright 실측: 가로 반경 배율 ~2.02배, 카드 스케일 배율 ~1.11배로 목표치와 정확히 일치, 부작용 없이 깔끔하게 동작 확인. 두 효과 모두 속도→목표값 민감도(`torusSpinRadiusBoostScale=6.25`, 공유)와 느린 복귀(각각 `torusSpinRadiusBoostFallRate`/`torusSpinScaleBoostFallRate=.01`, 관성 감쇠와 분리)를 따른다. 클릭 선택·카테고리 스냅 등 회전이 멈추는 모든 상황에서 동일하게 적용. **다시 조정 요청이 오면 각 최대치(`torusSpinRadiusBoostMax`/`torusSpinScaleBoostMax`)와 복귀 속도만 건드리고, `rz`는 절대 손대지 말 것.**
- ⚠️ 이 절의 변경들이 2026-08-23 세션 내내 아직 `main`에 커밋·배포되지 않은 상태였다(로컬 `dist/`·`localhost:8000` 기준으로만 매번 Playwright 실측 검증). 배포(`git push` → Vercel 자동 배포)는 사용자가 명시적으로 "배포해줘"라고 할 때만 진행한다는 규칙(`AGENTS.md`) 때문.
- **회전 관성(손 뗀 뒤 서서히 멈추는 것) 감쇠 속도(2026-08-23, 두 차례 조정)**: `spatialFrame()`의 `torusVelocity*=RATE`(프레임당 감쇠율)를 원래 `.94`에서 `.9695`(1차, "두 배 더 느리게")를 거쳐 최종 **`.9846`**(2차, "관성이 좀 더 오래 지속되도록, 2배 정도")로 변경 — 매번 "N배 느리게"는 `기존값**(1/N)`(= `sqrt`, N=2일 때)로 계산해서 "지수적으로 반으로 줄어드는 데 걸리는 시간(half-life)"이 정확히 N배가 되도록 함(`.94`→`.9695`→`.9846` 각 단계가 half-life 2배씩). Playwright로 `.torus-card.front`가 바뀌는 시점을 추적해 관성 지속 시간이 이전(~2.8초)보다 늘어난 것(~5.9초)을 실측 확인. 이 상수는 위 반경/확대 효과(`torusSpinRadiusBoostScale`이 이 속도값을 그대로 참조)와도 연결되어 있어, 관성이 느려지면 반경·확대가 커져 있는 시간도 자연히 함께 길어짐 — 의도된 상호작용이며 별도 보정 불필요. 다시 "더 오래/짧게 돈다"는 요청이 오면 이 감쇠율 하나만 `기존값**(1/N)` 또는 `기존값**N`으로 재계산(절대 가까운 소수점을 감으로 넣지 말 것), 관성 정지 임계값(`.00002`)은 그대로 둔다.
- **가상 공간 모드 전환 버튼(2026-08-23 추가, 스캐폴드만)**: 우상단에 항상 표시되는 원형 아이콘 버튼(`#torusModeToggle`, 글자 `⟳`)을 추가 — 좌상단의 `torusBack`(하위 그룹 진입 시에만 보임)과 달리 모드와 무관하게 **항상** 떠 있다. 클릭하면 `spatialModes=['torus','placeholder']` 배열을 순환하며 `#spatialIntro`의 `data-spatial-mode` 속성을 바꾼다(`torus`↔`placeholder`). `placeholder`가 되면 CSS로 `.torus-shell`/`.hologram`/`.torus-back`/`.scene-counter`를 숨기고 `.spatial-placeholder`("모드 2 준비 중")를 대신 보여준다 — **아직 실제 두 번째 가상 모드는 디자인되지 않았고, 사용자가 명시적으로 "버튼·전환 구조만 먼저" 만들라고 요청**해서 지금은 자리만 잡아둔 상태. 실제 두 번째 모드(어떤 3D/공간형 레이아웃일지)는 다음 요청에서 구체화 예정이며, 그때 `spatialModes` 배열에 값 하나 추가 + 해당 모드 렌더링 함수 연결로 확장하면 됨(모드 개수는 향후 더 늘어날 수 있음, 배열 기반이라 확장 용이). **자동 전환 모드(도넛을 너무 빠르게 돌리면 회전이 "터져서" 다른 모드로 자동 재조합되는 연출)는 사용자가 명시적으로 "아직 코딩하지 말아달라"고 해서 미구현** — 나중에 요청 오면 그때 설계.
- 버튼 위치는 반드시 **고정 px로 헤더(78px, 모바일 640px 이하는 70px)를 기준으로 여백을 둬야 한다** — 처음에 `top:4%` 같은 퍼센트로 배치했다가 짧은 섹션 높이에서 실제 계산값이 헤더 아래로 못 내려가 고정 헤더(`z-index:80`)가 클릭을 가로채는 문제가 있었음(Playwright로 클릭 타임아웃 발생시켜 발견). `hologram`은 `pointer-events:none`이라 시각적으로 살짝 겹쳐도 클릭은 안 막히지만, 실제 클릭 가능한 `.header`와는 항상 좌표를 띄워야 함.

## 가상 공간 모드 2 추가 개선 — 다중 링 공존 + 그리드 상시 노출 + 클러스터링/전환 버그 수정(2026-08-23, 같은 날 후속 라운드)

태양계 모델 확정 직후 실사용 피드백으로 4가지를 더 고쳤다: (1) 최상위(허브 없음)에서 카드 사이 연결선(그리드)이 아예 안 보이던 것을 상시 노출로, (2) 그룹 카드를 클릭하면 화면이 하위 카드로만 완전히 대체되던 것을 상위/하위가 함께 보이는 방식으로, (3) 가상 모드를 여러 번 왕복하면 네트워크 카드가 전부 화면 정중앙으로 뭉쳐버리던 버그, (4) 모드 전환 시 카드가 흩어졌다가 다시 나타나는 과정이 안 보이고 "퍽" 하고 순간 이동하듯 보이던 버그.

- **다중 링(계층 공존) 구조로 전면 재설계**: 기존엔 `networkMode`/`networkItems` 단일 레벨만 렌더링했지만, 이제 `networkStack`(배열, 0번=최상위 groups, 마지막=현재 보고 있는 레벨)을 도입해 **스택에 쌓인 모든 레벨을 동시에 렌더링**한다. 가장 안쪽 링(ring 0, 방금 진입한 레벨의 자식들)은 원래대로 크고 뚜렷하게, 그 바깥 링(ring 1=부모 레벨, ring 2=조부모 레벨)은 반지름을 더 크게(`NETWORK_RING_GAP=.75`배씩 누적), 크기(`nodeScale`, 링당 -16%, 최소 55%)와 투명도(`nodeOpacity`, 링당 -22%, 최소 45%)를 낮춰 "배경으로 물러난 상위 카드"처럼 보이게 한다. 궤도 공전 속도도 바깥 링일수록 느려진다(`NETWORK_RING_SPEED_FALL`) — 큰 구조일수록 천천히 도는 것이 우주 비유에 더 맞는다는 판단. **바깥 링 카드는 클릭 불가**(`interactive:false`, `<button>` 대신 `<div>`로 렌더링 + CSS `pointer-events:none`)로 만들어, "장식으로만 공존하고 실제 조작은 현재 레벨에서만" 하는 단순한 규칙으로 정리 — 배경 카드를 클릭해 그 레벨로 점프하는 기능은 이번엔 넣지 않음(필요하면 후속 요청으로).
- **그리드(연결선)를 항상 렌더링** — 2026-08-25 재설계: 처음엔 모든 링의 모든 카드가 원점으로 연결선을 그리게 했으나 실사용 피드백("그리드 형태가 이상하다, 잘 안 보인다")으로 **링(궤도) 단위의 원형 그리드**로 교체했다. 상세는 아래 "2026-08-25 추가 개선" 절 참고.
- **back(뒤로 가기)는 스택 pop으로 단순화**: 예전엔 `networkGroupIndex`+`torusGroups[...].direct` 조합으로 부모를 역산하는 별도 로직이 있었는데, 스택 모델에서는 그냥 `networkStack.pop()`만 하면 자동으로 정확한 부모 레벨(및 그 부모의 등급별 분기: 하위그룹이 있었는지 없었는지)로 복원된다 — 로직이 크게 단순해짐.
- **클릭 인덱스 처리 주의사항(구현 중 발견)**: 여러 링을 하나의 `networkNodes` 배열로 합치면서, "전역 배열 인덱스"와 "현재 레벨(`networkItems`) 안에서의 인덱스"가 더 이상 같지 않게 됐다(링 0이 배열 뒤쪽에 push되므로). 리프 카드를 두 번째 클릭했을 때 `openModal(index, networkItems)`을 호출하던 부분이 잘못된 인덱스를 넘길 뻔한 버그라 각 노드에 `itemIndex`(자기 레벨의 원본 배열에서의 위치)를 별도로 저장해 그걸 넘기도록 수정 — **여러 소스를 하나의 평면 배열로 합칠 때는 "전역 위치"와 "원래 그룹 내 위치"를 반드시 별개 필드로 유지할 것**, 하나로 착각하면 조용히 엉뚱한 아이템이 열린다.

### 버그: 가상 모드를 여러 번 왕복하면 네트워크 카드가 화면 정중앙으로 뭉침
원인: `.network-shell`은 CSS로 `display:none`(비활성 모드일 때) 처리되는데, `transitionSpatialMode()`가 **모드가 실제로 바뀌기 전에** "정리 차원에서" 네트워크를 항상 다시 빌드하고 있었다(`if(networkInitialized){...rebuild...}`, 조건에 목표 모드 여부가 없었음). 도넛 모드에 있다가 네트워크로 넘어가는 순간에는 이 재빌드가 **네트워크 셸이 아직 숨겨진(0×0 크기) 상태에서** 실행되어, 화면 크기를 재는 `measureNetwork()`가 `getBoundingClientRect()`로 0을 읽어 궤도 반지름 `r=0`이 되고, 결과적으로 모든 카드의 좌표가 `(0,0,0)`(정중앙)으로 붕괴했다. 이후 화면이 실제로 네트워크로 바뀌어도 이미 반지름 0으로 계산된 좌표를 그대로 쓰니 카드가 전부 중앙에 뭉쳐 보인다. **수정**: 재빌드 조건을 `networkInitialized && 현재모드==='network'`(즉 "지금 네트워크를 떠나는 참이라 화면이 아직 보이는 시점")로 한정 — 숨겨진 상태에서는 절대 재측정하지 않는다. 추가로 `measureNetwork()` 자체에도 방어 코드를 넣어, 측정값이 0이면 마지막으로 유효했던 반지름(`lastNetworkRadius`)을 그대로 쓰도록 해 비슷한 계열의 버그가 재발해도 카드가 중앙에 뭉치는 대신 이전 크기를 유지하게 안전장치를 걸었다. **교훈**: `display:none`인 요소의 크기를 재는 코드는 "그 요소가 진짜 화면에 보이는 시점"에만 실행되도록 호출 시점을 엄격히 통제해야 한다 — 안 보이는 동안 측정하면 0이 나오는 게 당연한데, 그 0이 다른 계산(궤도 반지름 등)에 조용히 스며들면 훨씬 나중에야 "카드가 뭉친다"는 형태로 튀어나와 원인 추적이 어렵다.

### 버그: 전환 시 카드가 흩어졌다가 나타나는 과정이 안 보이고 "퍽" 하고 순간 이동
원인: `scatterIn` 계열 함수들이 "① 모든 카드를 흩어진 위치로 순간 이동(트랜지션 없음) → ② 컨테이너 리플로우 강제 → ③ 곧바로 같은 동기 실행 흐름 안에서 트랜지션을 건 자연 위치로 재이동"의 순서를 쓰고 있었는데, 강제 리플로우 직후 **같은 틱(tick) 안에서** 곧바로 스타일을 다시 바꾸면, 브라우저가 ①과 ③ 사이의 "흩어진 상태"를 한 번도 실제로 페인트하지 않고 두 스타일 변경을 하나의 프레임으로 합쳐버리는 경우가 있다 — 그 결과 트랜지션이 "재생"되는 게 아니라 최종 상태로 즉시 점프한 것처럼 보인다. **수정**: 리플로우 이후 ③(트랜지션 적용 + 최종 위치 지정)을 `requestAnimationFrame`을 **두 번 중첩**해서(`requestAnimationFrame(()=>requestAnimationFrame(()=>{...}))`) 미뤘다 — 브라우저가 반드시 "흩어진 상태"를 한 프레임 실제로 그리고 난 다음에야 최종 상태로의 전환이 시작되도록 강제하는, 잘 알려진 안정적인 패턴. Playwright로 전환 중간 지점(스크린샷 여러 장)을 찍어 카드가 작고 흐릿하게 시작해서 점점 커지고 선명해지는 중간 프레임이 실제로 보이는 것을 확인(수정 전에는 이 검증 방법 자체가 없어 놓쳤던 부분 — `style.transform` 값을 JS로 읽는 것만으로는 이 버그를 발견할 수 없고, 반드시 화면을 실제로 캡처해서 눈으로 확인해야 한다는 것도 이번에 배움).

### 버그: 리프 카드를 포커스(1차 클릭)한 뒤 같은 카드를 다시 눌러도 뷰어가 안 열림
다중 링 도입 후 스트레스 테스트 중 발견(다중 링이 아니어도 이론상 존재했을 오래된 버그): 포커스된(화면 중앙으로 확대된) 카드는 `applyNetworkBillboard()`의 매 프레임 갱신 대상에서 제외되는데(`entering` 클래스가 있으면 스킵), 이때 `z-index`도 그 카드가 마지막으로 가졌던 값에 **그대로 멈춘 채** 남는다. 반면 나머지(계속 공전 중인) 카드들은 매 프레임 새로 순위를 매겨 z-index가 계속 바뀐다 — 시간이 조금만 지나면 공전 중인 다른 카드가 우연히 더 높은 z-index를 받아, 화면 정중앙(포커스된 카드가 있는 바로 그 자리)을 지나가면서 포커스된 카드를 완전히 가려버린다. 그 상태에서 "같은 카드"를 다시 클릭한 사용자의 클릭은 실제로는 위를 덮은 다른(공전 중인) 카드를 클릭한 게 되어, 뷰어가 열리는 대신 포커스가 엉뚱한 카드로 옮겨간다. **수정**: 포커스 시작 시 그 카드에 `z-index:999`를 명시적으로 고정(다른 카드가 절대 못 덮도록)하고, 동시에 나머지 형제 카드들에는 `pointer-events:none`을 걸어(포커스 해제 시 복원) 설령 시각적으로 겹치더라도 클릭을 가로채지 못하게 이중으로 막았다. Playwright로 900ms 대기(공전이 한 바퀴 가까이 돌 만큼 충분히 긴 시간) 후에도 4라운드 연속 정상적으로 뷰어가 열리는 것을 확인.

### 2026-08-25 추가 개선 — 그리드 재설계 + 그룹/현재 레벨 시각적 차별화 + 선택 시 흐려지며 뒤로 빠짐

정전으로 끊긴 세션을 재개, 위 라운드(2026-08-23)의 실사용 피드백 4가지를 추가로 반영했다.

- **그리드를 "카메라를 항상 정면으로 보는 궤도 링"으로 교체**: 처음엔 각 링을 실제 3D 궤도 평면에 눕혀서(`rotateX(90deg)`) 렌더링했는데, 기본 카메라 피치(`-8°`)가 거의 0에 가까워 원이 옆에서 본 것처럼 거의 실선 두께로 찌그러져 사실상 안 보였다(`그리드가 잘 안 보인다` 원인). **수정**: 카드처럼 매 프레임 `rotateY(-yaw) rotateX(-pitch)`로 카메라를 항상 정면으로 보게(빌보드) 만들어, 레이더 화면 같은 뚜렷한 동심원으로 항상 보이게 했다(`renderNetworkOrbits()`+`applyNetworkBillboard()`). 최상위(허브 없음)에도 링 0 궤도 원 하나가 항상 존재해 "그리드 상시 노출" 요구를 그대로 만족. 허브→자식 스포크 선(`.network-edge`)은 원래 의도대로 링 0(현재 레벨의 자식)에만, 허브가 있을 때만 그린다 — 예전에 모든 링이 원점으로 선을 그리던 방식(관계없는 조상 카드들이 현재 허브로 선이 모이는 모양)이 "그리드 형태가 이상하다"의 원인이었다.
- **그룹(상위 링) vs 현재 레벨(링 0) 크기 차이 확대**: `NETWORK_RING_GAP`(링간 반지름 배율) `.75`→`1.1`, `NETWORK_RING_SCALE_FALL`(링당 축소율) `.16`→`.3`(바닥 `.42`), `NETWORK_RING_OPACITY_FALL`(링당 투명도 감소) `.22`→`.38`(바닥 `.28`)로 대폭 확대. 추가로 링>0 카드에는 인라인 `filter:saturate() blur()`(링 번호에 비례)를 걸어 "배경으로 물러난" 느낌을 스케일·투명도와 함께 이중으로 강화 — "그룹과 그룹 아닌 것이 섞여 구분이 안 된다" 해결.
- **카드 선택 시 나머지는 사라지지 않고 흐려지며 뒤로 빠짐**: 리프 1차 클릭(`focusNetworkNode()`)과 그룹 드릴다운(`enterNetworkNode()`) 모두 기존엔 선택 안 된 카드를 `opacity:0`으로 완전히 숨겼는데("선택한 카드만 덩그러니 보인다" 원인), 이제 `NETWORK_FOCUS_DIM_OPACITY=.16`으로 흐리게만 만들고, 동시에 `applyNetworkBillboard()`에서 선택 안 된 카드(허브 포함)에 `NETWORK_FOCUS_RECEDE_SCALE=.7`·`NETWORK_FOCUS_RECEDE_Z=-190`(카메라에서 멀어지는 방향, `.torus-card.diving`과 동일 부호 규약)을 적용해 실제로 뒤로 물러나는 것처럼 보이게 했다. 선택된 카드는 기존대로 `z-index:999`+허브 스케일로 확 앞으로. `clearNetworkFocus()`가 이전엔 방금 포커스 해제된 카드 하나만 복원하던 비대칭 버그도 겸사겸사 수정(이제 전체 노드/엣지/허브를 base 상태로 복원) — `restoreNetworkView()`는 이제 그냥 `clearNetworkFocus()`의 별칭.
- **검증 중 발견한 셀렉터 함정**: 다중 링 렌더링 이후 `.network-node[data-index]`는 더 이상 "클릭 가능한 카드만"을 뜻하지 않는다 — 배경(링>0, `<div class="network-node--bg">`) 카드도 `data-index`는 그대로 갖고 있고 단지 `<button>`이 아니고 CSS `pointer-events:none`일 뿐이다. 클릭 가능한 카드만 골라야 하면 `button.network-node[data-index]`로 질의해야 한다 — 처음 Playwright 검증에서 이걸 놓쳐 "중심 카드 재클릭 시 원본 보기가 안 열린다"는 가짜 회귀로 오인했었다(실제로는 검증 스크립트가 클릭 불가능한 배경 카드를 잘못 골라서 발생한 스크립트 버그, 실제 사이트 동작은 정상이었음). Claude 메모리 `torus-network-mode`에도 이 정정을 기록해 둠.
- 위 3가지("그룹1 클릭시 상위/하위 공존", "전환 반복 시 중앙 뭉침", "전환 시 퍽 나타남") 자체는 2026-08-23 라운드에서 이미 근본 수정되어 있었고, 이번 라운드에서도 Playwright로 재검증해 여전히 정상 동작함을 확인했다(전환 3회 반복 후에도 카드 퍼짐 폭 ~720×360px로 중앙 뭉침 없음, 흩어짐 중간 프레임이 스크린샷에 실제로 보임).

### 버그(재발): "전환 시 퍽 나타남"이 실제로는 도넛→네트워크 방향의 **두 번째 이후 진입**에서만 여전히 남아있었음
위 검증 직후 사용자가 "그런 과정이 안 보이고 갑자기 전부 나타난다... 두 번 해서 원래대로 돌아가 버린다"고 재보고 — 재검증 방법의 허점이 원인이었다. **진짜 원인**: `applySpatialMode()`가 네트워크 모드 진입 시 부르는 `ensureNetworkBuilt()`가 "**최초 1회만** 재구성"하도록 되어 있었다 — 두 번째 이후 도넛→네트워크 전환에서는 지난번 나갈 때 화면 밖으로 흩어진 채 멈춰있던 낡은 좌표를 "자연 위치"로 착각해 그 방향으로 애니메이션했다. 즉 실제 복귀 애니메이션은 화면 밖 어딘가에서 벌어져 안 보였고, 전환이 끝나 렌더 루프가 재개되는 순간(`applyNetworkBillboard()`가 매 프레임 궤도 데이터에서 위치를 다시 계산) 카드가 올바른 자리로 한 프레임 만에 "순간 스냅"됐다 — 이게 "퍽 나타남"의 정체. 애니메이션이 멈춘 것처럼 보여 사용자가 혼란스러워 다시 클릭하면, 이번엔 정상 작동하는 네트워크→도넛 방향이 걸려 "두 번 눌러서 원래대로 돌아간" 것처럼 보였다 — 두 신고가 같은 원인. **수정**: `ensureNetworkBuilt()`가 매번(최초 포함) `syncNetworkCurrent()`로 재동기화하도록 변경 — 이 함수가 불릴 시점엔 이미 CSS 모드가 전환되어 셸이 보이는 상태이므로(`.network-shell{display:block}` 적용 후), "숨겨진 0×0 상태에서 재측정하면 중앙에 뭉친다"는 예전 버그를 다시 불러오지 않는다. **검증 방법의 교훈**: 이전 라운드 검증은 network→torus 방향의 중간 프레임과 torus→network 방향의 "완전 정착 후" 상태만 캡처해서 이 비대칭 버그를 놓쳤다 — 토글의 양방향 모두, 그리고 최초 1회가 아니라 3회 이상 반복한 뒤의 중간 프레임을 반드시 캡처해야 한다. 재수정 후 3번째 토글(도넛→네트워크 재진입)의 150/270/570ms 중간 프레임을 캡처해 570ms 시점에 이미 정상적인 카드 배치가 보이는 것으로 확인.

### 버그(더 심각한 재발): "전환이 제대로 안 된다, 자꾸 원래대로 돌아간다" — 회전 관성이 전환 시 초기화되지 않음
위 수정 직후 사용자가 다시 "전환이 잘 안 되고 자꾸 원래대로 돌아간다"고 보고 — 이번엔 완전히 다른, 더 심각한 원인이었다. `transitionSpatialMode()`가 드래그 플래그(`networkDragging` 등)는 초기화하면서도 **회전 관성 속도값(`torusVelocity`/`networkVelocityYaw`/`networkVelocityPitch`)은 전혀 초기화하지 않았다**. 빠르게 돌리는 것 자체가 전환을 유발하는 조건(수동 클릭 도중이든, 최고 속도 자동전환이든)이라 이 잔여 속도가 전환 후에도 그대로 남고, 특히 네트워크 쪽 속도는 도넛 모드에 있는 동안 전혀 감쇠되지 않는다(`networkFrame()` 자체가 안 돌기 때문). 그 결과 네트워크 모드로 다시 들어가는 순간, 그 첫 프레임의 최고속도 자동전환 검사가 여전히 임계값을 넘는 낡은 속도값을 보고 **즉시 다시 튕겨나갔다** — 그리고 이 값을 소비/감쇠시킬 방법이 전혀 없었기 때문에, 한 번 발동되면 페이지를 새로고침하기 전까진 네트워크 모드에 사실상 영구히 못 들어가는 상태가 됐다. **수정**: `transitionSpatialMode()` 맨 앞에서 세 속도값을 모두 0으로 초기화 — 드래그 플래그를 초기화하는 것과 같은 자리에 나란히 추가. Playwright로 속도를 임계값 이상(20, 임계값 16)으로 인위 주입한 뒤 자동전환이 발동해 속도가 즉시 0으로 리셋됨을 확인하고, 그 직후 수동으로 다시 네트워크 모드에 진입해 2초 이상 추가로 지켜봐도 튕겨나가지 않고 안정적으로 유지됨을 확인했다(이전엔 이 시점에서 즉시 재발동했을 것). **교훈**: "이 상호작용/세션은 끝났다, 깨끗하게 시작한다"는 의미의 전환 지점은 관련된 모든 관성/속도 상태를 초기화해야 한다 — 플래그만 지우고 숫자형 관성값은 그대로 두면 그 자체로는 문제없어 보여도, 별개의 임계값 기반 자동 트리거와 결합해 스스로 반복되는 루프가 될 수 있다.

### 버그(세 번째 재발): 도넛 쪽에도 똑같은 "낡은 좌표를 자연 위치로 착각" 버그가 그대로 남아있었음
사용자가 다시 "중간 과정이 자꾸 연계가 안 되고 끊깁니다... 갑자기 나타나거나 합니다"라고 보고. 이번엔 **네트워크→도넛 방향(재진입)에 똑같은 병이 처음부터 안 고쳐진 채 남아있었다** — `applySpatialMode()`에 애초에 `mode==='torus'` 분기 자체가 없어서, 도넛으로 돌아올 때 `layoutTorus()`를 새로 부르는 코드가 전혀 없었다. 도넛이 화면 밖에 있는 동안(`spatialFrame()`이 `!isOnScreen(torusShell)`이면 `layoutTorus()`를 건너뜀) 카드의 `.style.transform`은 지난번 나갈 때 흩어진 좌표에 그대로 얼어붙어 있었고, `scatterInTorus()`가 이걸 "자연 위치"로 읽어 그쪽으로 애니메이션했다 — 네트워크 쪽과 완전히 같은 원인. **수정**: `applySpatialMode()`에 else 분기 추가, 도넛 진입 시 `torusMetrics=null;layoutTorus();noteTorusInput()`. **수정 중 새 버그 유발 후 즉시 발견·수정**: 이 코드를 조건 없이 넣었더니 페이지 부팅 시 최초 1회 자동 호출되는 `applySpatialMode()`(스크립트 맨 아래, `torusMetrics` 등 여러 `let` 선언보다 코드상 **앞쪽**에서 실행됨)에서 아직 초기화 안 된 변수를 참조해 `ReferenceError`가 터지고 그 뒤 스크립트 전체가 조용히 멈춰버렸다(Playwright 스모크 테스트로 즉시 발견) — `torusScene.children.length`가 있을 때만(부팅 시점엔 아직 카드가 없음) 실행하도록 가드를 추가해 해결. **교훈**: 여러 호출 지점을 가진 함수에 "새로 리빌드" 코드를 추가할 때는 그 함수가 불리는 **모든** 지점의 타이밍(특히 부팅/초기화 시점 호출)을 다 확인해야 한다.

### 그리드 — 네 번의 재설계 끝에 "화면 2D 좌표로 그리는 파이프 라인"으로 확정
1차(궤도 원만, "원으로 나온다") → 2차(원+십자선, "고정이야, 연결 안 됨") → 3차(카드마다 원점으로 이어지는 실시간 연결선, 3D 회전으로 위치 지정 + z-index를 소유 카드와 동률로) — 이 3차에서 가시성 문제(z-index로 항상 카드에 가려짐)는 해결됐지만, 곧바로 **"모드 전환할 때 연동 안 됨"**(전환 애니메이션은 카드 translate3d만 다루는데 선은 항상 원점(0,0,0)에 고정이라 흩어짐에 반응하지 않음), **"회전하면 굵어졌다 가늘어짐"**(선을 진짜 3D 평면으로 회전시키다 보니 카메라 각도에 따라 원근 압축으로 두께가 실제로 변함), **"그리드가 카드 위에 옴"**(소유 카드와 z-index를 맞춰서 생긴 부작용) 세 가지가 연쇄로 드러났다. **네 번째(최종)**: 세 문제 모두 "선을 3D 공간에서 실제로 회전시킨다"는 표현 방식 하나에서 나온 증상이라는 걸 깨닫고, 표현 자체를 **순수 2D 화면 좌표계**로 바꿨다 — 매 프레임 카메라 회전 행렬로 카드의 픽셀 위치를 투영(CSS `perspective`와 동일한 공식으로 직접 계산)해 화면상의 실제 거리·각도를 구하고, 선은 `rotateY/rotateZ`(3D) 대신 평범한 `rotate(deg)`(2D)로만 그린다. 2D 아핀 회전은 원리상 두께가 절대 안 변하므로 "굵기 요동" 문제가 근본적으로 사라지고(카메라 회전 전후 `getComputedStyle().height`가 4px→4px로 완전 동일함을 확인 — `getBoundingClientRect().height`로는 검증 불가, 회전된 2D 박스는 실제 두께가 그대로여도 축 정렬 바운딩박스 높이는 각도에 따라 달라짐), 전환 애니메이션은 선을 카드와 별도 취급(`width`를 늘렸다 줄이며 흩어짐과 동기화)해 "연동" 문제를 해소했고, z-index는 항상 `auto`(카드보다 항상 아래)로 고정해 "카드 위에 옴" 문제를 없앴다. 두께 4px, 파이프처럼 보이도록 수직 그라디언트(위쪽 밝게)+길이 방향 마스크 페이드를 적용. **교훈**: 같은 기능의 증상이 형태를 바꿔가며 계속 재발하면, 매번 그 자리에서 패치하지 말고 공통된 "표현 방식" 자체를 의심할 것 — 여기서는 "실제 3D 회전으로 위치 지정"이라는 선택 하나가 세 가지 다른 증상의 근원이었다.

### 그리드 다섯 번째 재수정 — "따로 논다"/"굵기 요동" 재발 원인은 빌보드 누락
4차 수정(2D 화면 좌표계로 전환) 직후에도 사용자가 "그리드가 카드랑 따로 논다", "회전하면 굵어졌다가 가늘어진다"고 재보고 — 이번엔 4차 수정 자체의 계산은 맞았지만 **적용이 미완성**이었다. 선을 담는 `div`가 3D로 회전하는 부모 씬(`#networkScene`, `transform-style:preserve-3d`)의 자식인데, 카드들과 달리 부모 회전을 상쇄하는 처리가 없어서 "2D로 계산한 회전값"이 여전히 부모의 3D 기울기 위에 얹혀 다시 뒤틀렸다. 카드가 이미 쓰고 있던 것과 정확히 같은 공식(`rotateY(-yaw) rotateX(-pitch)`, 부모 회전의 역행렬)을 선에도 추가해 완전히 평평하게 만든 뒤에야 실제로 두께가 고정됐다 — 카메라를 돌리기 전/후 실제 스크린샷을 비교해 확인(지난 라운드에 썼던 `getComputedStyle().height` 검증은 선언된 CSS 값만 읽어서 이 버그를 놓쳤었다 — 렌더링 결과 검증에는 반드시 실제 화면 캡처가 필요하다는 교훈 재확인). 두께도 "너무 굵다"는 피드백으로 4px→2.2px로 줄였다.

### 그리드 여섯 번째(최종) 재수정 — "카드 위에 옴"의 진짜 원인은 z-index가 아니라 preserve-3d
5차 수정 이후에도 "그리드가 카드 위에 오면 안 된다"는 재지적을 받았다. 원인은 z-index 설정과 무관했다: `#networkScene`가 `transform-style:preserve-3d`라서, 그 안의 카드·선은 CSS z-index가 아니라 **실제 3D 깊이(Z 위치)로 그리기 순서가 정해진다.** 카드는 궤도를 돌며 Z값이 수시로 양/음으로 바뀌는데, 선은 Z=0에 고정돼 있어서 카드가 Z=0보다 "카메라에서 먼" 쪽(음수)으로 갈 때마다 선이 그 카드보다 앞으로 그려졌다 — z-index를 아무리 조정해도 preserve-3d 안에서는 무시되는 경우였다. **최종 수정**: 선을 아예 `#networkScene`(3D 컨텍스트) 밖으로 꺼내서, `#networkShell` 안에 별도의 평면(2D) 형제 레이어 `#networkEdges`를 만들고 씬보다 DOM상 앞쪽에 배치했다. 3D 깊이 정렬에 전혀 참여하지 않는 평면 레이어이므로, 일반적인 페인트 순서(나중에 그려지는 `#networkScene`이 항상 위)만으로 카드가 항상 선 위에 오는 게 100% 보장된다. 이 구조 변경 덕에 5차에서 추가했던 빌보드 상쇄 회전도 불필요해져 제거(평면 레이어에는 애초에 상쇄할 부모 3D 기울기가 없음). scatter 애니메이션·접기 애니메이션 등 `networkScene.children`을 순회하던 모든 코드를 `networkEdges`도 함께 포함하도록 수정.

### 그룹 카드 재선택 시 하부 접기(토글) — 허브 클릭 = 뒤로 가기, 애니메이션 포함(2026-08-25, 2차 개선까지, ※애니메이션 방식은 "카드 위치 이동 금지" 규칙으로 이후 대체됨 — 아래 참고)
"그룹 카드를 선택 시 하부가 펼쳐지고, 다시 그룹 카드를 선택하면 하부가 오므라져서 감춰지게" — 그룹을 클릭해 들어가면 그 그룹이 화면 중앙의 "허브"가 되는데, 지금까지 허브는 클릭이 안 되는 순수 장식이었다. 이제 허브를 클릭하면 좌상단 뒤로 가기 버튼과 동일하게 상위 레벨로 복귀한다. **클릭 신뢰성 함정**: 허브는 화면 정중앙에 크게 떠 있지만 z-index가 낮게 고정돼 있어(태양 앞을 행성이 지나가는 연출을 위해 의도적으로) 궤도를 도는 다른 카드가 순간적으로 허브 자리를 가리면 클릭이 그 카드로 잘못 인식될 수 있었다. 클릭 좌표가 허브의 화면 영역 안에 들어오면 다른 무엇이 위에 떠 있든 무조건 허브 클릭으로 우선 처리하도록 수정 — Playwright로 궤도가 실제로 도는 상태에서 5회 연속 클릭해 5/5 성공 확인.
- **접기 애니메이션 범위 재조정 — 2차례 걸쳐 완성**:
  - *2차*(사용자 피드백: "해당 카드의 하부 카드들만 모이고, 나머지 그룹카드는 오므려들지 않게... 다 모이고 나면 그룹 카드들만 남으니까, 그것들은 다시 적당한 간격을 취해줘"): 처음 버전은 화면의 모든 카드(펼쳐졌던 하부는 물론, 배경의 형제 그룹 카드까지)를 한꺼번에 오므렸는데 어색했다. 닫힘 단계(허브+현재 링만 중심으로 모여 사라짐, 배경 형제는 완전히 그대로 둠)와 재배치 단계(스택 pop 후 배경 카드를 DOM 재사용으로 새 간격·크기에 부드럽게 전환)로 분리.
  - *3차*(사용자 재지적: "선택된 그룹 카드는 크기가 변동되지 말고, 주변의 카드들이 모여줘... 다 모였으면, 그룹 카드가 원래의 자기 자리로 찾아가는 과정을 보여줘... 전체적으로 축소되고 멀리서 그룹카드 한 개가 다시 나타나는 게 아니고"): 2차 버전은 닫힘 단계에서 허브 자체도 함께 줄어들어 사라졌다가, 재배치 단계에서 완전히 새로운 카드가 fade-in하는 방식이었다 — 이게 "그룹 카드가 사라졌다 멀리서 다시 나타남"으로 보였던 것. **수정**: 닫힘 단계에서 허브(선택된 그룹 카드 자신)는 위치·크기 모두 절대 건드리지 않고, 오직 그 하부(현재 링) 카드들만 모여들어 사라지게 함. 재배치 단계에서는 허브였던 그 카드를 "새로운 항목"이 아니라 "허브 위치/크기 그대로 얼어있던 카드"로 인식해, 그 마지막 렌더 상태를 그대로 물려받는 새 버튼을 만들고, 거기서부터 자기 자리(새 최상위 링의 정상 위치)로 이동하는 애니메이션을 재생 — 카드 자체의 연속성이 완전히 보존된다.
- **사용자가 영구 기억을 명시적으로 요청한 원칙**: "앞으로 움직이는 것, 상태 변화하는 것에 대해서 모든 변환 과정이 나타나야 해. 갑자기 퍽 나타나는 거는 안 돼." — Claude 메모리 `always-animate-transitions`에 영구 정책으로 저장함. 앞으로 이 프로젝트든 다른 프로젝트든 상태/위치가 바뀌는 UI를 만들 때 항상 적용.

### 그룹 진입(펼치기) 시에도 하부 카드가 "갑자기 나타남" — 접기의 반대 방향도 동일하게 수정(2026-08-25, ※스폰 애니메이션은 "카드 위치 이동 금지" 규칙으로 이후 대체됨 — 아래 참고)
"그룹카드를 선택시 하부카드가 갑자기 나타난다. 부모카드에서 분리되어 나오는 과정을 넣어줘." 지금까지 접기(뒤로 가기) 방향은 여러 차례 다듬었지만, 펼치기(그룹 진입) 방향(`pushNetworkLevel`)은 손대지 않아 여전히 하드 컷이었다 — DOM을 다시 그리면서 새 하부 카드들이 처음부터 최종 위치·크기·투명도로 바로 나타났다. **수정**: 새 DOM을 정상적으로 만든 뒤, 허브를 제외한 모든 카드·연결선을 허브 위치(원점)·작은 크기·투명도 0으로 순간 되돌리고, 리플로우를 강제한 다음 원래(자연) 목표 값으로 트랜지션 — 하부 카드들이 허브에서 "갈라져 나와" 각자의 궤도 자리로 퍼지는 것처럼 보인다. 허브 자체는 이 되돌림에서 제외했다 — 클릭 직전 애니메이션이 이미 그 카드를 허브 위치/크기로 옮겨놓았고, 새로 그려지는 허브 div도 정확히 같은 값으로 생성되므로 같은 프레임에서 바뀌어도 티가 안 난다(접기 방향과 달리 여기선 신구 허브 값이 애초에 동일함). **부수적으로 발견한 타이밍 문제**: 이 스폰 애니메이션이 비동기(끝나는 시점이 함수 리턴보다 한참 뒤)라서, "전환 중" 플래그를 반드시 애니메이션이 실제로 끝날 때까지 유지해야 했다 — 이전에는 애니메이션이 끝나기 전에 플래그가 풀려서, 애니메이션 총 소요 시간이 480ms→약 950ms로 늘어난 걸 놓치면 "방금 들어간 레벨에서 바로 클릭했는데 반응이 없다"는 버그로 이어질 뻔했다.

### 말단 카드를 선택한 채 화면을 돌리면 카드가 찌그러지는 버그
"말단 카드가 선택후 전체 회전하면 말단카드가 찌그러진다. 카드는 항상 정면을 봐야 한다." 원인: 카드가 "진입/포커스 중"(`.entering` 클래스) 상태일 때는 매 프레임 카메라 정면 보정 계산 자체를 완전히 건너뛰도록 되어 있었다 — 원래는 진입 애니메이션(약 480ms) 동안만 위치가 흔들리지 않게 하려는 의도였는데, 말단 카드를 포커스하면 이 클래스가 포커스를 풀 때까지 계속 남아있어서, 사실상 "포커스된 동안 내내" 정면 보정이 멈춰 있었다. 그 상태로 화면(카메라)을 돌리면, 카드 자신의 역회전 값은 포커스한 순간에 고정된 채 부모 장면만 계속 회전해서 서로 상쇄가 안 되고 뒤틀려 보였다. **수정**: `.entering` 카드도 매 프레임 건너뛰지 않고, 위치·크기는 그대로 둔 채 회전 성분만 최신 카메라 각도로 계속 갱신하도록 변경. 카메라를 120도 정도 돌린 뒤에도 포커스된 카드의 위치·크기는 완전히 그대로였고 회전만 카메라를 따라간 것을 확인했다.

### 전환이 평면적이다 — 카드 3D 텀블링 추가, 그리드 더 얇게+원근감, 선택 시 카메라가 카드로 이동(2026-08-25)
- **그리드 얇게+거리별 가늘기**: 기본 두께를 1.3px로 더 줄이고, 매 프레임 원근 투영에 쓰던 배율(`sc`, 카메라에서 먼 카드일수록 작아짐)을 선의 두께에도 그대로 곱해 카메라에서 먼 카드로 갈수록 선이 더 가늘어지게 했다(0.35~1.5배 범위로 클램프).
- **전환 시 카드가 밋밋하게 움직이는 문제**: 지금까지 도넛 카드는 흩어질 때 항상 `rotateY(0deg)`(회전 없음)로 고정돼 있었고, 네트워크 카드도 위치만 바뀌고 회전은 그대로였다 — 실제로는 3D 이동인데도 시각적으로 납작하게 느껴진 이유. **수정**: 흩어지는 동안(도착 지점은 그대로 정확한 원래 자리)에만 무작위 3축 회전(텀블링, ±150°)과 방향에 무작위 편차(±25~50%)를 추가해 카드마다 다르게, 입체적으로 날아가 보이게 했다. 최종 도착 위치·회전·크기는 전혀 안 바뀌므로 상태가 꼬일 위험은 없다.
- **카드 선택 시 카메라가 그 카드 쪽으로 움직임 — 도입 → 제거 → 최종적으로 유일한 선택 표현 방식으로 재도입**: 처음엔 클릭된 카드가 원점으로 날아가는 것과 동시에 카메라도 그쪽을 보도록 회전시켰다가, "다른 카드를 선택하면 카메라 각도가 리셋된다"는 피드백으로 카메라 이동 기능만 제거했었다. 그런데 이후 "말단 카드끼리만 자리를 바꾸고 카메라는 안 움직인다"는 문제가 재현되면서, 실은 카메라가 아니라 **카드가 자리를 옮기는 것 자체**가 문제였음이 드러났다 — 자세한 경위와 최종 규칙은 "카드 위치 이동 금지 + 카메라 이동으로 선택 표현" 절 참고.

### 배경 그룹 카드 클릭 가능 — 선택 시 현재 그룹 닫고 선택된 그룹으로 이동(2026-08-25)
"하부카드가 펼쳐진 경우, 상부 그룹카드를 선택시 현재의 그룹은 닫고, 선택된 그룹이 펼쳐지게 해줘." 지금까지 배경(상위 링)에 보이던 카드들은 장식용으로 클릭이 안 됐다. 이제 모든 카드가 클릭 가능해졌고, 배경 카드를 클릭하면 `networkStack`을 그 카드가 속한 레벨까지 잘라낸 뒤(`networkStack.length = 그 레벨+1`) 그 자리에서 곧바로 클릭한 카드로 새로 진입한다 — 즉 "현재 펼쳐진 걸 닫고 선택한 걸 편다"가 한 동작으로 자연스럽게 처리된다. 일반 하부 카드 클릭과 동일한 애니메이션(형제 흐려짐, 클릭한 카드가 허브 자리로 날아감, 카메라도 함께 이동)을 그대로 재사용했다.

### 그리드 굵기 안정화 — "처음엔 굵다가 점점 가늘어짐"의 원인은 드리프트가 아니라 편차 폭
"그리드가 처음에는 굵었다가 카드 전환을 반복하면 점차로 가늘어진다... 가늘게 보이는 상태의 굵기가 딱 좋으니, 그 굵기를 처음부터 유지해줘." 원근 배율에 따라 매 프레임 새로 계산되는 값이라 실제로 누적되며 드리프트하는 건 아니었지만, 허용 범위(0.35~1.5배)가 넓어서 카드마다 깊이 편차가 크게 드러나 보였다 — 처음 빌드 직후 카메라에 가까운 카드가 우연히 많으면 "굵게 시작"하는 것처럼 느껴지고, 궤도가 퍼지면서 "점점 가늘어지는" 것처럼 보인 것. 범위를 0.8~1.1배로 크게 좁히고 기본 두께도 1.3px→1.5px로 살짝 올려(최종 범위 1.2~1.65px) 안정적이면서도 항상 잘 보이게 했다.

### 말단 카드 선택 후 카메라 이동 직후 드래그하면 흐름이 끊기는 버그
"하부 그룹이 펼쳐진 상태에서 말단카드를 선택시 카메라 이동후 재개될 때 흐름이 끊긴다." 원인: 카드 선택 시 카메라를 돌리는 애니메이션(CSS transition)이 실제로 정리(제거)되는 시점이, "이제 다른 조작을 받아도 된다"는 내부 플래그가 풀리는 시점보다 60ms 늦었다. 그 틈(480~540ms) 사이에 사용자가 바로 화면을 드래그하면, 드래그가 매 프레임 그리는 회전값이 아직 안 지워진 예전 트랜지션에 걸려 부드럽게 따라가지 못하고 뭉개져 보였다. **수정**: 드래그가 시작되는 바로 그 순간 무조건 트랜지션을 강제로 제거하도록 해서, 어떤 애니메이션의 잔여물이든 다음 드래그를 방해하지 못하게 했다.

### 버튼 위치 조정 및 "전환 중 카드가 정면을 잃는" 근본 버그 수정(2026-08-25)
- **버튼 위치**: 좌상단 "부모로 이동" 버튼(`left:44%`)과 우상단 모드 전환 버튼(`right:3%`)이 카드 콘텐츠 영역 가장자리에 딱 붙어 있어 "너무 떨어져 있다"는 지적을 받았다. 각각 콘텐츠 영역 중심 쪽으로 20%가량(`left:49%`, `right:8%`) 이동해 카드와 더 가까워 보이게 했다.
- **전환 중 카드가 정면을 잃는 버그**: "카드간 클릭시 주변카드 전체가 그대로 회전하고 갑자기 전부 다시 나타나 전면을 보는데, 전환도중 전면을 바라보게 해줘." 원인은 "전환 중"이라는 플래그 하나가 서로 다른 두 가지(위치를 고정할지, 카메라 정면 보정을 계속할지)를 한꺼번에 막고 있었던 것 — 카드 선택 시 카메라가 함께 회전하는 기능을 추가하면서, 카메라는 실제로 도는데 다른 카드들의 정면 보정 계산은 통째로 멈춰있어서 카메라를 따라 함께 기울어져 보이다가 전환이 끝나는 순간 갑자기 정면으로 스냅됐다. **수정**: "위치 고정"과 "회전 보정 갱신"을 분리해서, 위치는 여전히 전환 중엔 고정하되 회전 보정만은 매 프레임 계속 갱신하도록 했다. 이 변경으로 새로 생겨나는 하부 카드들의 "허브에서 태어나는" 스폰 애니메이션이 매 프레임 재계산에 깨질 뻔했는데, 스폰 중인 카드 전체에도 기존 "회전만 갱신, 위치는 그대로" 예외 처리(원래 포커스된 카드 한 장에만 쓰던 것)를 넓혀 적용해 해결했다.

### 그리드 재차 축소 + 말단 카드 포커스 시 주변 카드 동시 후퇴(2026-08-25, ※"후퇴(recede)" 효과는 "카드 위치 이동 금지" 규칙으로 이후 제거됨 — 아래 참고)
- **그리드 재차 축소**: "제일 처음에 나타나는 그리드가 굵다", "전환시 나타나는 그리드도 굵다" — 지난 라운드에 안정화한 범위(1.2~1.65px)도 여전히 굵게 느껴진다는 재요청으로, 정확한 원인을 더 파고들기보다 요청대로 두 차례 더 줄였다(기본 1.5px→1px, 허용폭 0.8~1.1배→0.85~1.05배, 최종 범위 0.85~1.05px).
- **말단 카드 포커스 시 "다시 나타나는" 현상**: "선택되어 중심으로 이동할때 주변카드도 그에 맞게 뒤로 이동되게 해줘. 지금은 선택된 카드가 중심을 잡고 주변에서 다시 나타난다." 원인: 포커스 상태를 나타내는 값이 카드가 중심으로 날아가는 애니메이션이 **다 끝난 뒤에야** 설정되고 있었다 — 그래서 날아가는 동안 주변 카드는 흐려지기만 할 뿐 제자리에 그대로 있다가, 애니메이션이 끝나는 순간 갑자기(트랜지션 없이 한 프레임 만에) 작아지고 뒤로 밀려나 "다시 나타나는" 것처럼 보였다. 포커스 상태를 카드가 날아가기 **시작하는 순간**으로 앞당기고, 주변 카드에도 트랜지션을 걸어서 선택된 카드가 중심으로 이동하는 것과 정확히 같은 시간 동안 함께 뒤로 물러나도록 고쳤다. 포커스를 해제할 때(원래대로 돌아올 때)도 같은 방식으로 애니메이션이 걸리도록 대칭으로 수정했다.

### 카드 블러 제거 + "전환 반복 시 그리드가 거의 없어짐"의 진짜 원인(2026-08-25)
- **카드 화면이 흐림**: 배경(상위 링) 카드에 "receded" 느낌을 주려고 채도 감소와 함께 블러도 걸었었는데, 이제 배경 카드도 클릭 가능한 실제 콘텐츠가 된 마당에 블러가 가독성을 해쳤다. 블러를 제거하고 채도 감소만 남겼다.
- **그리드가 반복 전환 시 사라짐 — 실측으로 원인 확정**: 추측 대신 Playwright로 "진입→뒤로가기"를 4회 반복하며 매번 연결선 투명도를 직접 측정했다. 결과: 뒤로가기(`backNetworkLevel`) 직후에만, **매번 예외 없이** 최소 투명도가 0으로 나왔다(진입 시엔 정상). 원인: 방금까지 허브였던 카드가 다시 보통 카드로 복귀할 때 연결선을 새로 만드는데, 이 새 연결선은 투명도 0으로 "씨앗" 상태로 시작하도록 되어 있었고, 이후 원래 밝기로 되돌리는 코드가 카드(이미지)에는 있었지만 연결선에는 빠져 있었다 — 그래서 뒤로 갈 때마다 최소 하나씩 선이 영구히 투명한 채로 남았다. 재사용되는 다른 배경 카드들의 선도 새 링에 맞는 밝기로 안 갱신되고 이전 값에 머물러 있던 부수적 문제도 같이 발견해 함께 고쳤다. 수정 후 4회 반복 측정에서 매번 정상 밝기(0.85)로 유지됨을 확인. (아래 "카드 위치 이동 금지" 규칙 확정 이후 이 버그의 원인이 된 메커니즘 자체가 통째로 사라졌다.)

### 카드 위치 이동 금지 + 카메라 이동으로 선택 표현 — 최종 아키텍처 확정(2026-08-25, 이 날의 마지막 재설계, 이전 애니메이션 설계 다수를 대체)
사용자 직접 지시, 영구 규칙: **"카드간의 위치 이동은 절대 없다. 처음이나 끝이나. 그룹 카드나 말단카드나. 모든 카드는 고유의 궤적을 회전하는 것이고, 선택된 카드로 카메라가 이동하는 것이다."**

경위: 처음엔 클릭한 카드가 중앙(허브 자리)으로 날아가 확대되는 방식뿐이었다 → 여기에 카메라도 함께 도는 기능을 추가했다("전환이 평면적이다" 절 참고) → "다른 카드 선택시 카메라 각도가 리셋된다"는 피드백으로 카메라 이동 기능만 제거하고 카드가 날아가는 방식은 그대로 두었다 → 그런데 "말단 카드가 이미 포커스된 상태에서 다른 말단 카드를 선택하면, 카메라는 안 움직이고 카드끼리만 자리를 맞바꾼다"는 정확히 같은 부류의 문제가 재현됐고, 이때 사용자가 위 영구 규칙을 명시적으로 지시했다 — 문제는 카메라 기능이 아니라 애초에 **카드가 위치를 바꾸는 것 자체**였다.

**최종 구현**: 모든 카드(그룹·말단 불문)는 선택 여부와 무관하게 항상 자기 궤도만 계속 돈다 — 위치·크기 절대 불변, 선택돼도 화면 중앙으로 이동하거나 커지지 않는다. 대신 **카메라(시야각)** 만 선택된 카드가 있는 방향을 보도록 매끄럽게 회전한다. 카메라 이동은 CSS 트랜지션이 아니라 매 프레임 JS로 각도를 보간하는 방식으로 구현했는데, CSS 트랜지션을 쓰면 카메라(컨테이너)는 서서히 도는데 각 카드의 "카메라를 향해 정면을 유지하는" 반대회전 계산은 목표각을 즉시 참조해버려서 카드가 잠깐 일그러져 보이는 문제가 있었기 때문이다.

이 규칙에 따라 제거된 것들(전부 "카드가 위치/크기를 바꾸는" 애니메이션이었음): 카드가 허브 위치로 날아가 확대되는 효과, 선택 시 나머지 카드가 뒤로 빠지는 효과(recede), 그룹 진입 시 하부 카드가 허브에서 튀어나오듯 펼쳐지는 스폰 애니메이션, 허브 접기 시 카드가 자기 자리로 이동하며 접히는 애니메이션(및 이를 위해 있던 복잡한 "요소 재사용" 로직 전체). 전부 **불투명도 페이드**로 대체했다 — 선택 시 나머지 카드는 흐려지기만 하고, 레벨을 오갈 때는 카드가 사라졌다 나타나는 대신 페이드 아웃/인만 한다. (※같은 날 뒤쪽 라운드에서, 접기 방향에 한해 "하부 카드가 그룹 카드로 모여드는" 위치 애니메이션만 사용자 요청으로 다시 추가됨 — 아래 "굵은 테두리 재발" 다음 라운드 참고. 펼치기/선택 방향은 이 규칙 그대로 유지.)

부수 효과: 위 "그리드가 사라짐" 버그의 원인이었던 요소별 상태 이어받기 로직이 통째로 사라져, 같은 버그가 구조적으로 재발할 수 없게 됐다. Playwright로 재검증: 말단 카드를 연속으로 재선택할 때 카메라 각도가 매번 실제로 바뀌고, 두 카드의 좌표는 궤도상 정상적인 미세한 변화만 있을 뿐 순간이동하거나 서로 자리를 바꾸지 않음을 확인. 반복적인 진입/뒤로가기 3회에서도 그리드 투명도가 항상 정상 범위(0.53~0.85)로 유지됨을 확인.

### 선택된 카드를 카메라가 계속 추적(지구중심 시점) + 카드 화면 흐림 진짜 원인 해결(2026-08-25, 같은 날 마지막 라운드)
- **카메라 지속 추적** (2026-08-25, ※이 방식은 바로 다음 라운드에서 "렌더링 원점 이동" 방식으로 대체됨 — 아래 "선택 카메라 방식 재수정" 절 참고): "선택된 카드가 중심으로 보이는 효과를 내줘. 카메라가 계속 따라다니는 거다. 선택된 카드는 중앙에 정지될 것이고, 주변 카드가 움직일 거다. 지구에서 보면 달이나 태양이 움직이는 것처럼." 바로 위 절의 카메라 이동은 클릭 시점 위치를 한 번만 조준하는 방식이라, 조준이 끝난 뒤에도 카드는 계속 궤도를 돌기 때문에 서서히 화면 중심에서 벗어나는 점이 남아있었다. **수정**: 매 프레임 그 카드의 "지금 이 순간" 궤도 좌표를 다시 조준해 카메라 각도를 그 방향으로 조금씩(부드럽게) 맞춰가는 지속 추적 방식으로 교체했다. 목표 각도 자체가 매 프레임 거의 안 바뀌는 수준으로만 갱신되기 때문에, 처음 선택할 때는 부드럽게 다가가는 느낌을 주면서도 이후엔 거의 완벽하게 화면 중앙에 고정된 것처럼 보인다 — 정확히 사용자가 말한 "선택된 것은 정지, 나머지가 움직인다"는 지구중심적 시점. 드래그하면 일시적으로 추적을 멈추고 자유롭게 둘러볼 수 있고, 손을 떼면 다시 부드럽게 원래 카드를 추적한다. Playwright 검증: 포커스된 카드의 화면 좌표가 약 1.2초(6프레임 샘플) 동안 1px 이내로 고정된 반면, 옆의 다른 카드는 같은 시간에 화면에서 약 180px 이동하고 카메라 각도는 계속(38도가량) 바뀜을 확인. **이 방식의 부작용**: "각도를 매 프레임 강제로 다시 맞춘다"는 게 핵심이었으므로, 드래그로 각도를 바꿔도 손을 떼는 즉시 다시 원래 조준 각도로 서서히 되돌아가는 문제가 있었다(다음 라운드에서 재지적됨).

### 선택 카메라 방식 재수정 — 각도 강제 조준 대신 "렌더링 원점을 선택된 카드로 이동" + 선택 카드 확대(2026-08-25, 같은 날 마지막 라운드)
"선택된 카드가 중앙에서 크게 보이게 해주고, 궤적을 보는 각도를 바꿀때 바뀐 궤적을 유지하게 해줘. 자꾸 원래로 돌아간다." 위 절의 "카메라 지속 추적" 방식은 매 프레임 카메라 각도(요/피치)를 선택된 카드 쪽으로 강제로 다시 맞추는 방식이었는데, "look-at 각도"는 어떤 위치를 정면으로 보기 위한 각도가 수학적으로 딱 하나뿐이라서, 사용자가 드래그로 다른 각도를 만들어도 손을 떼는 순간 그 유일한 정답 각도로 도로 끌려갔다 — "자꾸 원래로 돌아간다"는 정확히 이 현상이었다.

**수정**: 카메라 각도를 강제로 조정하는 로직을 완전히 없애고, 대신 **매 프레임 렌더링에 쓰는 좌표계의 기준점(원점)을 선택된 카드의 실시간 위치로 옮기는** 방식으로 바꿨다. 원래 허브(중심 태양 카드)는 항상 로컬 좌표 원점 `(0,0,0)`에 있고, 이 원점은 카메라를 어떤 각도로 돌리든 항상 화면 정중앙에 투영된다는 불변의 성질을 이 시스템이 처음부터 갖고 있었다 — 이 성질을 그대로 재사용해서, 매 프레임 "선택된 카드의 현재 위치"를 빼주는 방식으로 모든 카드(허브 포함)의 좌표를 다시 계산하면, 선택된 카드는 계산상 항상 정확히 원점이 되어 **카메라 각도와 무관하게 항상 화면 중앙에 나타난다.** 이제 카메라 각도는 순전히 드래그(및 그 관성)에 의해서만 바뀌므로, 사용자가 만든 각도가 그대로 유지된다.

**"크게 보이게" 요청도 함께 반영**: 선택된 카드가 이제 원점(허브 자리)을 차지하게 된 김에, 허브와 같은 확대 배율(약 1.85배)로 부드럽게(순간적으로 팍 커지지 않고) 커지도록 만들었다. (참고: 이때 "선택 해제 시에도 부드럽게 줄어든다"고 의도했으나 실제로는 순간적으로 팍 줄어드는 버그가 있었음 — 바로 다음 라운드에서 발견·수정됨)

**부수적으로 필요했던 수정**: 연결선(그리드)이 기존에는 "허브는 항상 화면 중앙"이라는 가정하에 원점에서부터의 거리·각도만 계산해서 그렸는데, 이제 허브도 다른 카드처럼 화면 중앙을 벗어날 수 있게 되어, 연결선을 "허브의 실제 화면 위치 → 카드의 실제 화면 위치"를 정확히 계산해서 잇는 방식으로 바꿨다. 카드들이 화면에 그려지는 순서(깊이 정렬)는 이 좌표 이동에 영향받지 않는다(모든 카드에 같은 양만큼 이동을 적용했으므로 카드 간 상대적 거리는 그대로 유지되기 때문).

**검증**: Playwright로 포커스된 카드의 화면 좌표가 화면 중앙과 서브픽셀 단위로 일치하고 실제 렌더 크기가 약 1.85배(96×128px → 약 178×237px)로 커짐을 확인. 드래그로 카메라 각도를 크게 바꾼 뒤 1.5초 대기해도 각도가 드래그 이전 값으로 전혀 되돌아가지 않고(관성에 의한 자연스러운 감속만 있을 뿐 방향이 역전되지 않음) 유지됨을 확인. 그룹 진입, 배경 카드 클릭, 말단 카드 포커스/해제, 3회 반복 뒤로가기를 전부 재검증해 콘솔 에러 없음을 확인.

### 포커스 크기·전환 애니메이션 미세 조정 + 동급 카드 안 흐리기 + 그룹/포커스 테두리 구분(2026-08-25, 같은 날 마지막 라운드)
"포커스를 잃은 카드는 다시 원 크기로 돌아가게 해줘. 포커스된 것이 말단 카드일때 동급의 카드들로 잘보이게 해줘. 흐리게 하지 말고. 그룹카드들은 테두리를 넣어 다른 것과 구분해주고, 현재 포커스된 카드는 더 굵은 테두리를 넣어줘. 테두리가 굵은 만큼 약간 흐리게 하자. 물론 현재처럼 포커스를 잃으면 테두리는 없어지게 해줘. 카드의 포커스가 전환될때 전환과정을 보여줘." 바로 위 라운드에서 두 가지가 실제로는 의도한 대로 동작하지 않고 있었다.

- **① 포커스 해제 시 순간적으로 줄어드는 버그**: 커지는 배율 값을 "선택된 카드 한 장"에만 적용하는 공용 변수 하나로 관리했는데, 포커스가 풀리는 순간 그 변수를 적용할 대상 자체가 사라져서 스르륵 줄어드는 과정 없이 즉시 원래 크기로 튀었다. **수정**: 배율 값을 카드마다 각자 갖고 있도록(즉 카드 각자가 "지금 내가 몇 배로 보여야 하는지"를 계속 스스로 추적) 바꿔서, 포커스를 잃은 카드도 자기 힘으로 원래 크기까지 부드럽게 줄어들도록 했다.
- **② 포커스가 다른 카드로 바뀔 때 화면이 순간 이동하던 문제**: "화면 중심을 선택된 카드로 옮기는" 계산이 매 프레임 선택된 카드의 "지금 위치"를 그대로 가져다 썼는데, 선택이 A카드에서 B카드로 바뀌는 순간 그 위치가 한 프레임 만에 뚝 바뀌어서 화면 전체가 순간 이동하는 것처럼 보였다. **수정**: 이 기준점도 매 프레임 목표 위치로 부드럽게 다가가도록(스냅이 아니라 이동) 바꿔서, 카드 간 포커스 전환도 화면이 자연스럽게 이어지며 넘어가도록 했다.
- **③ 말단 카드 포커스 시 동급 카드를 흐리게 하지 않음**: 지금까지는 카드를 포커스하면 나머지 전부(같은 단계 카드 포함)를 흐리게 만들었는데, 말단 카드를 포커스했을 때만은 같은 단계(동급)의 카드는 흐리게 하지 않고 그대로 선명하게 보이도록 바꿨다(상위 그룹의 배경 카드는 계속 흐리게 유지 — "동급"이 아니므로). 그룹 카드를 눌러 하위로 진입할 때의 흐림 처리는 기존 그대로 유지했다(사용자가 "말단 카드일 때"로 콕 집어 요청했기 때문).
- **④ 그룹/포커스 카드 테두리 구분**: 동급 카드를 더 이상 흐리게 하지 않게 되면서, "지금 무엇이 포커스됐는지"를 다른 방식으로 보여줄 필요가 생겼다. 하위 그룹으로 이어지는(즉 눌러도 최종 작품이 아니라 또 다른 목록으로 들어가는) 카드에는 항상 은은한 보라색 테두리를 넣어 일반 카드와 구분되게 했고, 그중 현재 포커스된 카드는 테두리를 더 두껍게 하고 부드럽게 번지는 은은한 빛(그림자 효과)을 추가했다 — 사용자가 말한 "테두리가 굵은 만큼 약간 흐리게"는 카드 사진 자체를 흐리게 하는 게 아니라 테두리 주변에 부드러운 빛 번짐 효과를 주는 것으로 구현했다(사진이 흐려 보이는 문제는 이미 이전 라운드에서 별도로 고쳤으므로, 사진 자체는 계속 선명하게 유지). 포커스를 잃으면 이 두꺼운 테두리와 빛 효과도 부드럽게 사라진다.

**검증**: Playwright로 그룹 단계 카드에 테두리가 있고 말단(최종 목록) 단계 카드에는 없음을 확인, 말단 카드 포커스 중 옆 카드 불투명도가 그대로 1(안 흐려짐)임을 확인, 포커스된 카드가 굵은 테두리 클래스를 갖고 실제 확대 배율이 약 1.85배임을 확인, A카드에서 B카드로 포커스를 옮길 때 화면 기준점이 여러 프레임에 걸쳐 서서히 이동함(한 프레임 순간이동이 아님)을 확인, 포커스 해제 후 확대 배율이 약 1.0으로 부드럽게 돌아오고 테두리 클래스도 사라짐을 확인. 그룹 진입·배경 카드 클릭·반복 뒤로가기까지 전부 콘솔 에러 없이 통과.

### 그룹 카드(허브)가 멤버 선택 후에도 계속 크게 남아있던 버그 수정(2026-08-25, 같은 날 마지막 라운드)
"선택된 그룹 카드 상태에서 멤버를 선택하면, 멤버는 크게, 크게 커져서 가운데 중앙 위치하는데, 그룹 카드는 원래대로 작게 돌아가지 않는다." 지금까지 그룹의 중심 카드(허브 — 현재 들어와 있는 그룹을 나타내는 태양 역할의 카드)는 항상 고정된 확대 배율로만 그려지고 있었다. 이는 원래 의도된 동작이었지만(허브는 "지금 여기에 있다"는 표시로 항상 커야 함), 이번 세션에서 확정한 "커질 때는 선택받은 카드 뿐이다"라는 규칙과 충돌했다 — 그 그룹 안의 멤버 카드를 선택하면 멤버가 커지는데, 허브도 여전히 계속 크게 남아 있어서 화면에 큰 카드가 두 개 동시에 있는 것처럼 보였다.

**수정**: 허브도 다른 카드들과 마찬가지로 "지금 내가 몇 배로 보여야 하는지"를 스스로 부드럽게 추적하도록 만들었다 — 멤버가 선택되면 허브는 원래 크기로 작아지고(선택이 다른 곳으로 옮겨갔으므로), 아무것도 선택되지 않은 상태(그냥 둘러보는 중)로 돌아가면 허브가 다시 원래의 확대된 크기로 돌아온다. 이제 항상 "선택된 카드 단 하나만" 화면에서 크게 보인다.

**검증**: Playwright로 하위 단계까지 들어가 허브가 있는 상태를 만든 뒤, 멤버 카드를 선택하기 전(허브 배율 약 1.85) → 선택 후(허브 배율 약 1.00, 멤버 배율 약 1.85로 성장) → 다시 선택 해제 후(허브 배율 약 1.85로 복귀)까지 전부 확인. 전환 중간 스크린샷으로 멤버가 커지는 것과 허브가 작아지는 것이 동시에 부드럽게 진행됨을 시각적으로도 확인. 최상위 레벨(허브 없음)로 되돌아가는 반복 뒤로가기까지 콘솔 에러 없이 통과.

### 굵은 테두리 표시 기준을 "선택/중앙"에서 "마우스 오버"로 전면 교체(2026-08-25, 같은 날 마지막 라운드)
"포커스를 받을 때에만 두꺼운 테두리를 주게끔 했는데, 항상 두꺼운 테두리가 있다. 두꺼운 테두리는 중앙가운데 카드에 주지말고, 마우스 오버시에 포커를 받는 카드에 주자." 추측하지 않고 Playwright로 직접 확인한 결과: 방금 만든 "선택 시 굵은 테두리" 기능 자체는 코드상 정확히 의도대로 동작하고 있었다(아무것도 선택 안 됐을 때 굵은 테두리를 가진 카드는 실제로 하나도 없었음). "항상 굵은 테두리가 있다"고 느껴진 진짜 원인은 따로 있었다 — 그룹의 중심 카드(허브)에는 이번 세션 이전부터(8월 23일) 이미 은은한 보라색 테두리와 빛 번짐 효과가 항상 걸려 있었는데, 허브는 대부분의 상황에서 화면 중앙에 있다 보니 "중앙 카드는 늘 굵게 테두리가 있다"는 인상을 줬던 것.

사용자는 이 기회에 아예 방식을 바꾸기로 했다: 선택되어 중앙에 있는 카드에는 테두리 강조를 아예 주지 말고(크고 중앙에 있는 것 자체로 충분한 표시), 대신 마우스를 올린(hover) 카드에만 굵고 빛나는 테두리가 나타나게 하자는 것.

**수정**: 방금 만들었던 "선택 시 굵은 테두리" JS 로직을 완전히 제거하고, 그 스타일(굵은 테두리 + 은은한 빛 번짐)을 CSS의 순수 마우스 오버 효과로 옮겼다 — 어떤 카드든 마우스를 올리면 자동으로 굵은 테두리가 나타나고 떼면 부드럽게 사라진다(그룹/말단, 선택 여부와 무관하게 전부 동일하게 적용). 또한 허브의 원래 있던 테두리 빛 번짐 효과도 제거해서, 중앙에 있다는 이유만으로 굵은 테두리처럼 보이는 일이 없도록 정리했다(허브 고유의 은은한 보라색 테두리 색상은 유지, 굵기는 다른 카드와 동일).

**검증**: 아무것도 선택 안 된 상태에서 굵은 테두리를 가진 카드가 없음을 확인, 허브의 그림자 효과가 일반 그림자만 남고 테두리 느낌의 링이 사라졌음을 확인, 선택되어 중앙에 있는 카드도 테두리 굵기가 기본값(1px)임을 확인, 실제 마우스 커서를 다른(선택되지 않은) 카드 위로 이동시켰을 때 그 카드만 굵은 테두리(4px)로 바뀜을 확인. 스크린샷으로 "중앙의 큰 카드는 얇은 테두리, 옆의 마우스 올린 작은 카드는 굵고 빛나는 테두리"인 것을 시각적으로도 확인.

### 같은 문제 재발 — 이번엔 "클릭 자체가 곧 마우스 오버"였던 게 진짜 원인(2026-08-25, 같은 날 마지막 라운드)
바로 위 수정을 배포한 뒤, 사용자가 정확히 같은 문장으로 다시 같은 문제를 보고했다. 추측하지 않고 다시 Playwright로 확인한 결과, 이번엔 원인이 완전히 달랐다: 카드를 클릭하면 마우스 커서는 클릭한 그 자리에 그대로 머무르는데, 방금 선택된 카드는 화면 중앙으로 커지면서 이동해오기 때문에, 전환이 끝났을 때 커서가 우연히 그 커진 카드 위에 그대로 놓여 있게 되는 경우가 잦았다 — 이건 "의도치 않게 남아있는 것"이지만 브라우저 입장에서는 진짜로 그 카드를 hover하고 있는 상태라서, 순수 CSS 마우스 오버 효과만으로는 "일부러 올린 것"과 "클릭하고 나서 우연히 남은 것"을 구분할 수 없었다.

**같은 증상, 다른 원인이었다는 점이 핵심**: 첫 번째 라운드의 원인은 허브의 오래된 고정 테두리 효과였고, 이번 라운드의 원인은 클릭이 곧 호버로 이어지는 상호작용 특성이었다 — 겉보기엔 똑같이 "항상 굵은 테두리가 있다"로 보였지만 완전히 다른 버그였다.

**수정**: 선택되어 중앙에 있는 카드는 실제 마우스가 그 위에 있는지 여부와 무관하게 항상 굵은 테두리 효과에서 제외되도록 예외 규칙을 추가했다. 이제 마우스가 우연히 선택된 카드 위에 머물러 있어도 굵은 테두리가 나타나지 않고, 오직 선택되지 않은 다른 카드에 실제로 마우스를 올렸을 때만 나타난다.

**검증**: 마우스를 화면 먼 곳에 둔 상태에서 모든 카드(선택된 카드 포함) 테두리가 기본값임을 확인, 마우스를 선택된 카드 바로 위로 직접 이동시켜도 여전히 기본 테두리로 유지됨을 확인(브라우저 hover 상태 자체와 실제 컴퓨티드 스타일을 모두 대조), 실제 "클릭 후 마우스를 움직이지 않고 그대로 두는" 시나리오를 끝까지 재현해도 방금 선택된 카드에는 절대 굵은 테두리가 나타나지 않음을 확인. (검증 과정에서 테스트 스크립트 자체의 오류도 하나 발견해 바로잡았다 — 카드 위치를 미리 읽어둔 좌표로 마우스를 이동시켰는데, 그 사이 카드가 계속 궤도를 돌고 있어서 대기 시간이 지나면 커서가 다른 카드 위에 가 있었던 것으로, 실제 버그가 아니라 측정 방법의 오차였음을 `document.elementFromPoint()`로 교차 확인해 가려냈다.)

### 그룹 카드 테두리 제거 + 첫 진입 시 카드 회전 부자연스러움 수정 + 접기 애니메이션 복원(2026-08-25, 같은 날 마지막 라운드)
"그룹 카드에 가는 테두리를 주기로 했는데, 그거는 빼죠. 처음 가상공간2로 전환할 때, 모든 카드가 기울어진 상태로 움직이다 갑자기 전면을 향함. 부드럽게 전면을 향하게 해줘. 하부가 펼쳐진 그룹카드를 선택시 하부카드들이 그룹카드로 모여 없어지고 난뒤, 그룹카드가 제위치를 찾아야하는데, 하부카드들이 모이는 과정이 없어졌다. 기존에 있었는데 확인해줘." 관련 없는 세 가지 요청을 한 번에 처리했다.

**① 그룹 카드 가는 테두리 제거**: 요청대로 완전히 뺐다. 그룹 카드를 구분하는 용도로 쓰던 CSS 클래스와 관련 로직을 전부 삭제했다.

**② 가상공간2 첫 진입 시 카드가 기울어진 채 움직이다 갑자기 정면을 향하던 문제 — 1차 시도(불완전)**: 카드가 흩어져 있다가 제자리로 모이는 애니메이션에서, 날아오는 도중엔 무작위 기울임 회전이 걸려 있는데 도착 지점 값에는 그 기울임 회전이 아예 없어서 브라우저가 매끄러운 개별 보간 대신 행렬 보간으로 전환되며 부자연스러워 보인다고 진단하고, 도착 지점에도 "기울임 없음(0도)"을 명시적으로 넣어 맞췄다. **→ 이 수정은 그 자체로는 맞았지만 문제를 해결하지 못했다 — 사용자가 "여전히 있다"고 재보고, 진짜 원인을 다시 찾음(아래 새 라운드 참고).**

**③ 그룹 카드 접기(뒤로가기) 시 "하부 카드가 모여드는" 과정 복원**: 이 문서 앞부분에서 설명한 "카드 위치 이동 금지" 영구 규칙에 따라 한때 완전히 제거됐던 기능인데, 사용자가 명확하게 "기존에 있었다"며 복원을 요청해 다시 넣었다. 단, 이번엔 선택(진입) 방향이 아니라 **접기(닫기) 방향에 한정된 예외**로 구현했다 — 사라질 하부 카드들이 자신을 흡수하는 그룹 카드(허브)의 자리로 모여들면서 작아지고 사라지는 애니메이션이며, 허브 자신과 상위(배경) 카드는 여전히 그냥 페이드만 한다. 선택 시 카메라만 움직이고 카드는 절대 움직이지 않는다는 원래 규칙은 그대로 유지된다.

**검증**: 그룹 카드에 테두리 클래스가 더 이상 없음을 확인. 그룹 진입 후 뒤로가기 시 하부 카드가 실제로 중심(원점)으로 이동하며 축소되는 것을 애니메이션 도중 좌표 값으로 직접 확인(예: x좌표 160→0, 크기 1→0.12). 진입→접기를 4회 반복해도 그리드·카드 밝기가 항상 정상 범위로 유지됨을 확인. 전 과정 콘솔 에러 없음.

### 첫 진입 회전 문제 진짜 원인 발견 + 그룹 진입도 매끄럽게 이어지도록 전면 재설계(2026-08-25, 같은 날 마지막 라운드)
"첫 진입 시 카드가 기울어진 채 움직이다 갑자기 정면을 향하던 문제가 여전히 있다. 확인해줘. 그룹카드를 선택시 하부가 펼쳐지는 과정이 없어졌다. 복원해줘. 주변의 그룹카드를 선택하면 카드가 중앙으로 오고, 그 상태에서 하부가 펼쳐지게 해줘... 그런데, 그룹카드가 중앙에 오고, 그다음 바깥에서 펼쳐진 그룹들과 함께 전체가 다시 들어온다. 매끄럽지 않다. 수정해 줘. 한마디로 어떻게 되든 끊긴다는 느낌없이 매끄럽게 넘어가야 돼. 그게 대전제야."

**① 회전 문제의 진짜 원인**: 화면 전체(카드들이 들어있는 3D 공간 컨테이너)의 회전값이 지금까지 딱 두 곳(마우스 드래그, 그리고 1.2초 동안 아무 조작이 없을 때 자동으로 살짝 도는 기능)에서만 설정되고 있었다 — 즉 페이지에 처음 들어와서 드래그를 한 번도 안 하고 1.2초도 안 지났다면, 이 컨테이너는 "전혀 안 돌아간 상태"였다. 반면 각 카드는 "컨테이너가 지금 이 정도는 돌아가 있을 것"이라고 가정하고 자기 자신을 반대로 돌려서 항상 카메라를 보게 만드는 계산을 하는데, 실제로는 컨테이너가 하나도 안 돌아가 있었으니 이 계산이 전부 어긋나 카드들이 눈에 띄게 비뚤어져 보였던 것이다. 그러다 1.2초가 지나 자동 회전 기능이 처음으로 컨테이너 회전값을 실제로 설정하는 순간, 모든 카드가 갑자기 맞아떨어지며 "정면을 향하는" 것처럼 보인 것 — 정확히 사용자가 본 증상(움직이다가 잠시 뒤 갑자기 정면을 향함)과 타이밍까지 일치했다. 이전 라운드의 수정(회전 성분 개수 맞추기)은 그 자체로는 옳았지만 진짜 원인이 아니었다.

**수정**: 카드들의 회전을 계산하는 함수 자체가 컨테이너의 회전값도 매번 함께 맞춰 쓰도록 바꿨다. 이제 이 둘이 항상 같은 곳에서, 항상 함께 갱신되므로 다시는 어긋날 수 없다.

**② 그룹 카드 선택 시 "하부가 펼쳐지는" 과정 복원 + 전체를 하나의 매끄러운 동작으로 재설계**: 지난 재설계에서 하부 카드들이 그냥 불투명도만 바뀌며 나타나도록 단순화됐던 부분을, "하부 카드들이 허브(중심 카드)로부터 뻗어나가듯 펼쳐지는" 원래 동작으로 되살렸다 — 지난 라운드에 복원한 "접힐 때 모여드는" 동작과 정확히 대칭되는, 펼칠 때 버전이다.

더 중요한 건 **끊김 없이 이어지는 문제**였다. 기존에는 (1) 클릭한 그룹 카드가 중앙으로 다 커질 때까지 기다렸다가 → (2) 그 다음에서야 화면 전체를 통째로 다시 그리며 하부 카드들이 나타나는, 완전히 분리된 두 단계였다 — 이게 정확히 "카드가 중앙에 오고, 그다음... 전체가 다시 들어온다"는 끊기는 느낌의 원인이었다. **수정**: 클릭한 카드가 다 커지기를 기다리지 않고, 훨씬 이른 시점(원래 대기시간의 약 3분의 1)에 화면 재구성을 시작하도록 바꿔서, "카드가 커지는 움직임"과 "하부가 펼쳐지는 움직임"이 서로 겹쳐서 동시에 진행되게 했다 — 이렇게 하면 두 개의 분리된 동작이 아니라 하나로 이어지는 동작처럼 느껴진다. 이 과정에서 클릭했던 카드 자신이 "일반 카드"에서 "새로운 중심 카드(허브)"로 내부적으로 교체되는 순간에도 팍 튀지 않도록, 교체되기 직전까지 커지고 있던 크기값을 그대로 새 허브에 이어받게 해서 크기가 끊기지 않고 계속 자연스럽게 이어지도록 만들었다.

**③ "데이터 로딩이 느리면 빈 껍데기부터, 이미지는 나중에"**: 확인해보니 이 사이트는 카드 내용(제목, 이미지 등)을 서버에서 실시간으로 가져오는 구조가 아니라 전부 페이지에 미리 담겨 있는 정적 데이터라서, 애초에 "데이터 로딩이 느려서 위치 잡는 게 지연되는" 상황 자체가 발생하지 않는다. 또한 카드의 위치·크기·투명도 애니메이션은 처음부터 이미지 로딩 완료 여부와 전혀 무관하게 진행되도록 짜여 있었다(이미지는 브라우저가 알아서 백그라운드로 불러오고, 카드 레이아웃은 그것과 무관하게 먼저 자리를 잡는다) — 즉 요청하신 "위치부터 먼저, 이미지는 나중에" 원칙은 이미 지켜지고 있었다. 새로 만들 필요는 없었지만, 앞으로 이 부분을 건드릴 일이 생기면 이 원칙을 반드시 유지해야 한다.

**검증**: 화면 컨테이너의 실제 회전값을 첫 진입 시점부터 직접 측정해, 이전에는 비어 있다가(회전 없음) 1.2초 뒤 처음 값이 채워지던 것이 수정 후에는 처음부터 올바른 값으로 채워져 있음을 확인. 그룹 카드를 클릭한 뒤 하부 카드 하나를 프레임 단위로 추적해 실제로 중심에서 0으로 시작해 서서히 자리를 잡아가며 불투명도도 0→0.43→0.87로 점진적으로 올라가는 것을 정밀 측정으로 확인(약 400ms에 걸쳐 실제로 진행됨 — 순간적으로 팍 나타나는 게 아님). 클릭 직후 커지고 있던 카드의 크기값이 화면 재구성 시점에도 끊기지 않고 이어짐을 확인. 전환 도중 스크린샷으로 "커지고 있는 중심 카드"와 "펼쳐지고 있는 하부 카드들"이 동시에 함께 보이는 것을 시각적으로 확인. 상단 클릭 진입, 배경 카드 클릭 진입, 말단 카드 포커스/해제, 완전한 뒤로가기까지 포함한 전체 과정을 3회 반복해도 전부 콘솔 에러 없이 정상 동작함을 확인.

### 레벨 전환을 "다 지우고 새로 그리기"에서 "같은 카드를 진짜로 재사용해 이동"하는 방식으로 전면 재설계(2026-08-25, 같은 날 마지막 라운드)
"펼쳐진 그룹 카드를 다시 선택 시, 하부 카드들이 모이고 나서 현재 그룹 카드가 다른 그룹들 사이로 이동하는 게 있어야 돼. 그게 없다. 펼쳐져 있는 그룹 상태에서 다른 그룹을 선택 시의 전환 과정이 매끄럽지 않다. 여전히 외부에서 펼쳐지고 다시 동시에 들어온다... 무조건 매끄럽게 전체가 움직이는 과정이 다 보여야 된다. 그게 대전제야."

**원인**: 지금까지는 그룹을 열거나 닫을 때마다 화면에 있던 카드들을 전부 지우고 처음부터 다시 그리는 방식이었다. 계속 남아있어야 할 카드(예: 상위 그룹들, 방금까지 중심이었다가 이제 일반 카드가 되는 카드)도 예외 없이 "일단 사라졌다가 다시 옅게 나타나는" 식으로 처리되고 있었던 것 — 이게 "바깥에서 펼쳐지고 다시 들어온다"는 끊기는 인상의 진짜 원인이었다. 특히 방금까지 중심 카드였던 것이 접힐 때 그냥 사라지고 다른 곳에 새 카드가 옅게 나타나는 식이라, "그 카드가 이동한다"는 느낌 자체가 없었다.

**수정**: 화면이 바뀔 때마다 "이 카드, 이전 화면에도 있었나?"를 먼저 확인해서, 있었다면 화면 요소 자체를 그대로 재사용해 새 위치로 부드럽게 이동시키고(불투명도가 한 번도 0으로 떨어지지 않음 — 사라지는 순간이 아예 없음), 처음 보는 카드만 허브에서 튀어나오듯 새로 나타나게 했다. 특히 그룹을 접을 때 방금까지 중심이었던 카드는, 사라지는 대신 "일반 카드로 변신해서 다른 그룹들 사이 자기 자리로 실제로 이동하는" 모습이 보이게 됐다 — 요청하신 바로 그 동작이다. 다른 그룹으로 옮겨갈 때도, 계속 남아있는 상위 그룹 카드들이 사라졌다 다시 나타나지 않고 그 자리에서 매끄럽게 재배치된다.

**검증**: 그룹을 접을 때 방금까지 중심이었던 카드를 이름으로 추적해 매 프레임 상태를 기록한 결과 — 불투명도가 처음부터 끝까지 단 한 번도 0이 되지 않고 "1"로 유지됐고, 크기는 실제로 1.85배(중심 카드 크기)에서 1배(보통 카드 크기)로, 위치는 화면 중앙(0)에서 다른 카드들 사이(약 162px 떨어진 위치)로 부드럽게 이동하는 것을 확인했다. 다른 그룹으로 옮겨갈 때는 화면 요소 자체가 진짜로 재사용되는지(단순히 비슷해 보이는 게 아니라 완전히 같은 객체인지) 직접 확인했다 — 19개 카드 중 6개(공통 상위 그룹들)가 정확히 동일한 화면 요소로 유지됨을 확인했고, 완전히 다른 가지로 건너뛴 경우 이전 중심 카드는 정상적으로(옅어지며) 사라지는 것도 함께 확인했다. 상단 클릭 진입·배경 카드 클릭 진입·말단 카드 포커스/해제·완전한 뒤로가기를 포함한 전체 과정을 3회 반복해도 그리드·카드 밝기가 항상 정상 범위로 유지되고 콘솔 에러 없이 통과함을 확인.

### 홈페이지 첫 진입 시 가상 공간 모드를 랜덤하게 선택(2026-08-25, 같은 날 마지막 라운드)
"홈페이지가 시작할 때 가상 화면을 랜덤하게 올려 줘." 지금까지는 항상 도넛 브라우저(1번 모드)로 시작했는데, 페이지를 새로 열 때마다 도넛 브라우저와 인터랙티브 네트워크 중 하나를 무작위로 골라 먼저 보여주도록 바꿨다. 이후 모드 전환 버튼으로 오가는 동작은 기존과 동일하다.

인터랙티브 네트워크가 "첫 화면"으로 뜨는 경우는 지금까지 한 번도 실제로 일어난 적이 없던 경로였다(항상 토글 버튼을 눌러서, 즉 전환 애니메이션을 거쳐서만 들어갔었음) — 그래서 이 경로도 별도로 새로 검증했다. 페이지를 12번 새로 열어 도넛/네트워크가 각각 첫 화면으로 뜨는 것을 모두 확인했고, 네트워크가 첫 화면일 때도 카드·연결선·궤도 회전이 전부 정상적으로 초기화됨을 확인했으며 콘솔 에러도 없었다.
- **카드 화면 흐림의 진짜 원인**: "아까 블러처리 오류 수정했다고 하나, 여전히 카드 화면이 흐리다." 이전에 제거한 CSS 블러 효과는 실제로 있던 문제였지만 전부는 아니었다 — 네트워크 카드가 데이터 월(아카이브) 그리드용으로 압축된 저용량 이미지(`art-XX.webp`)를 쓰고 있었던 게 남은 원인이었다. 확인해보니 원본과 픽셀 해상도(1254×1254)는 같았지만 압축 품질이 더 낮아(같은 이미지 기준 약 138KB vs 204KB) 카드 크기로 확대해서 보면 부드럽게 뭉개져 보였다. 도넛 브라우저 카드가 원래 쓰고 있던 고화질 원본(`original-XX.webp`)으로 교체해 선명하게 만들었다.

### 랜덤 초기 모드 도입 후 "그리드가 깨진다"(2026-08-25, 같은 날 마지막 라운드)
"랜덤 기능을 넣고 나니까 그리드가 깨진다." Playwright로 네트워크 모드가 첫 화면으로 뜨는 경로를 직접 조사해 카드·연결선의 인라인 `opacity`가 빈 문자열(`""`)로 남아있는 걸 확인 — 즉 실제로 투명도 값이 아예 설정되지 않은 상태였다.

**원인**: 바로 앞 라운드("레벨 전환을 재사용 방식으로 전면 재설계")에서 DOM 요소를 새로 만드는 부분(`renderNetworkDom()`)을 고치면서, 새로 생기는 카드·연결선에 초기 `opacity`를 실제로 적어주는 코드 한 줄이 실수로 빠졌다(내부 장부 값(`dataset.baseOpacity`)만 남기고). 지금까지 이 버그가 안 보였던 건 도넛↔네트워크 전환, 그룹 진입/뒤로 가기 등 지금까지 네트워크 모드로 들어가는 모든 경로가 전부 자기 나름의 등장 애니메이션(흩어졌다 모이기, 페이드인 등)을 갖고 있어서 그 안에서 opacity를 다시 한번 명시적으로 지정해줬기 때문 — 유일하게 아무 애니메이션도 없이 "그냥 딱 그려지는" 경로가 이번에 새로 추가된 "페이지를 열자마자 네트워크 모드로 바로 시작"뿐이었고, 그 경로에서만 이 빠진 opacity 설정이 그대로 드러났다.

**수정**: 새로 만들어지는 카드·연결선에는 만들어지는 즉시 올바른 초기 투명도 값을 명시적으로 적어주도록 복원(기존 요소를 재사용하는 경우는 건드리지 않음 — 재사용 요소는 자기 "이전" 투명도를 그대로 유지해야 전환 애니메이션이 정상 작동하므로).

**검증**: Playwright로 네트워크 모드가 첫 화면으로 뜰 때까지 반복 새로고침(3회) → 매번 카드·연결선 opacity가 정상 값(`"1"`/`"0.85"` 등)으로 설정됨을 확인, 콘솔 에러 0건. 이어서 리프 포커스, 그룹 진입, 배경 카드 클릭, 뒤로 가기를 3회 반복하는 회귀 테스트도 실행해 opacity가 매 사이클 동일하게 안정적임을 확인(`edges: 0.85`, `cards: 1`, 스택 길이 정상 복귀) — 이번 수정이 앞서 완성된 "요소 재사용" 애니메이션 체계를 깨뜨리지 않았음을 함께 확인함.

**⚠️ 이 검증은 불충분했음 — 사용자가 직접 지적**: "그리드가 깨지는 경우는 띄우고 나서 가상 화면을 전환하면 깨지는데, 그걸 확인한 거야? 아니면 처음에 그냥 띄워보기만 한 거야? 그냥 띄워보기만 할 때에는 안 깨져." 위 검증은 전부 "페이지를 새로 열 때" 케이스만 반복한 것이었고, 사용자가 실제로 말한 재현 경로("띄우고 나서 전환")는 한 번도 직접 테스트하지 않았다. 정확한 재현 경로로 다시 확인하자 **완전히 다른, 더 심각한 실제 버그**를 발견했다 — 아래 새 항목 참고.

### "그리드가 깨진다"의 진짜 원인 — 모드를 전환할 때마다 고아 DOM 요소가 계속 쌓임(2026-08-25, 같은 날 마지막 라운드)
사용자가 지적한 정확한 재현 경로(페이지 로드 → 가상 모드 토글 버튼으로 전환 → 다시 전환)로 직접 재현·측정: 네트워크 모드로 시작 → 도넛으로 전환 → 다시 네트워크로 전환, 딱 한 번의 왕복 후 실제 카드 DOM 개수(`button.network-node`)는 24개인데 코드가 실제로 추적/제어하는 카드 배열(`networkNodes`)은 8개뿐이었다. 즉 16개는 화면 어딘가에 마지막으로 그려졌던 위치에 그대로 얼어붙어 남아있는 "유령 카드/연결선"으로, 매 프레임 위치를 다시 계산해주는 로직의 손이 전혀 닿지 않는 상태였다 — 이게 화면에 뒤엉킨 카드·엉뚱한 연결선으로 보이며 "그리드가 깨진다"로 인식된 것.

**원인**: 그룹 진입/뒤로 가기(`pushNetworkLevel`/`backNetworkLevel`)는 화면을 다시 그리기 전에 항상 "지금 화면에 뭐가 떠 있는지" 스냅샷을 찍어 넘겨줘서, 다시 그릴 때 안 쓰는 옛 요소를 정리(삭제)하고 살아남는 요소는 재사용한다. 그런데 **가상 모드를 껐다 켤 때 호출되는 두 곳(모드 진입 시 `ensureNetworkBuilt()`, 모드 이탈 직전 재동기화 코드)은 이 스냅샷을 넘기지 않고 있었다** — 스냅샷이 없으면 화면 정리(옛 요소 삭제) 로직 자체가 통째로 건너뛰어지도록 짜여 있었기 때문에, 전환할 때마다 기존 카드·연결선은 그대로 남겨둔 채 완전히 새로운 세트를 통째로 새로 만들어 덧붙이기만 했다 — 왕복할수록 유령 요소가 계속 누적되는 구조였다.

**수정**: 두 호출 지점 모두 스냅샷을 찍어 넘기도록 수정(`syncNetworkCurrent(snapshotNetworkScene())`) — 페이지를 처음 열 때는 아직 카드가 하나도 없으니 빈 스냅샷이라 기존과 동일하게 동작하고, 그 이후 모든 전환에서는 그룹 진입/뒤로 가기와 똑같은 방식으로 옛 요소를 정리하고 살아남는 요소는 재사용하게 됨.

**검증**: 네트워크 모드로 시작해 토글 버튼을 6번 눌러(왕복 3회) 매번 실제 DOM 카드/연결선 개수가 코드가 추적하는 개수와 정확히 일치함을 확인(8=8, 유령 요소 0개). 더 까다로운 시나리오로 그룹 하나에 진입해 있는 상태(계층 2단계 깊이)에서 왕복 전환을 2회 반복해도 매번 정확히 일치(19=19, 허브 포함), 이어서 뒤로 가기까지 실행해도 최상위 8개로 정상 복귀함을 확인. 스크린샷으로도 유령 카드나 엉뚱하게 뻗은 연결선 없이 깔끔하게 보임을 확인, 콘솔 에러 0건.

### 모바일에서 상하 스와이프 시 화면 자체가 스크롤됨(2026-08-25, 같은 날 마지막 라운드)
"모바일 화면에서 상하로 손가락으로 스와이프할 경우, 화면 자체가 스크롤 돼버린다." 원인은 CSS `touch-action` 속성 — `.network-shell`(인터랙티브 네트워크 모드 영역)에 `touch-action:pan-y`(세로 방향은 브라우저의 기본 페이지 스크롤에 맡기고, 가로 방향만 우리 코드가 가로채 쓰겠다는 설정)가 걸려 있었다. 이 설정은 2026-08-01, 도넛 브라우저(가로 회전만 가능)를 만들 때 정해진 값을 네트워크 모드를 새로 만들면서 그대로 복사해 온 것 — 도넛은 가로로만 도는 게 맞아서 세로 스와이프는 페이지 스크롤에 넘겨주는 게 원래 의도대로 맞지만, 그 후 네트워크 모드에는 세로 방향 카메라 회전(위아래로 훑어보기)이 추가됐는데도 이 낡은 설정이 그대로 남아있어서, 손가락으로 위아래로 밀면 정작 화면 안의 3D 회전은 전혀 반응하지 않고 브라우저가 그 제스처를 그냥 "페이지를 스크롤하라"는 뜻으로 먼저 가로채 버린 것이었다.

**수정**: `.network-shell`의 `touch-action`을 `pan-y`에서 `none`으로 변경 — 이 영역 안에서는 가로·세로 모든 방향의 스와이프를 브라우저가 가로채지 않고 전부 우리 코드(카메라 회전 로직)가 직접 처리하도록 함. 이미 같은 방식으로 동작 중인 원본 뷰어의 확대/이동 영역과 동일한 설정이다. 도넛 브라우저(`.torus-shell`)는 원래 의도대로 가로 회전만 지원하므로 건드리지 않음 — 세로 스와이프는 여전히 페이지 스크롤로 자연스럽게 넘어간다.

**검증**: 실제 터치 입력을 흉내 내는 정확한 방법(Chrome DevTools Protocol의 터치 이벤트 디스패치 — 자바스크립트로 직접 만든 가짜 터치 이벤트는 브라우저의 터치 제스처 인식 자체를 우회해버려서 이런 버그를 못 잡아낸다는 걸 확인 후 방법을 바꿈)으로 모바일 화면 크기에서 확인: 네트워크 모드에서 위로 스와이프 시 카메라 각도(피치)가 실제로 바뀌고(-8°→약 71°) 페이지 스크롤 위치는 그대로(0) 유지됨을 확인; 도넛 모드로 전환해 같은 스와이프를 해보면 여전히 페이지가 정상적으로 스크롤됨(의도한 그대로, 회귀 없음)을 확인; 가로 스와이프(좌우 회전)와 카드 탭(그룹 진입)도 문제없이 동작함을 함께 확인.

### 모바일에서 가상화면 전환 버튼이 메뉴 버튼에 너무 붙어있음(2026-08-26)
"가상화면 전환 버튼이 모바일에서는 메뉴버튼 바로 아래에 올라붙는다. 차상위 이동 버튼과 동일 세로 위치에 넣어줘." 모바일(≤640px) 화면에서 전환 버튼의 세로 위치가 `82px`로 고정돼 있어 상단 메뉴 버튼 바로 아래 너무 가깝게 붙어 있었던 반면, "차상위로 이동" 버튼(뒤로 가기 버튼)은 실제로는 화면 실측을 바탕으로 동적으로 계산되는 훨씬 아래쪽 위치(대략 `390px` 부근, 기기별로 약간씩 다름)에 놓여 있어 둘의 높이가 크게 어긋나 있었다.

**부수 발견**: 수정하려고 보니 도넛 모드의 뒤로 가기 버튼과 네트워크 모드의 뒤로 가기 버튼조차 서로 다른 세로 위치 공식을 쓰고 있었다 — 도넛 쪽은 최근에 동적 계산 방식으로 업그레이드됐는데 네트워크 쪽은 예전의 고정값(`390px`)에 그대로 남아 있던 것. 즉 지금까지는 모드에 따라 뒤로 가기 버튼 위치 자체가 미묘하게 달랐던 셈.

**수정**: 전환 버튼과 네트워크 모드의 뒤로 가기 버튼 둘 다, 도넛 모드의 뒤로 가기 버튼이 쓰는 것과 동일한 동적 계산 공식으로 통일 — 모바일에서는 이제 어느 모드에 있든 "차상위 이동" 버튼(보일 때)과 가상화면 전환 버튼이 항상 같은 가로줄에 나란히 놓인다. 태블릿/데스크톱 폭은 이번에 문제로 지적되지 않아 건드리지 않았다.

**검증**: 도넛 모드에서 하위 그룹으로 진입해 뒤로 가기 버튼이 보이는 상태와 네트워크 모드에서 동일하게 진입한 상태 각각에서 두 버튼의 실제 화면상 세로 좌표를 측정 — 두 모드 모두 전환 버튼과 뒤로 가기 버튼의 세로 좌표가 정확히 일치함을 확인, 스크린샷으로도 나란히 놓인 모습을 확인.

## 가상 공간 모드 2 — 인터랙티브 네트워크: 태양계 궤도 모델(2026-08-23, 최종 확정)

여러 차례 재설계를 거쳐 최종적으로 "우주를 생각하면 된다 — 태양 주변을 행성이 돌고, 그 태양은 은하 주변을 돌고…"라는 사용자의 명시적 모델로 확정됨. 이전 버전들(85개 카드 평면 매핑 → 도넛과 똑같은 "탑다운 드릴다운, 클릭하면 화면이 하위 목록으로 완전히 바뀌는" 방식)은 전부 대체됨 — 아래가 현재(최종) 사양이며, 다시 손댈 때는 이 섹션만 참고하면 된다.

- **배경 투명**: `.network-shell`은 별도 배경이 없다 — 메인 히어로 그라디언트가 그대로 비침("메인 화면 바탕을 그대로 사용해달라").
- **데이터**: 도넛 브라우저(모드 1)와 완전히 동일한 계층(`torusGroups`→`childTorusItems`/`makeSubgroupItems`→`makeSubItems`)을 재사용.
- **항성-행성 구조**: `networkHub`(현재 "태양" — 최상위에서는 `null`, 그 아래로 내려가면 방금 클릭해서 들어간 그룹/하위그룹 아이템)가 있으면 화면 중앙(로컬 좌표 원점 `0,0,0`)에 확대된 "허브 카드"(`.network-hub`, 일반 노드의 `NETWORK_HUB_SCALE=1.85`배, 보라색 글로우 테두리, 클릭 불가능한 순수 장식용 `<div>`)가 렌더링된다. 최상위(허브 없음)에서는 대분류 8개가 빈 공간(우주) 안에서 그냥 서로 공전한다.
- **행성은 계속 공전한다**: 각 자식 노드는 정적 위치가 아니라 `orbitRadius`/`orbitAngle`/`orbitSpeed`(랜덤, 방향도 랜덤)/`tilt`(궤도면 기울기, 랜덤)를 갖고 **매 프레임(`networkFrame()` 안의 `advanceNetworkOrbits(1)`) 자체적으로 계속 회전**한다 — 사용자가 드래그를 전혀 안 해도 행성들이 저절로 공전한다. 카메라(드래그로 돌리는 `.network-scene`의 yaw/pitch)와는 **완전히 별개의 움직임**이며 둘 다 항상 동시에 적용됨.
- **연결선(스포크)**: 허브가 있을 때만, 허브(원점)→각 행성으로 얇은 선을 그린다(`positionNetworkEdge(div, NETWORK_ORIGIN, node)`, 기존 최근접-이웃 메시 방식은 폐기). 최상위(허브 없음)에는 선이 없다 — 빈 우주에는 아직 중심 천체가 없으므로.
- **클릭 = 그 행성 쪽으로 카메라가 이동, 카드 자신은 궤도 위치 그대로**(2026-08-25 최종 규칙 — "카드 위치 이동 금지" 절 참고): 그룹/하위그룹 레벨에서 카드를 클릭해도 그 카드는 위치·크기가 전혀 바뀌지 않고 계속 자기 궤도를 돈다. 대신 카메라(시야각)가 그 카드가 클릭된 순간의 위치를 바라보도록 매끄럽게 회전한다(`panCameraToPoint()`, `NETWORK_ENTER_MS=480ms`). 동시에 나머지 형제 카드·이전 허브는 페이드아웃 → 그 다음 그 아이템의 자식들이 새로운 궤도 행성 세트로 렌더링되며 페이드인된다(`enterNetworkNode()`→`pushNetworkLevel()`). 새 레벨의 허브는 여전히 로컬 원점 `(0,0,0)`에 렌더링되고 원점은 어떤 카메라 각도에서도 항상 화면 정중앙에 투영된다.
- **뒤로 가기(줌아웃)**: 좌상단 `#networkBack`. `fadeNetworkTo(items,mode,hub,ms)`로 전체 페이드아웃→재조합→페이드인(현재 허브와 형제들의 정확한 궤도 슬롯을 유지한 채 축소하는 정밀 애니메이션은 구현하지 않고, 단순 크로스페이드로 처리 — 복잡도 대비 이득이 적어 의도적으로 단순화함).
- **리프(개별 작품) 레벨**: 여전히 2클릭 방식 유지 — 첫 클릭은 그 카드를 허브 위치로 확대만(`focusNetworkNode()`, `networkFocusedIndex`로 상태 추적), 같은 카드를 다시 클릭해야 `openModal()`(원본 뷰어)이 열린다. `item.profile`(회사 소개 항목)은 클릭 시 `#company`로 스크롤.
- **카드 모양**: 라운드 직사각형(96×128px 데스크톱), 빌보드(각 카드가 부모 씬 회전의 역행렬만큼 스스로 반대 회전해서 항상 카메라를 정면으로 바라봄, `applyNetworkBillboard()` — 수학: 자식 회전행렬 = 부모 회전행렬의 역행렬, `Ry(-yaw)·Rx(-pitch)`).

### 이 버전에 남아있는 이전 트랩들 (계속 유효)
1. `user-select:none`/`-webkit-user-drag:none`/`-webkit-tap-highlight-color:transparent`을 `.network-shell`/`.network-node`/이미지에 걸어둠 — 없으면 드래그 중 브라우저 기본 선택 하이라이트가 파란 틴트로 화면을 덮는다.
2. `startNetworkDrag()`에서 네이티브 `e.target.closest('.network-node')`가 극단적 회전 각도에서 실패할 수 있어 `networkNodeAtPoint()`(모든 노드의 `getBoundingClientRect()`를 직접 대조하는 자체 폴백)를 항상 같이 둔다.
3. `pointerdown` 시점에 클릭 대상을 미리 저장(`networkDownIndex`)하고 `pointerup`에서 `e.target`을 다시 안 쓴다 — `setPointerCapture` 때문에 필수(도넛의 `torusDownIndex`와 동일 패턴).

## 가상 모드 간 전환 연출 — 순차 "해쳐 모여" → 겹쳐서 이어지는 크로스페이드로 재구조화(2026-08-25 최종)

우상단 모드 전환 버튼을 누르면 카드들이 흩어져 화면 밖으로 사라졌다가, 새 모드의 카드들이 다시 나타나 재조합된다. 버전이 여러 번 바뀌었다(①완전 사라짐 → ②화면 안 머무름+스태거 → ③다시 ①로, 성능 수정) — 그런데 ③ 상태에서 개별 버그(낡은 좌표, 관성 미초기화)를 다 고쳐도 사용자가 계속 "중간 과정이 끊긴다"고 재보고했다. **진짜 원인은 개별 버그가 아니라 구조 자체**였다: "나가는 애니메이션이 완전히 끝난 뒤에야 들어오는 애니메이션이 시작"하는 순차 2단계 구조는, 아무리 다듬어도 "사라짐 → (짧은 공백) → 나타남"으로 읽힌다. **최종 수정**: 두 장면을 **동시에 겹쳐서** 보여주는 진짜 크로스페이드로 재구조화 — `#spatialIntro`에 `.spatial-overlap` 클래스를 걸어 전환 중엔 도넛/네트워크 셸을 강제로 둘 다 보이게 하고, `scatterOut`(나가는 장면)과 `scatterIn`(들어오는 장면, 준비된 직후 같은 실행 틱에서 바로 시작)을 **간격 없이 동시에** 발동시킨다. 두 애니메이션이 ~450ms 동안 실제로 화면에 함께 보이는 구간이 생겨야 "연결된다"는 인상을 준다는 게 핵심 교훈 — 각 단계 자체의 정확성만 고쳐서는 부족했다.

- **오케스트레이터**: `transitionSpatialMode()`(토글 버튼 클릭 핸들러 겸 최고 속도 자동전환의 호출 대상) — 드래그 플래그와 회전 관성을 모두 초기화 → `.spatial-overlap` 추가(양쪽 셸 강제 노출) → `scatterOut(420ms)` 시작 → **곧바로 같은 틱에서** 모드 전환(`applySpatialMode()`, 목적지 장면 재구성 포함) → `scatterIn(460ms)` 시작 → 둘 다 끝날 시간이 지나면 `.spatial-overlap` 제거+버튼 재활성화. "매번 목적지 장면을 완전히 새로 재구성" 자체는 예전과 동일하게 필수(그래야 `scatterIn`이 읽는 "자연 위치"가 정확함) — 달라진 건 그 재구성과 화면 노출의 **타이밍**뿐이다.
- **확장 가능한 구조는 유지**: `spatialModeTransitions={torus:{...},network:{...}}` 룩업 테이블, 세 번째 모드가 생기면 항목만 추가하면 됨.
- **거리/투명도**: `SPATIAL_SCATTER_DIST=1700px`(화면 밖으로 확실히 나가는 거리)로 밀어내며 opacity도 `0`으로 페이드 — "화면에서 안 보였다가 다시 나타나는" 느낌을 명시적으로 요청받아 원복.
- **성능(끊김/멈춤 현상) 수정**: 이전 버전은 카드마다 개별적으로 `void el.offsetWidth`(강제 리플로우)를 호출해서, 카드 수만큼 레이아웃 재계산이 반복되며 버벅임이 났다. **모든 카드의 "시작(흩어진) 상태" 스타일을 먼저 한 번에 다 세팅한 뒤, 리플로우는 컨테이너에 대해 딱 한 번만 강제**(`void torusScene.offsetWidth` 또는 `void networkScene.offsetWidth`)하고, 그 다음에 전체 카드에 트랜지션+목표(자연) 상태를 한 번에 세팅하는 "읽기/쓰기 배치(batch)" 패턴으로 변경. **DOM 스타일을 여러 개 순회 변경할 때 강제 리플로우가 필요하면 항상 컨테이너 레벨에서 한 번만 트리거할 것 — 요소마다 트리거하면 반드시 버벅인다.**
- 스태거·팝 스케일·바운스 이징은 전부 제거(성능·"무미건조함" 둘 다 스태거보다 "화면 밖으로 사라졌다 나타나는" 원래 스타일 자체가 만족스럽다는 판단, 단순한 `ease-in`/`ease-out`으로 회귀).
- **모드 전환 버튼 아이콘 교체(2026-08-25, 3차 개선까지)**: 1차 비스듬한 3D 타원 → 2차 "3등분 원+화살표"(사용자 요청대로) → 3차 세부 다듬기("틈이 안 보인다", "화살표 머리가 너무 크다·안 이쁘다", "원을 조금 더 크게"): 원 반지름과 SVG 렌더 크기를 키우고, 호 90°·간격 30°로 틈을 더 뚜렷하게 벌리고, 화살촉을 뭉툭한 삼각형 대신 작고 날렵한 노치형 모양으로 교체, 두께도 살짝 줄여 균형을 맞췄다. 3개의 호 경로가 SVG `<marker>` 하나(`orient="auto"`로 경로 접선 방향에 자동 정렬)를 공유해 화살촉 각도를 일일이 계산할 필요가 없었다. `stroke/fill="currentColor"`로 버튼의 기존 hover 색상 전환을 그대로 물려받는다.
- **렌더 루프 충돌 방지**(계속 유효): `spatialTransitionActive` 전역 플래그를 `spatialFrame()`/`networkFrame()` 맨 앞에서 확인, 드래그 시작 함수에도 동일 가드.

### 버그: "카드가 1장만 남을 때가 있다" — 진짜 원인은 원본 뷰어(모달)가 열려 있는 상태에서의 전환
1차 수정("리프 포커스 중 opacity가 안 지워짐")으로는 부족했고, **Playwright로 집요하게 재현한 끝에 진짜 원인을 찾음**: 원본 이미지 뷰어(`.modal.open`)가 열려 있는 동안 배경의 도넛/네트워크는 계속 관성으로 돌고 있는데(모달이 열려도 `spatialFrame()`/`networkFrame()`이 안 멈춤), 그 잔여 관성이나 자동전환 임계값을 넘기거나 사용자가 실수로 버튼을 누르면 **모달이 화면을 덮고 있는 동안 뒤에서 `transitionSpatialMode()`가 실행**된다 — 이 상태에서의 흩어짐/재조합 연출·리빌드는 사용자가 보지 못하는 상태에서 꼬여서, 모달을 닫고 나면 카드 대부분이 사라진 채로 발견된다(재현: 리프 카드를 두 번 클릭해 모달을 연 뒤 모드 토글 버튼을 누르면 100% 재현). **수정**: `transitionSpatialMode()` 맨 앞에 `if(modal.classList.contains('open'))return`을 추가 — 원본 뷰어가 열려 있으면 수동 버튼도 자동 폭발도 아예 아무 일도 안 일어난다(뷰어를 닫은 뒤에는 정상 동작). Playwright로 "드릴다운→포커스→모달 열기→토글 시도(차단 확인)→모달 닫기→토글(정상 동작 확인)"을 4라운드 반복 검증, 매번 카드 전부 정상 표시됨. **교훈**: 배경에서 계속 도는 애니메이션/전환 시스템이 있으면, 그 위를 덮는 다른 풀스크린 UI(모달 등)가 열려 있는 동안에도 배경 트리거가 계속 발동할 수 있다는 걸 잊기 쉽다 — "지금 사용자가 실제로 이 화면을 보고 있는가"를 명시적으로 확인하는 가드가 필요.
- 상하 회전 180도 제한도 이 라운드에 같이 제거됨 — `dragNetwork()`/`networkFrame()`의 `networkPitch` 클램프(`±75°`)를 완전히 삭제, 이제 좌우처럼 무제한 회전.

### 최고 속도 회전 시 자동 모드 전환("폭발")
`spatialFrame()`/`networkFrame()`의 관성 분기 안에서 속도가 임계값을 넘으면 `transitionSpatialMode()`를 그대로 호출(버튼과 동일한 연출). 처음 잡은 임계값(도넛 `.22`, 네트워크 `30`)이 실제 사용해보니 잘 안 터진다는 피드백으로 **`torusVelocity>.12`(반경/확대 부스트가 최대치에 도달하는 지점 `|v|≈.16`보다 오히려 낮춤 — 실사용 체감상 이전 값은 너무 안 터졌음), `Math.hypot(networkVelocityYaw,networkVelocityPitch)>16`**로 낮춤. 손을 뗀 뒤 관성 단계에서만 발동(드래그 중엔 발동 안 함, 의도적).

## 도넛/네트워크 브라우저 2026-08-27 라운드 — 상하 틸트, 전환 다양화, 발광 테두리, 화질 문제 근본 수정

이 날 하루 동안 이어진 대규모 개선 라운드. 항목이 많아 기능별로 정리한다.

### 도넛 카드 상하 틸트(±45°) + 뒷카드 선택 버그
카드를 직접 잡고 위아래로 드래그하면 도넛 전체가 ±45° 범위로 틸트된다(마우스·터치 모두 지원). **카드 위에서 시작한 드래그만 틸트**, 빈 배경 드래그는 기존 좌우 회전/스크롤을 그대로 유지 — `touch-action:none`을 `.torus-card`에만 걸고 셸 자체는 `pan-y` 유지, `torusDownIndex`로 "카드에서 시작했는지"를 판별하는 JS 게이트로 구현. 틸트로 뒤로 젖혀 드러난 카드를 클릭해도 앞으로 안 나오는 버그는 **진짜 Chromium 네이티브 히트테스트 버그**였다(디버그 바운딩 박스가 실제 사진과 정확히 일치하는데도 `elementFromPoint`/`closest()`가 부모를 반환) — `networkNodeAtPoint()`와 동일한 수동 폴백 `torusCardAtPoint()`를 만들어 해결. 네트워크 모드도 동일하게 "카드 드래그=원래 동작, 배경 드래그=스크롤" 분리 적용.

### 카드 선택 회전을 매끄러운 전체 링 회전으로, 리프 카드 클릭 시 모달로 확대(zoom) 진입
도넛에서 카드를 선택하면 링 전체가 재구성이 아니라 매끄럽게 회전하도록 수정(`tweenTorusRotation`). 리프 카드를 클릭해 원본 뷰어로 들어갈 때 카드가 실제로 화면 중앙으로 확대되며 모달로 이어지는 전환을 추가(`zoomCardIntoModal`/`reverseZoomFromModal`, 유령 오버레이 FLIP 기법 — 모달을 닫을 때도 카드 자리로 역재생). 동영상 카드는 재생 중인 비디오가 아니라 포스터 이미지를 복제해 유령을 만든다.

### 모드 전환("헤쳐 모여") 다양화 — 방향·회전축·바운스·궤도·사인파, 그리고 "착지 후엔 정지" 원칙
가상 공간 전환(도넛↔네트워크) 시 카드가 흩어지고 재조합되는 연출을 여러 차례에 걸쳐 다양화했다. 핵심 원칙(사용자 명시): **모든 통통 튀기/회전 효과는 전환 "과정 중"에만 있어야 하고, 착지 후에는 절대 없어야 한다 — 유일한 예외는 착지 직후 Y축으로만 팽그르르 빠르게 돌다 탁 멈추는 것**(`landingSpinSnap`, 460ms, Y축 전용). 이 원칙을 어긴 초기 버전(착지 "이후" setTimeout으로 별도 발동하는 `landingFlourish`/`landingBounce`)은 전부 제거하고, 바운스류 효과는 전부 흩어짐/재조합 애니메이션 "안에" 키프레임으로 내장했다.
- **경로 스타일 4종**: `pickPathStyle()`이 매 카드마다 `bounce`(통통 튐, 15%) / `orbit`(공간을 삥 둘러 나가거나 들어옴, 12%) / `wave`(사인파로 출렁이며 이동, 12%) / `plain`(직선 이동, 61%) 중 하나를 뽑는다. 공통 오프셋 계산은 `fancyOffset(kind,seed,t,...)` 하나로 통합했고, WAAPI 키프레임 생성(`buildFancyKeyframes`, scatter-out용)과 매 프레임 실시간 추적(`driveFancyLive`, scatter-in용) 두 경로 모두 이 동일한 오프셋 함수를 공유한다.
- **이동 방향 7종·회전축 다양화**: `up/down/left/right/radial/toward/away` 7가지 스캐터 방향(`scatterVector`), 회전은 X/Y/Z 중 한 축만 쓰는 `randomTumble()`(16% 확률로 1080~1800° "휘리릭" 대회전).
- **속도 조정 이력**: "너무 빠르다→반으로 늦춤→50% 더 빠르게(단 팽그르르/바운스 종류는 간혹 섞기)→전체 다시 반으로 늦춤→바운스/사인파 계열은 특히 안 보이니 별도로 더 느리게" 순으로 여러 차례 조정됐다. **교훈: "반 정도 늦춰줘/빠르게 해줘"는 항상 원본이 아니라 직전 값 기준의 상대 조정으로 해석해야 한다.**
- **버그: 화면 잔여 UI가 바운스 카드를 가림** — 흩어지는 카드가 원래 위치에서 멀리 벗어나면 `.intro-copy`/`.intro-meta`(z-index:10)에 가려 안 보였다. 활성 스캐터 카드에 `z-index:150`을 임시로 부여, 완료 시 제거.
- **"절반 이상 합류되면 브라우저 정상 동작, 늦게 합류하는 카드는 늦게라도 제자리 찾기"**: `settleTracker(total)`이 `half`/`all` 두 Promise를 제공 — 절반 이상 완료되면 인터랙션을 재개하고, 전부 완료되면 나가는 장면을 완전히 숨긴다.
- **버그(두 차례 재발): 늦게 합류하는 카드가 위치 보정 중 "튄다"** — 1차 수정(`smoothLateJoin`, CSS 트랜지션으로 마지막 순간만 매끄럽게)은 불충분했다. **진짜 원인**: WAAPI 바운스 애니메이션이 전환 시작 시점의 "얼어붙은" 목적지로 향해 날아가는 동안, 그 아래 링은 관성 회전으로 계속 움직여 실제 목적지가 계속 바뀌고 있었다(목적지 드리프트). **최종 수정**: `driveFancyLive` — `requestAnimationFrame` 루프에서 매 프레임 `layoutTorus()`/`applyNetworkBillboard()`를 실제로 호출해 "지금 이 순간의 진짜 목적지"를 다시 읽어와 추적하는 방식으로 완전히 재작성. 주입 테스트(중간에 `torusRotation+=1.4` 강제 변경 후 50ms 간격 위치 샘플링)로 "튐 비율"(단일 스텝 이동량÷전체 이동량)이 0.07~0.21로 나와 실제 튐(≈1.0)이 없음을 확인.
- **"안 움직이는 카드가 있다" 버그**: `scatterVector`의 `radial`(기본) 케이스가 카드의 로컬 좌표가 원점(0,0,0)에 가까울 때 방향 벡터가 0이 되어 지터만 남는 결함이 있었다 — 원점 근처 카드는 랜덤 방향으로 강제 대체하도록 수정.

### 도넛 최전면 카드 발광 테두리 — 번쩍임 + 회전하는 "고슴도치" 빛 꼬리
최전면(`.front`) 카드에 두 단계 효과: ①테두리가 순간적으로 밝게 번쩍(`torusFrontFlashGlow`, box-shadow 키프레임) ②번쩍임이 잦아들면 테두리를 따라 빛이 도는 체이스 효과(`torusFrontChase`, `@property`로 등록한 각도 커스텀 프로퍼티 + conic-gradient 마스크-xor 트릭). 이후 "더 강하게, 푸른색 계열로, 맨 앞에 고슴도치 같은 빛+꼬리, 회전 속도 반으로"라는 요청에 따라 색을 푸른색(rgba(56,142,255,*))으로, 강도를 2배 가까이 키우고, 회전 주기를 2.4s→4.8s로 늦췄다. "고슴도치 머리처럼 안 보인다"는 재지적이 있어 conic-gradient의 스톱을 313~360° 구간에 촘촘하게 명암 교대로 배치해 가시성을 높였다(완전히 해결됐다는 확증은 없음 — 단일 `::before` 안에서 표현 가능한 범위의 한계로, 추가 개선 여지가 남아있다). 동영상 카드에는 이 효과가 안 보이던 버그는 `.torus-card.front:before`에 `z-index:3`을 명시해 해결(비디오를 감싸는 `<span class="video-frame">`이 `z-index:auto`라 명시적 z-index를 가진 가상요소보다 항상 아래에 그려지는 CSS 스태킹 규칙이 원인 — `video-frame`에 직접 `position:relative`를 강제하는 안은 다른 곳의 `position:absolute;inset:0` 의존 레이아웃을 깰 위험이 있어 폐기).

### 네트워크 모드 화질 문제 — 두 가지 서로 다른 원인, 두 차례에 걸쳐 수정
1차: 허브/카드 사진이 평소엔 흐리다가 마우스를 올리면 선명해지는 현상 — 아카이브 월과 동일한 "GPU 합성 레이어 페인트 무효화" 버그(이미지는 로드 완료됐지만 3D-transform 카드 스택 안에서 실제 화면 레이어에 합성되지 않다가 우연한 리페인트가 있어야 반영됨)로 진단, `forceMediaClarity()`(decode 후 display 토글 강제 리페인트)를 허브·모든 카드 이미지 생성 직후에 호출해 해결하고, 눌림 효과에 기대던 걸 없애는 대신 hover 시 테두리 발광으로 대체.
2차(같은 날 후속): "여전히 흐리다, 마우스 오버로 포커싱되면 **작아지면서** 또렷해진다"는 재보고로 **진짜 원인이 다른 버그였음이 드러남** — 허브는 평소 `NETWORK_HUB_SCALE=1.85`배로 확대되어 표시되는데, Chromium이 `transform:scale()`로 확대되는 합성 레이어를 낮은 해상도 텍스처 그대로 재사용해 스케일이 클수록 흐려지고 1배(포커스 시 축소)에 가까워질수록 선명해지는 것이었다(1차 수정의 "생성 시 1회 리페인트"로는 스케일이 나중에 커질 때를 커버 못 함). **수정**: `networkFrame()`에서 매 프레임 이징 중인 `displayScale`이 목표값에 0.015 이내로 수렴하는 "정착 순간"을 감지해 그때마다 `forceMediaClarity()`를 다시 호출(허브·각 카드 공통, 목표값이 바뀌면 정착 플래그 리셋) — 즉 "생성 시 1회"가 아니라 "표시 크기가 안정될 때마다" 리페인트하도록 일반화. 동시에 이번 라운드에서 실험적으로 추가했던 `.network-node`/`img`의 `will-change:transform`은 오히려 이 문제를 악화시킬 위험(레이어를 고정 승격시켜 저해상도 텍스처 재사용을 유도)이 있다고 판단해 제거, `image-rendering:-webkit-optimize-contrast`(도넛 카드와 동일)와 `backface-visibility:hidden`만 남겼다. **이 화질 저하가 실제로 해소됐는지는 육안 확인 전 단계 — 다음에 사용자가 다시 흐리다고 보고하면 스케일 정착 감지 임계값(0.015)이나 정착 판정 자체를 의심할 것.**
3. 네트워크 모드의 hover 발광 색상을 기존 보라색 계열에서 도넛과 통일된 푸른색 계열로 변경(`.network-hub:hover`, `.network-node--current:hover`, `.network-node:hover`).

### 원본 보기 진입 확대(zoom) 크기를 원본 이미지 실제 크기에 맞게 제한
`zoomCardIntoModal`의 확대 유령(ghost)이 뷰포트 고정 비율(62~86%)로만 커져서, 이미지의 실제 원본 크기/비율과 무관하게 과도하게 확대되는 문제 — 미디어의 `naturalWidth/naturalHeight`(비디오는 `videoWidth/videoHeight`)를 읽어 뷰포트 상한 안에서 **원본 해상도를 넘지 않는 배율로만** 확대하도록 수정(`Math.min(maxW/naturalW,maxH/naturalH,1)`). 미디어가 아직 로드 전이라 자연 크기를 못 읽는 경우엔 기존 고정 비율 방식으로 자동 폴백.

### 같은 날 후속 라운드 — "안 움직이는 카드" 진짜 원인 발견, 발광 테두리 재설계, 자전/공전 추가, 즉시 회전 재개

**작업 원칙 확인**: 사용자가 "이전에 루틴을 추가한 것들을 빼지 말아줘. 새로 추가한 것들인데, 왜 자꾸 예전의 걸 빼는 거야?"라고 명시적으로 지적했다. 이 라운드부터는 기존 루틴(예: plain 카드의 `randomTumble()` 회전 방식, `bounce`/`orbit`/`wave` 경로 시스템 자체)을 절대 재구성하거나 제거하지 않고, 전부 **순수 추가(additive)** 방식으로만 처리했다 — 아래 항목들 모두 기존 코드 경로를 건드리지 않고 새 필드/새 분기만 얹었다.

- **"안 움직이는 카드" 진짜 원인 발견**: 지난 라운드의 "원점 근처면 방향이 0" 수정으로는 부족했다. 실제 원인은 `scatterVector`의 `toward`/`away`(그리고 가끔 `radial`) 종류가 카메라를 향해/반대로(주로 Z축)만 강하게 밀고 화면 평면상의 가로/세로(X/Y) 이동은 순전히 지터(무작위 값, 0에 가까울 수도 있음)에만 의존한다는 점이었다 — Z축 이동은 3D 원근 투영에서 주로 "크기 변화"로만 보이고 화면상 위치는 거의 그대로일 수 있어, 실제로는 좌표가 바뀌어도 "제자리에 가만히 있는" 것처럼 보였다. Playwright로 실제 화면 픽셀 위치(`getBoundingClientRect`)를 전후 비교해 확인. **수정**: 모든 종류(kind) 계산 후 "화면 평면상 이동량(가로/세로 벡터 길이)"이 최소 기준(`D*.5`) 미만이면, **기존 벡터를 같은 방향으로 그 최소치까지 확대(rescale)** — 새 무작위 방향을 더하는 방식은 기존 지터와 상쇄될 위험이 있어(실제로 1차 시도에서 이 버그가 났었다: 무작위 보정 벡터가 기존 지터와 반대 방향으로 걸리면 서로 상쇄돼 여전히 작은 값이 나옴) 반드시 "기존 방향을 유지한 채 배율만 키우는" 방식이어야 함. Playwright로 `toward`/`away`/`radial` 200회 샘플의 최소 화면-평면 이동량이 858~935px(기준 850px 이상)임을 확인, 실제 전환 8개 카드의 전체 이동거리도 836~1178px로 전부 충분히 큼을 확인.
- **최전면 카드 발광 테두리 재설계 — "점선처럼 보인다"는 지적 반영**: 사용자가 "고슴도치"라는 표현으로 원했던 건 점박이 무늬가 아니라 "나뭇잎 사이로 갑자기 탁 보이는 햇살"처럼 밝은 한 덩어리의 빛(+꼬리)이었다. 기존 313~360° 구간의 명암 교대 스톱(밝음-어두움-밝음... 반복)이 정확히 "점선"으로 보이는 원인이었다 — 전부 제거하고, 215°(꼬리 시작)부터 360°(머리)까지 **끊김 없이 한 방향으로 밝아지기만 하는** 단일 conic-gradient 램프로 교체, 머리 부분(357~360°)은 순백색 덩어리로 고정. 여기에 `filter:drop-shadow()`를 3겹(타이트한 흰빛 3px + 중간 파란빛 13px + 넓은 파란 번짐 32px)으로 쌓아 머리 부분이 실제로 강하게 번지는 플레어처럼 보이게 함(가장 밝은 지점일수록 번짐도 강하게 몰리는 성질 이용). 동영상 카드 지원은 지난 라운드에 이미 `z-index:3`으로 해결됐고 이번 모양 변경도 카드 종류와 무관하게 동일 규칙이라 별도 처리 불필요.
- **최전면 카드 첫 번쩍임 강도 절반으로 감소**: `torusFrontFlashGlow`의 피크 `box-shadow` 값들(흰 테두리 링·파란 bloom)의 반경/블러/알파를 대략 절반으로 낮춤.
- **자전(self-spin) 추가 — 이전엔 bounce/orbit/wave 경로에 회전이 전혀 없었다**: 확인해보니 `bounce`/`orbit`/`wave` 세 경로는 비행 내내 회전값이 목적지의 고정값으로 완전히 얼어붙어 있어서(디자인상 처음부터 그랬음), 화면 밖으로 나가거나 들어올 때 전혀 안 돌고 있었다. `fancySeed(kind)`가 이제 매 카드마다 `spin:{axis,deg}`(단일 축, 16~20% 확률로 900~1620° "휘리릭")도 함께 뽑고, `buildFancyKeyframes`(나가기, 0°→풀 각도로 증가 — 날아가며 점점 빨리 돎)와 `driveFancyLive`(들어오기, 풀 각도→0°로 감소 — 이미 돌면서 등장해 착지하며 딱 멈춤) 둘 다 이 값을 목적지 회전값 앞에 곱으로 얹는다. **plain 카드의 기존 `randomTumble()` 방식은 완전히 그대로 유지** — 이번에 손댄 건 원래 회전이 전혀 없던 bounce/orbit/wave 세 경로뿐.
- **공전(orbit) 반경 확대**: 기존 `260~480px`(전체 흩어짐 거리 1700px에 비해 작아 "공전"처럼 안 읽힘) → `520~900px`로 확대, 가상 공간을 실제로 삥 둘러 나가거나 들어오는 느낌이 훨씬 뚜렷해지도록.
- **늦게 합류하는 카드를 기다리지 않고 가상 공간이 먼저 회전 재개**: 기존엔 `settleTracker`의 `half`(카드 절반 이상 착지) 시점에야 카메라/링 자동 회전이 재개됐는데, 사용자는 그마저도 기다리지 않고 "가상공간이 먼저" 돌기를 원했다("그게 멋있어"). `transitionSpatialMode()`를 수정해 `scatterIn()` 호출 직후 더블 `requestAnimationFrame`(흩어진 시작 위치가 화면에 반영될 시간만 확보하는 최소 지연)만 기다린 뒤 곧바로 회전/상호작용을 재개하도록 변경 — `settleTracker`의 `half`/`all` Promise 자체는 그대로 두고(과거에 추가한 루틴이라 삭제하지 않음, `all`은 여전히 배경 셸을 언제 완전히 숨길지 결정하는 데 쓰인다), 재개 시점만 `half` 대신 즉시로 바꿨다. **부작용 방지**: 카메라가 이렇게 일찍부터 다시 돌면, `plain` 경로 카드(들어올 때 여전히 CSS 트랜지션으로 "고정된 목적지"를 향해 감)가 트랜지션이 끝나는 순간 실제로는 이미 회전해버린 새 위치와 어긋나 "탁" 튈 위험이 생긴다 — 이 위험을 없애기 위해 plain 카드가 착륙한 직후(`landingSpinSnap` 확률(15%)에 걸리지 않은 나머지) 전부 기존에 이미 만들어 둔 `smoothLateJoin()`(0.5초 매끄러운 보정 글라이드, 이전 라운드에 bounce용으로 만들었다가 지금은 `landingSpinSnap`에서만 쓰이던 함수)을 추가로 호출하도록 했다 — **기존 함수를 그대로 재사용**한 것이라 새 회귀 위험이 낮다.

이 라운드는 Playwright로 (1) 회전 재개 시점이 실제로 ~270ms대로 단축됐음, (2) 8개 카드 전원이 유의미하게(836px 이상) 이동함, (3) 3회 연속 왕복 후 카드 8/8 유지·좀비 애니메이션 0개, (4) 새 콘솔 에러 없음(무관한 기존 `ppt-manifest.js` 404 제외)을 확인했다. 발광 테두리가 실제로 "햇살처럼" 보이는지, 자전/공전이 사용자 눈에 만족스러운지는 스크린샷만으로 완전히 판단하기 어려운 영역이라 — 실제로 보시고 피드백 주시면 바로 조정하겠다.

### 같은 날 세 번째 라운드 — 튐 버그 재발의 진짜 원인(위 `smoothLateJoin` 임시조치는 불충분했음), 발광 머리 재재설계, 공전 경로 신규 추가, Y축 초고속 자전

바로 위 라운드에서 "즉시 회전 재개"의 부작용을 `smoothLateJoin`(0.5초 일회성 보정 글라이드)으로 막았다고 적었는데, 사용자가 바로 "다시 툭툭 끊기는 현상이 재발했다"고 재지적했다. **원인 재분석**: `smoothLateJoin`은 목적지를 딱 한 번만 다시 읽고 고정 0.5초 트랜지션으로 이동하는 "일회성 스냅샷" 방식인데, 회전이 그 0.5초 동안도 계속 진행 중이면 트랜지션이 끝나는 순간 또 다시 낡은 값이 되어, 다음 프레임에 `layoutTorus()`/`applyNetworkBillboard()`가 트랜지션 없이 즉시 보정하면서 여전히 "탁" 튄다 — **이전 세션에서 bounce 카드에 대해 이미 한 번 배운 교훈**(일회성 스냅샷은 결국 다시 낡아진다, 매 프레임 실시간 재계산만이 근본 해법)을 이번엔 plain 카드와 `landingSpinSnap`에 적용하지 못했던 것.

**진짜 수정**: `plain` 스타일을 `bounce`/`orbit`/`wave`와 **동일한 실시간 추적 파이프라인**(`buildFancyKeyframes`/`driveFancyLive`)에 통합했다 — 더 이상 CSS 트랜지션으로 "고정된 목적지"를 향해 가지 않고, 매 프레임 진짜 목적지를 다시 읽어 그쪽으로 계속 보정하며 이동한다. 이 통합이 "이전 루틴을 뺀다"는 우려에 걸리지 않도록, plain 카드의 회전 특성(축 40/30/30 가중치, 16% 확률 1080~1800° 대회전)은 `randomTumble()`을 건드리지 않고 **완전히 동일한 확률분포**를 반환하는 `tumbleSpinDesc()`로 로직만 추출해(순수 리팩터링, 반환값 분포 100% 동일 — `randomTumble()`은 이제 이 함수를 그대로 호출) `fancySeed('plain')`에 심었다. 마찬가지로 `landingSpinSnap`(착지 후 유일하게 허용된 Y축 팽그르르 예외)도 고정된 `natural` 스냅샷을 향해 WAAPI로 도는 방식에서, 매 프레임 `refreshFn()`으로 실시간 위치를 다시 읽어 그 위에 감쇠하는 회전만 얹는 rAF 루프로 재작성 — **이 두 변경 이후로는 `smoothLateJoin` 호출이 더 필요 없어져 자연스럽게 안 쓰이게 됐다(함수 자체는 삭제하지 않고 남겨둠, 나중에 다른 일회성 효과에 재사용 가능).**

**검증 방법론 함정, 두 가지**: (1) 이 사이트는 **첫 진입 시 가상 공간 모드가 랜덤**이라는 사실(2026-08-25 확정 사양)을 검증 스크립트가 매번 반영하지 않아서, "토러스→네트워크" 전환이라 생각하고 측정했는데 실제로는 페이지가 이미 네트워크로 시작해 "네트워크→토러스"였던 경우가 섞였다 — 이 경우 스캐터-아웃 중인 네트워크 카드의 정상적인 "투명도 0으로/큰 좌표로" 변화가 마치 심각한 버그처럼 보였다. **교훈: 이 사이트에서 전환 방향을 검증할 땐 항상 `spatialModes[spatialModeIndex]`를 먼저 읽어 시작 모드를 확인하거나, 실제 버튼 클릭만으로 원하는 모드에 먼저 도달시킬 것 — `spatialModeIndex`/`applySpatialMode()`를 테스트 코드에서 직접 조작해 모드를 강제로 정규화하면 정상 오케스트레이션을 건너뛰어 상태가 꼬일 수 있다(실제로 이렇게 했다가 총 이동거리가 수십만 px로 튀는 가짜 corruption을 만들어냈다).** (2) 연속으로 여러 번 Playwright 브라우저를 띄우는 스트레스 테스트를 하다가 `browser.close()` 누락/타임아웃으로 **크롬 프로세스가 16개까지 누적**돼 시스템 자원 경합으로 타이밍이 완전히 깨지는 가짜 버그(전 카드 popRatio 0.9~1.0)를 만들었다 — 이런 반복 테스트 세션 사이에는 `tasklist`로 잔여 chrome 프로세스를 주기적으로 확인/정리할 것.

이 두 함정을 걷어내고 (실제 버튼 클릭만 사용, 시작 모드 확인, 크롬 프로세스 클린 상태) 6회 반복 측정한 결과: 8개 카드 중 7개는 항상 "튐 비율"(단일 프레임 최대 이동 ÷ 전체 이동) 0.01~0.02로 매우 깨끗했고, 매 회 1개 카드만 0.1~0.4대의 다소 높은 값을 보였는데 — 그 1개는 항상 새로 추가한 **`loop`(공전) 경로**를 뽑은 카드였다. `loop`은 원점 기준 큰 반지름(카드의 시작 지점 거리)으로 크게 원을 그리며 도는 경로라, 3D 원근 투영 특성상 카메라에 가까워지는 순간 화면상 겉보기 속도가 실제로 커진다(연속적인 정상 움직임이지 순간이동이 아님) — 이는 버그가 아니라 "삥 둘러 들어오는" 효과 자체의 자연스러운 특성으로 판단.

- **공전(`loop`) 경로 신규 추가**: "가상공간 둘레를 돌면서 진입하여 합류하는 카드도 만들어 줘"에 대한 응답으로 5번째 경로 스타일 추가(확률 9%, 다른 스타일들과 균형 맞춰 재분배). `fancyOffset`이 이 경로에 한해 일반적인 "시작-끝 보간 + 오프셋" 공식 대신 `{abs:{x,y,z}}`(절대 좌표 직접 지정)를 반환하도록 확장 — 처음 80% 구간은 원점 중심 원을 그리며 돌고(각도가 완전히 한 바퀴), 마지막 20% 구간에서 실제 목적지로 매끄럽게 블렌드. 두 구간 경계에서 속도가 뚝 끊기는 킥이 발견돼(80% 지점에서 각속도가 갑자기 0으로 꺾이는 문제), `smoothstep`(3t²-2t³) 이징을 양쪽 구간에 적용해 경계에서의 속도 불연속을 제거했다.
- **Y축 초고속 자전 추가**: "Y축으로 회전하여 합류하는 것 중 엄청 빠른 속도로 자전하는 것도 추가해줘 (자전만 빠르게, 합류 자체는 천천히), 느린 경우도 있어야 해"에 대한 응답으로 `randomSpinDesc()`에 세 번째 등급 추가 — 축이 Y로 뽑히면 25% 확률로 2400~4200°(약 7~12바퀴)의 "초고속" 자전, 기존 일반(140~400°)/휘리릭(900~1620°) 등급은 그대로 유지(순수 추가, 기존 두 등급의 확률/범위 불변).
- **발광 머리 재재설계 → 근본 원인(overflow:hidden 클리핑) 완전 해결**: "여전히 기존처럼 돌아갔다, 햇빛처럼 빛나게 해줘"라는 재지적에 원인을 다시 조사한 결과, **`.torus-card`가 `overflow:hidden`을 갖고 있어(이미지/동영상을 둥근 모서리로 자르는 데 필수) 발광 머리의 `drop-shadow` 번짐이 카드 테두리 밖으로 퍼지지 못하고 그 안에서 잘려나가고 있었다**는 게 근본 원인으로 확인됨. 1차로는 이 제약을 유지한 채 링 두께(4px→11px)·머리 폭(3°→15°)·drop-shadow 4겹으로만 개선했으나, **사용자가 "그렇게 처리해줘"라며 근본 해결(아키텍처 변경)을 명시적으로 요청**해 실제로 진행: `.torus-card`의 `overflow:hidden`을 완전히 제거하고, 대신 `.torus-card img{border-radius:inherit;overflow:hidden}`과 `.torus-card .video-frame{border-radius:inherit!important;overflow:hidden}`을 추가해 사진·동영상이 각자 스스로 둥근 모서리로 잘리도록 책임을 이전했다(`border-radius:inherit`이라 데스크톱/모바일 브레이크포인트별로 카드의 실제 반지름이 달라져도 항상 정확히 일치). 이제 카드 자체는 클리핑을 하지 않으므로 `:before`의 drop-shadow 번짐이 카드 테두리를 실제로 벗어나 배경까지 퍼진다 — Playwright 스크린샷으로 카드 옆 배경까지 하얗고 푸른 빛이 번지는 것을 실제로 확인. 회귀 검증: 사진·동영상 카드 모두 모서리가 여전히 깔끔하게 둥글게 잘림(스크린샷 확인), 3회 왕복 전환 후 카드 8/8 유지, 콘솔 에러 없음.
- **첫 번쩍임 강도 50% 추가 상향**: 지난 라운드에 절반으로 줄인 값 기준으로 "50%를 더 추가해달라"는 요청 — 흰 테두리 링과 파란 bloom의 반경/블러/알파를 전부 1.5배.

### 같은 날 네 번째 라운드 — 그리드(연결선) 따로 놀던 진짜 원인 발견·수정, 발광 머리를 조명(전구)형으로 완전 재설계, 배경 그룹 진입 시 카메라 왕복 제거

- **"그리드가 따로 논다" 진짜 원인 발견**: 네트워크 모드에서 카드를 허브에 잇는 연결선(`.network-edge`)의 위치/각도/길이는 `applyNetworkBillboard()`가 그 카드의 "정상 궤도 좌표"(`p.px/py/pz`)만 보고 매 프레임 다시 계산하는데, 흩어짐 전환 중인 카드는 `driveFancyLive`가 그 좌표를 bounce/orbit/wave/loop 오프셋을 더해 완전히 다른 화면 위치로 그리고 있었다 — **즉, 연결선은 카드가 "원래 있어야 할 자리"를 향해 그려지고, 실제 카드 그림은 전혀 다른 곳에서 날아다니고 있어서 서로 따로 노는 것처럼 보였다.** 수정: `applyNetworkBillboard()`의 연결선 갱신 로직을 `syncEdgeToPosition(p,x,y,z)`라는 별도 함수로 추출하고, `driveFancyLive`에 매 프레임 현재 좌표를 전달받는 선택적 콜백(`onFrame`)을 추가해, `scatterInNetwork`가 각 카드의 흩어짐-경로 프레임마다 그 카드의 **진짜 현재 화면 좌표**로 연결선을 다시 그리도록 연결했다. 스크린샷 시퀀스로 확인한 결과 연결선이 각 카드의 실제 위치를 계속 따라가며 자연스럽게 줄어드는 것을 확인. 분해(scatter-out) 방향은 카드마다 소요 시간이 다른데(빠른 plain 카드 vs 느린 bounce/orbit/wave/loop 카드) 연결선은 항상 짧은 시간(`ms`)에 맞춰 사라지고 있어 "선이 카드보다 먼저 없어짐" 문제도 있었다 — 연결선의 투명도 페이드만 가장 긴 지속시간(`SPATIAL_BOUNCE_MS`)에 맞추도록 늘려 완화(폭 축소 자체는 기존대로 빠르게 유지, 시각적으로 "탁 사라짐" 대신 "서서히 옅어짐"으로).
- **"안 움직이는 카드가 많아졌다"는 위 그리드 버그와 같은 원인으로 상당 부분 설명됨**: 카드는 실제로 움직이고 있었지만 연결선이 엉뚱한 자리에 고정돼 있어 "무언가 안 움직이는 게 많다"는 인상을 준 것으로 판단 — 그리드 수정과 함께 재검증 필요(사용자 확인 대기).
- **배경(외곽) 그룹 카드 선택 시 카메라가 반대편까지 갔다가 되돌아오는 문제 수정**: `enterFromRing(index)`가 선택된 배경 카드를 향해 카메라 전체를 이동시키는 `networkFollowIndex=index` 트리거를 갖고 있었는데, 배경 링 카드는 화면 반대편처럼 먼 곳에 있을 수 있어 "중심을 거쳐 반대편까지 갔다가, 하부가 펼쳐지며 전체가 다시 들어오는" 큰 왕복이 발생했다. 이후 어차피 `pushNetworkLevel()`이 장면을 통째로 다시 지어 카메라를 리셋하므로 이 왕복은 낭비였다 — `networkFollowIndex=index` 대신 `node.displayScale=NETWORK_HUB_SCALE`을 직접 대입해 새 허브가 즉시 올바른 크기로 시작하도록 하고, 카메라 이동(follow) 자체를 제거했다. Playwright로 `networkShiftX/Y/Z`가 전환 내내 0에 가깝게 유지됨을 확인(수정 전이라면 선택한 카드의 실제 좌표만큼 크게 흔들렸을 값).
- **발광 머리를 "전구가 빛나는" 형태로 완전 재설계**: 기존 conic-gradient+mask 방식(고리를 따라 도는 그라디언트 띠)은 아무리 다듬어도 사용자가 원하는 "동그란 전구 알" 느낌이 나지 않는다는 지적이 반복됐다. **완전히 다른 기법으로 교체**: 최신 CSS 삼각함수(`cos()`/`sin()`, Chromium 111+)를 이용해 `calc(50% + cos(var(--torusFrontAngle)) * 44%)` 형태로 실제 원형 `radial-gradient` 점(지름 20px 머리 + 14px/10px/7px 꼬리 점 3개)의 중심 좌표를 카드 둘레를 따라 실시간으로 계산 — 각지지 않은 진짜 둥근 빛 덩어리가 카드 가장자리를 따라 돌면서 꼬리를 남긴다. 기존 conic-gradient/mask/padding 속성은 전부 제거(더 이상 "고리 안에서만 보이게 가두는" 기법이 필요 없어짐 — 각 layer가 처음부터 독립된 원이라 별도 마스킹이 불필요), `filter:drop-shadow` 4겹은 그대로 유지해 번짐 강도를 보존. 지난 라운드에 이미 `.torus-card`의 `overflow:hidden`을 제거해뒀기 때문에 이 새 전구 효과도 카드 테두리 밖으로 자연스럽게 번진다. Playwright 스크린샷으로 카드 둘레를 도는 뚜렷하고 둥근 파란 빛과 꼬리, 테두리 밖으로의 번짐을 직접 확인 — 이전의 "고슴도치"/"햇살" 시도들과 확연히 다른, 명확한 원형 광원으로 보임.
- **네트워크 중앙(허브) 사진이 포커스 전엔 계속 흐리다는 재재보고 — 진행 상황과 한계**: 이 문제는 이번이 세 번째 보고다. 매 프레임 동일 좌표로 반복 재기록되는 것이 원인인지 확인하려고 렌더 루프를 완전히 멈춘 뒤 강제 리페인트한 상태와, 평소 그대로 돌아가는 상태를 헤드리스 Playwright 스크린샷(등배·2배 확대 모두)으로 비교했으나 **육안/자동 비교 모두 유의미한 차이를 찾지 못했다** — 이는 이 종류의 GPU 합성/텍스처 해상도 문제가 헤드리스 크로미움(소프트웨어 렌더링 경로를 쓸 수 있음)에서 실제 사용자의 GPU 가속 브라우저와 다르게 동작해 스크린샷으로는 재현/검증이 원천적으로 안 될 가능성이 있음을 시사한다. **일단 추가로 할 수 있는 조치는 다 반영**: 기존 "정착 시점에 재선명화"(scale이 목표치에 수렴하는 순간)에 더해, 이제 허브와 포커스된 카드에 대해 **700ms마다 주기적으로도** `forceMediaClarity()`를 걸어 정착 감지를 놓치는 경우까지 커버하도록 안전망을 추가했다. **이 항목만은 스크린샷으로 자체 검증이 불가능하다는 한계를 사용자에게 명확히 알릴 것** — 실제 화면에서 여전히 흐리다면 이 안전망도 충분치 않다는 뜻이므로, 그 다음엔 완전히 다른 가설(예: 소스 이미지 자체 해상도, `image-rendering` 값 재검토, 혹은 GPU 레이어 승격 여부를 크롬 DevTools의 레이어 패널로 직접 확인)로 넘어가야 한다.

### 같은 날 다섯 번째 라운드 — "대부분의 카드가 안 움직인다"는 심각한 회귀의 진짜 원인(렌더 루프와 흩어짐 애니메이션의 쓰기 충돌), 발광 위치·꼬리 재조정, 그룹 진입에 이동 과정 추가

- **"대부분의 카드가 안 움직인다" — 이번 세션 최대 규모 회귀, 원인은 "즉시 회전 재개" 기능 자체의 부작용**: 실측(Playwright로 흩어짐 시작 직후와 2.7초 뒤의 화면 좌표 비교) 결과 카드 이동 거리가 1~48px 수준으로 거의 0에 가까웠다(정상이면 500~3000px). 원인 추적 끝에 발견: 몇 라운드 전 "가상공간이 먼저 회전하게 해달라"는 요청으로 `spatialTransitionActive`를 거의 즉시(2프레임, ~32ms) 꺼지도록 바꿨는데, 이게 꺼지자마자 `spatialFrame()`/`networkFrame()`의 자체 렌더 루프가 **매 프레임 `layoutTorus()`/`applyNetworkBillboard()`를 무조건 다시 호출**해 **모든 카드의 위치를 "정상 궤도 좌표"로 되돌려 쓴다** — 이 두 함수는 애초에 "흩어짐 경로 중인 카드"라는 개념 자체를 몰라서, `driveFancyLive`가 매 프레임 공들여 계산한 bounce/orbit/wave/loop 좌표를 렌더 루프가 곧바로 자연 위치로 덮어써버렸다. 회전 재개가 거의 즉시 이루어지도록 바꾼 그 순간부터 이미 잠복해 있던 버그였는데, 그 전까지는 `spatialTransitionActive`가 흩어짐 전체 지속시간 동안 꺼지지 않아서 렌더 루프가 끼어들 틈이 없었을 뿐이다.
  - **수정**: `driveFancyLive`가 자신이 지금 제어 중인 엘리먼트 집합을 `scatterActiveEls`(Set)로 직접 관리(시작 시 add, 완료 시 delete)하도록 하고, `layoutTorus()`/`applyNetworkBillboard()`의 카드별 쓰기 루프에 `scatterActiveEls.has(card)`면 건너뛰는 가드를 추가 — 렌더 루프는 이제 "지금 흩어짐 애니메이션 중이 아닌" 카드만 정상적으로 계속 갱신하고, 흩어짐 중인 카드는 전적으로 `driveFancyLive`에게 맡긴다.
  - **부수 효과**: `driveFancyLive`가 매 프레임 목적지를 알아내려고 `layoutTorus()`/`applyNetworkBillboard()`(장면 전체를 다시 계산하는 무거운 함수) **전체를 통째로 호출**하던 것도 함께 정리 — 대신 카드 한 장/노드 한 개의 "정상 위치"만 계산하는 순수 함수 `computeTorusCardNatural(card)`/`computeNodeNatural(p)`를 새로 만들어 `getLiveNatural`이 이걸 쓰게 했다(부작용 없음, 다른 카드에 영향 안 줌, 성능도 더 가벼움). Playwright로 재검증: 토러스·네트워크 양방향 모두 카드 이동 거리 150~3000px대로 정상 회복, 4회 연속 왕복 후 카드 8/8·좀비 애니메이션 0·`scatterActiveEls` 잔여 0 확인. **교훈: 렌더 루프를 "즉시 재개"하도록 만들 때는 그 루프가 개별 애니메이션이 진행 중인 엘리먼트를 알아서 피해가는지 반드시 같이 확인해야 한다 — 이번엔 몰랐다가 사용자가 실사용 중 발견했다.**
- **그리드(연결선)가 여전히 따로 논다는 재확인 요청**: 지난 라운드에 넣은 `syncEdgeToPosition`/`onFrame` 실시간 추적이 실제로 반영됐는지 스크린샷 시퀀스로 재확인 — 연결선이 각 카드의 실제 이동 경로를 계속 따라가는 것을 확인했다(위 "카드가 안 움직인다" 버그가 있는 동안에는 카드 자체가 거의 안 움직였으니 "그리드가 카드를 못 따라간다"는 것도 체감상 구분이 어려웠을 수 있음 — 이번 라운드의 렌더 루프 충돌 수정으로 카드가 다시 정상적으로 움직이면서 그리드 추적 결과도 같이 재검증됨).
- **네트워크 중앙 카드 흐림 — "포커싱되면 살짝 내려앉으면서 또렷해진다"는 더 구체적인 재보고**: 기존 hover box-shadow 규칙만으로는 이 "내려앉는" 느낌을 설명할 CSS를 찾지 못했다(재검색해도 hover 시 transform/scale을 바꾸는 규칙은 없음, box-shadow만 존재) — 근본 원인 규명에는 실패했지만, 실용적 대응으로 `networkShell`에 `pointerover` 리스너를 추가해 `.network-node`에 마우스가 올라갈 때마다 명시적으로 `forceMediaClarity()`를 걸도록 했다(지난 라운드의 700ms 주기 재선명화에 더해 순수 추가). 근본 원인 불명은 여전히 남은 과제.
- **발광 머리 위치·꼬리 형태 재조정**: (1) "카드 가운데서 원을 그린다"는 지적에 궤도 반지름을 44%→48%로 키워 실제 테두리에 더 가깝게 붙도록 조정(45%대 이상은 원 자체 반지름 때문에 카드 밖으로 너무 빠져나가 다른 문제가 생길 수 있어 48%가 현실적 한계로 판단). (2) "꼬리가 짧은 개별 점 2~3개로 보인다, 원래처럼 길게 늘어지는 형태로 해달라"는 지적에 따라 트레일 원의 개수를 3개(18°/36°/54° 간격, 큰 간격)에서 8개(5°~54° 사이 촘촘한 간격, 크기·투명도가 매끄럽게 줄어드는 곡선)로 늘려 개별 점이 아니라 하나로 이어진 듯한 스미어(smear) 형태로 보이도록 재조정. 헤드/기존 4겹 drop-shadow 번짐 로직은 그대로 유지.
- **"외곽 그룹 카드 선택 시 갑자기 나타난다" — 카메라 왕복 제거가 만든 새로운 규칙 위반**: 지난 라운드에 카메라 왕복(부작용)은 없앴지만 그 왕복이 유일하게 제공하던 "선택한 카드가 이동하는 것처럼 보이는" 시각적 단서까지 같이 사라져 버려, "미리 자리잡고 자식을 펼치는 것"이 마치 순간이동처럼 보였다 — "모든 움직임에는 과정이 보여야 한다"는 이 세션의 확립된 규칙을 위반. **수정**: 카메라 자체를 옮기는 대신, 선택된 배경 카드의 스냅샷 유령(ghost) 하나만 `position:fixed`로 복제해 그 카드의 실제 화면 위치에서 화면 정중앙까지 CSS 트랜지션으로 날아가도록 하는 `ghostRingCardToCenter(node,duration)`를 새로 추가(기존 `zoomCardIntoModal`의 유령-오버레이 기법과 동일한 패턴 재사용) — 카메라나 다른 카드들의 위치는 전혀 안 건드리고, 선택된 카드 하나만 시각적으로 중심을 향해 날아가는 것처럼 보이게 한다. Playwright로 유령 엘리먼트가 실제로 생성되고 위치 값이 프레임마다 변하는(트랜지션 진행 중) 것을 확인.

### 같은 날 여섯 번째 라운드 — 유령 전환의 "잔상" 버그 수정, 발광 헤드를 검증된 방식으로 되돌림, 그리드 소멸 버그는 재현 실패

- **"외곽 그룹 카드 선택 시 반대편으로 갔다 오는 잔상이 남는다" — `ghostRingCardToCenter` 자체의 버그**: 방금 추가한 유령 이동 효과가 실제로는 두 가지를 놓치고 있었다. (1) **원본 카드 엘리먼트를 안 숨겼다** — 유령(복제본)만 날아가고, 진짜 선택된 카드 버튼은 원래 자리에 그대로 완전 불투명하게 남아있다가 `pushNetworkLevel()`이 나중에 통째로 지울 때에야 사라졌다 — 이 "그대로 남아있는 원본"이 사용자가 말한 "잔상"이었다. `zoomCardIntoModal`도 유령을 만들 때 원본에 `opacity='0'`을 거는데, 이번에 새로 만든 함수에서는 그 한 줄을 빠뜨렸다. (2) **유령의 목적지 계산이 틀렸다** — 뷰포트 전체 중앙(`innerWidth/2`)으로 이동시켰는데, `.network-shell`은 실제로 화면 오른쪽에 치우쳐 배치돼 있어서(`left:42%`) 유령이 실제 허브가 나타날 자리와 다른, 화면 더 왼쪽의 엉뚱한 지점으로 날아갔다 — 이게 "반대편으로 갔다"는 인상을 준 원인. 또한 목적지 크기도 `rect.width*1.15`라는 임의 배율이라 실제 허브 크기와 안 맞았다. **수정**: 원본 엘리먼트에 `opacity='0'` 추가, 목적지 좌표를 `networkShell.getBoundingClientRect()`의 실제 중심으로 변경, 목적지 크기를 `NETWORK_HUB_SCALE / 선택 당시의 실제 표시 배율`로 정확히 계산(임의 배율 대신). **여기서 또 하나의 버그**: `node.displayScale`을 유령 함수 호출 *전에* 이미 `NETWORK_HUB_SCALE`로 덮어쓰고 있어서, 유령 함수 안에서 "선택 당시 배율"을 읽으려 하면 이미 바뀐 값(결국 배율 1:1)을 읽게 되는 순서 버그가 있었다 — `ghostRingCardToCenter()` 호출을 `node.displayScale=NETWORK_HUB_SCALE` 대입보다 먼저 하도록 순서를 바꿔 해결. Playwright로 (a) 원본 엘리먼트의 인라인 opacity가 호출 즉시 '0'으로 설정됨, (b) 유령이 셸의 실제 중심을 향해 이동함을 확인.
- **발광 헤드 — 사용자의 명시적 경고("도저히 안 되면 원래대로 돌린다") 이후 검증된 방식으로 복귀**: 트리곤노메트리 기반 "전구" 재설계(타원 궤도 + 개별 원 여러 개로 꼬리 흉내)는 사용자가 "카드 내부에서 원을 그린다", "꼬리가 개별 점 여러 개로 보인다"고 재차 지적했고, 결국 "테두리 모서리를 정확히 따라가야 하고, 꼬리는 긴 실선이어야 한다"는 요구는 **처음부터 conic-gradient+mask 방식만이 구조적으로 만족시킬 수 있는 요구**라는 결론에 도달했다(원형 gradient를 좌표로 옮기는 방식은 본질적으로 타원 근사이지 진짜 모서리 추적이 아니고, 여러 개의 원으로는 "긴 실선"을 절대 흉내낼 수 없다 — 각각이 결국 뚜렷한 원으로 보인다). **이번 라운드에서 conic-gradient+mask 방식으로 전면 복귀**(오늘 초반 "발광 머리 재재설계" 항목에서 검증했던 정확히 그 배경/마스크 값) — 이 방식은 카드의 실제 padding-box 테두리에 마스킹되므로 모서리를 포함해 정확히 테두리를 따라가고, 끊김 없는 conic-gradient 자체가 곧 "긴 실선" 형태의 꼬리다. **명확한 한계 인지**: 이 방식에서 "머리"는 태생적으로 테두리를 따라 늘어진 밝은 호(arc)이지, 완벽하게 동그란 점은 아니다 — "전구처럼 동그랗게"와 "테두리를 정확히 따라가는 긴 실선 꼬리"는 이 하나의 기법 안에서 동시에 완벽히 만족시킬 수 없는 두 요구라는 점을 사용자에게 명확히 전달할 것. Playwright 스크린샷으로 카드 모서리를 정확히 따라 도는 끊김 없는 파란 호와 부드러운 실선 꼬리를 확인, 카드 밖으로의 번짐(지난지난 라운드 overflow:hidden 제거 효과)도 여전히 살아있음을 확인.
- **"네트워크 공간에서 흩어질 때 카드가 사라져도 그리드는 남아있다" — 재현 실패**: 단순 왕복(4회), 빠른 연속 클릭으로 전환 중간에 끼어들기(간섭) 등 여러 시나리오로 Playwright 검증했으나 **매번 정상적으로 `.network-shell`이 `display:none`이 되고 모든 `.network-edge`의 opacity가 0으로 확인돼, 이 버그를 재현하지 못했다**. `.spatial-overlap` 클래스 제거 → `[data-spatial-mode]` 불일치 → 기본 `display:none` 규칙으로 복귀하는 CSS 체인 자체는 논리적으로 문제가 없어 보인다. **재현 실패를 사용자에게 솔직히 알릴 것** — 실제로 재발하면 정확히 어떤 조작(드래그 중 전환, 특정 레벨에서 전환 등) 직후에 나타나는지 구체적 재현 절차를 물어봐야 다음 진단이 가능하다.

### 같은 날 일곱 번째 라운드 — 발광 전구·꼬리를 "하나의 기법"이 아닌 "독립된 두 레이어"로 분리해 동시에 해결

바로 앞 라운드에서 "동그란 전구"와 "테두리를 정확히 따라가는 긴 실선 꼬리"가 conic-gradient+mask 하나로는 동시에 불가능하다고 결론짓고 후자를 택했는데, 사용자가 "각각 따로 구현해서 동시에 움직이면 되는 거 아니냐"고 정확한 대안을 제시했다 — 맞는 지적이었다. 문제는 기법이 아니라 **가용한 슬롯이 부족했던 것**: `:before`(꼬리용 conic-gradient)와 `:after`(카드 하단의 라벨 텍스트, `content:attr(data-label)`)가 이미 둘 다 점유돼 있어 CSS 생성 콘텐츠 pseudo-element가 하나 더 필요했다.

**해결**: 굳이 `:after`(라벨)를 건드려 셋째 슬롯을 억지로 만들 필요 없이, **진짜 DOM 자식 엘리먼트**를 하나 추가하는 쪽을 택했다(라벨 시스템은 여러 상태별 스타일이 얽혀 있어 손대면 회귀 위험이 컸다 — 완전히 별개의 새 엘리먼트를 추가하는 게 기존 것을 하나도 안 건드리는 가장 안전한 방법). 카드 생성 코드(`torusItems.forEach(...)`)에서 `b.innerHTML=mediaMarkup(...)+'<span class="torus-bulb" aria-hidden="true"></span>'`로 빈 span 하나를 매 카드마다 추가하고, `.torus-card.front .torus-bulb`에 트리곤노메트리 기반 원형 `radial-gradient`(카드 테두리 44~48% 반경 궤도)를 적용 — `:before`의 conic-gradient 꼬리는 그대로 완전히 유지. 두 레이어 모두 **동일한 `torusFrontChase`/`torusChaseFadeIn` 키프레임과 타이밍**(`4.8s linear .55s infinite`)을 각자 독립적으로 재생하는데, `@property --torusFrontAngle{inherits:false}`라 서로 상속 관계는 없지만 **완전히 동일한 애니메이션이 완전히 동일한 순간(`.front` 클래스 부여)에 각자 시작**하므로 자동으로 동기화된다(같은 시계로 같은 시간에 출발하는 두 시계는 서로 참조 없이도 항상 같은 시각을 가리키는 것과 같은 원리) — 별도의 JS 동기화 코드가 전혀 필요 없었다.

Playwright로 (1) `.torus-bulb` 배경이 실제 좌표값으로 정상 계산됨, (2) 스크린샷 시퀀스로 카드 테두리를 정확히 따라 도는 파란 실선 꼬리와 그 머리 부분 근처에 위치한 뚜렷한 흰-파란 원형 전구가 함께, 서로 어긋나지 않고 이동하는 것을 확인, (3) 기존 라벨(`::after`)이 전혀 영향받지 않고 그대로 정상 동작함을 확인, (4) 4회 왕복 후 카드 8/8·좀비 애니메이션 0·`.torus-bulb` 8개(카드 수만큼) 정상 존재. **이 방식이 이번 세션 전체에서 시도한 발광 효과들 중 가장 사용자 요구를 정확히 충족한다** — 별도 지시 없이는 이 구조를 더 바꾸지 말 것.

### 같은 날 여덟 번째 라운드 — "이상 없다"던 검증이 실제로는 틀렸음(궤적 불일치), 진짜 원인과 근본 수정

사용자가 스크린샷 몇 장이 "비슷해 보인다"는 것만으로 검증을 끝낸 걸 정확히 짚었다: "실선은 카드 테두리 사각형을 따라 돌고, 발광전구는 카드 내부에서 원형을 그리면서 돌고 XY 값이 전혀 다른데, 너는 어떻게 똑같다고 한 거야?"

**진짜 원인**: `:before`의 conic-gradient 꼬리는 사각형(둥근 모서리) 테두리에 마스킹되므로, 각도 θ에서 실제로 빛나는 지점은 "중심에서 각도 θ 방향으로 쏜 광선이 **사각형 변**과 만나는 점"이다 — 이건 변에 따라 직선으로 움직이다가 모서리에서 방향이 꺾이는, 매우 비선형적인 경로다. 반면 지난 라운드에 넣은 전구는 `cos(θ)*48%, sin(θ)*48%`라는 **타원 궤도** 공식을 썼다 — 이건 같은 각도 θ를 쓰지만 완전히 다른 함수다. 두 함수는 카드의 상/하/좌/우 변 중앙(θ=0°/90°/180°/270° 부근)에서만 우연히 비슷해 보이고, 대각선 방향(모서리 쪽)에서는 크게 벌어진다 — 지난 검증 스크린샷들이 우연히 전자의 각도 근처에서 찍혀 "똑같아 보인" 것이었다. 몇 개의 스크린샷이 그럴듯해 보인다고 전체 각도 범위에서 실제로 일치하는지 검증 없이 "이상 없다"고 단정한 게 잘못이었다.

**근본 수정**: 사용자의 정확한 지적("발광 머리는 실선 머리의 위치를 받아서 그 위치에 표시") 그대로 구현 — CSS 계산을 버리고 JS로 전환. 매 프레임(`updateTorusBulbPosition()`, 독립 rAF 루프) `:before`의 실제 살아있는 `--torusFrontAngle` 값을 `getComputedStyle(front,'::before')`로 직접 읽어온 뒤, **체비쇼프 정규화**(`x/max(|x|,|y|), y/max(|x|,|y|)` — 원을 정사각형에 투영하는 표준 공식, 각 축을 독립적으로 퍼센트 처리하면 실제 사각형 모양이 무엇이든 자동으로 정확히 들어맞는다)로 진짜 사각형-교차점을 계산해 `--bulbX`/`--bulbY` CSS 커스텀 프로퍼티에 매 프레임 대입. `.torus-bulb`의 `radial-gradient`는 이제 `at var(--bulbX) var(--bulbY)`로 이 값을 그대로 사용 — 자체 애니메이션(`torusFrontChase`)은 제거(더 이상 스스로 각도를 진행시키지 않고, 꼬리가 진짜로 있는 자리를 매 프레임 그대로 받아쓴다). Playwright로 30프레임 연속 샘플링한 결과, X값이 "2.53%"에 고정된 채 Y값만 움직이다가(왼쪽 변을 따라 이동), 모서리에서 전환돼 Y가 "1.90%"에 고정된 채 X만 움직이는(위쪽 변을 따라 이동) 등 **진짜 사각형 변을 타고 이동하는 값**이 나옴을 확인 — 타원 곡선이 아니라 정확히 꺾이는 직선 구간들. 스크린샷으로도 전구가 정확히 테두리 선 위, 꼬리의 머리 부분에 겹쳐 있음을 확인.

- **부수 확인/수정**: "외곽 그룹카드가 중앙에 도착하기도 전에 하위 카드가 펼쳐진다"는 것도 같은 검증 소홀 계열의 버그였다 — 유령 이동(`ghostRingCardToCenter`)은 `NETWORK_ENTER_MS`(480ms)에 걸쳐 날아가는데, 자식 레벨을 실제로 짓는 `pushNetworkLevel()` 호출은 그보다 훨씬 짧은 `NETWORK_PUSH_DELAY`(168ms)로 예약돼 있어서, 유령이 채 절반도 못 간 시점에 이미 자식들이 나타나고 있었다. `enterFromRing`의 이 특정 호출 지연을 `NETWORK_ENTER_MS`로 변경(다른 곳에서 `NETWORK_PUSH_DELAY`를 쓰는 `enterNetworkNode`는 손대지 않음, 유령 효과가 없는 그쪽은 원래 타이밍이 맞다). `pushNetworkLevel`을 몬키패치해 실제 호출 시각을 측정한 결과 488ms(유령 도착 시점과 거의 정확히 일치)로 확인.

### 같은 날 아홉 번째 라운드 — 자식 생성 타이밍을 "같은 상수 값 맞추기"에서 "실제 도착 이벤트 감지"로 재설계

사용자가 위 수정 방식의 구조적 결함을 정확히 짚었다: "자식 생성을 480ms 그런 식으로 맞추게 되면, 부모 시간을 바꾸면 또 틀어질 거 아니야... 그룹 카드가 도착하면 그때 발동되도록 해줘." — `NETWORK_ENTER_MS`라는 같은 상수를 두 곳(유령 애니메이션 길이, 자식 생성 지연)에서 **중복 참조**하는 방식은 둘 중 하나만 바뀌어도 다시 어긋나는 취약한 결합이었다. 나중에 유령의 이동 속도만 따로 조정하면 이 버그가 똑같이 재발했을 것이다.

**재설계**: `ghostRingCardToCenter(node,duration,onArrive)`가 `onArrive` 콜백을 받도록 확장 — 유령의 실제 CSS 트랜지션이 끝나는 순간(`transitionend` 이벤트, `left` 속성 기준) 콜백을 호출한다(트랜지션이 어떤 이유로 안 끝나는 예외 상황을 대비해 `duration+120ms` 안전망 타이머도 같이 둠, 둘 중 먼저 오는 쪽이 실행되고 나머지는 무시됨). `enterFromRing`은 이제 `pushNetworkLevel(...)`을 짓는 로직 전체를 이 콜백 **안**으로 옮겼다 — 더 이상 "자식은 몇 ms 후에 만들라"는 시간 값을 전혀 참조하지 않고, "유령이 도착하면 그때"라는 이벤트에만 반응한다. 이제 `NETWORK_ENTER_MS`(유령 애니메이션 길이)를 나중에 어떻게 바꾸든, 자식 생성은 항상 자동으로 정확히 그 뒤를 따라간다 — 값을 두 번 맞출 필요가 원천적으로 없어졌다.

Playwright로 독립적인 캡처-단계 `transitionend` 리스너를 별도로 걸어 실측한 결과, 유령의 `left` 트랜지션이 실제로 끝나는 시점(≈600ms, `enterFromRing` 호출부터 유령이 DOM에 붙고 목표값이 걸리기까지의 준비 시간 ~120ms + 선언된 480ms 트랜지션)과 `pushNetworkLevel`이 실제로 호출되는 시점이 정확히 일치함을 확인 — 안전망 타이머가 아니라 진짜 이벤트가 트리거하고 있음을 검증했다.

### 같은 날 열 번째 라운드 — 발광 전구 각도 기준계 버그 발견, 그룹 카드 교체 시 "동시 진행" 재설계

- **발광 전구 위치가 실선 머리와 크게 어긋났던 진짜 원인 — 각도 기준계(좌표계) 자체가 틀렸었다**: 지난 라운드의 "체비쇼프 정규화" 공식 자체는 맞았지만, 여기 넣은 `Math.cos(θ)/Math.sin(θ)`가 **일반 수학 각도 관례**(0°=오른쪽, 반시계 방향 증가)를 썼는데, CSS `conic-gradient`의 각도는 **완전히 다른 관례**(0°=위쪽/12시 방향, 시계 방향 증가)를 쓴다 — 이 둘은 사실상 90도 어긋난 서로 다른 좌표계였다. 화면 좌표로 정확히 변환하려면 `dx=sin(θ), dy=-cos(θ)`를 써야 한다(0°일 때 위쪽 `(0,-1)`을 가리키는지 직접 검산해서 확인). 추가로, 꼬리의 실제 가장 밝은 지점(`#fff` 스톱)은 `--torusFrontAngle` 그 자체가 아니라 그 값에서 약 356° 지난 지점(그라디언트 스톱이 352~360deg 구간에 있으므로)이라는 것도 반영해 `headAngle=(rawAngle+356)%360`으로 보정. 이 두 수정을 합쳐 다시 계산하니 전구가 실선의 실제 밝은 머리 위치에 정확히 겹쳐짐을 스크린샷으로 확인했고, 사용자가 원한 대로 카드 테두리 바깥으로 자연스럽게 삐져나가는 것도 함께 확인됨.
- **외곽 그룹카드로 전환 시 "이전 카드 물러남"과 "새 카드 진입"이 순차적이 아니라 동시에 일어나도록 재설계**: 기존엔 하위 카드들이 그냥 흐려지기만 하고(이동 없음), 이전 허브는 유령이 도착한 뒤에야(즉, 순차적으로) 사라졌다 — 사용자가 원한 "하위 카드가 중앙으로 모이고, 동시에 이전 그룹카드는 물러나고, 동시에 새 그룹카드가 중앙으로 들어온다"는 것과 달랐다. `enterFromRing`을 재작성: 현재 레벨의 직속 자식(`ring===0`)들에게 `backNetworkLevel()`과 동일한 패턴(중앙으로 이동+축소(scale .12)+페이드, 269ms)을 적용해 실제로 "모이는" 움직임을 만들고, 이 애니메이션과 유령의 중앙 이동을 **같은 동기 실행 틱에서 동시에 시작**시켰다. 자식-수렴 애니메이션 완료와 유령 도착, 두 신호가 **둘 다** 끝나야 다음 레벨을 짓도록(`pushNetworkLevel`) 게이트를 걸었다(먼저 끝나는 쪽이 나머지를 기다림). Playwright로 `enterFromRing()` 호출 직후 자식 노드의 인라인 transform이 즉시 `scale(0.12)`·중앙 좌표로 바뀌는 것을 확인해 진짜 동시 시작임을 검증. **미검증으로 남은 부분**: "이전 그룹 카드가 자기 궤적으로 정확히 물러나는" 것 자체는 `pushNetworkLevel`/`renderNetworkDom`에 이미 있던 재사용(`fromHub`) 메커니즘에 맡겼다 — 이 허브 엘리먼트가 새 배경 링 위치로 정확히 이어지는지는 이번엔 픽셀 단위로 검증하지 못했다.

### 같은 날 열한 번째 라운드 — "동시 진행"의 정확한 의미 재정의, `fromHub` 재사용 메커니즘 실제 작동 확인, 자식 수렴을 진짜 순차 2단계로 재설계

사용자가 전체 시나리오를 매우 구체적으로 재정의했다: 이전 중앙 카드의 물러남은 **[자식 거둬들이기] → [자식 숨기기] → [자기 자신이 그룹 궤적으로 이동]** 3단계이고, 새 카드의 진입은 **[궤적→중앙 이동] → [자식 펼치기]** 2단계이며, **이 두 시나리오는 "시작하는 시점"이 동시여야 한다**(각 시나리오 내부는 순차적이어도 됨, 두 시나리오의 첫 스텝끼리만 동시). 또한 중앙이 말단(리프) 카드여도 "해당 그룹이 뭉쳐지고, 그 뭉쳐진 그룹 카드가 물러난다"는 동일 원리가 적용된다.

- **`fromHub` 재사용 메커니즘이 실제로 작동하는지 코드 추적 + 실측 확인**: 이전 라운드에서 "노후화된 인프라에 맡겼다, 검증 못 했다"고 적었던 부분을 이번엔 실제로 파고들었다. `buildNetworkData()`를 정독한 결과, 링 계산 로직(`excludeItem=networkStack[li+1].hubItem`)상 "이전 중앙 카드의 아이템"은 형제 그룹으로 전환하는 시나리오에서 새 장면의 배경 링에 **합법적으로 다시 나타나야 한다**는 것을 이론적으로 확인했고, Playwright로 직접 검증: 전환 후 `networkNodes`에서 이전 허브였던 아이템을 찾아보니 `fromHub:true`, `oldTransform`이 정확히 중앙(허브 스케일)이었고, 실측 200ms 후 `el.style.transform`이 실제로 배경 링 좌표(`translate3d(321px,...) scale(0.7)`)로 이동해 있음을 확인 — **이 재사용/복귀 메커니즘은 이미 정확히 작동하고 있었다.**
- **"자식이 거둬들여지는 과정이 안 보인다"의 진짜 원인 — 애니메이션은 실행 중이었지만 이동과 페이드가 동시에 겹쳐서 눈에 띄지 않았다**: 화면 실좌표(`getBoundingClientRect`)로 자식 카드를 추적한 결과 실제로는 위치·불투명도 모두 매끄럽게 전환되고 있었다(`el.style.transform`이 t=0부터 목표값을 보여주는 건 CSS 트랜지션의 정상적인 특성이지 버그가 아님 — 이 세션에서 이미 여러 번 배운 함정을 스스로 다시 헷갈릴 뻔함). 그런데도 사용자에게 안 보인 이유: 이동(269ms)과 페이드(269ms)가 **완전히 겹쳐서** 진행돼, 카드가 중앙에 도착하기도 전에 이미 반쯤 투명해져 "모이는 움직임"으로 인식되기보다 "그냥 사그라드는 것"처럼 보였다. 사용자가 원한 건 진짜 **순차** 2단계(모으고 → 그 다음에 숨기고)였다.
- **수정**: CSS `transition-delay`만으로 진짜 순차 2단계 구현(추가 JS 체이닝 불필요) — `transition:transform ${moveMs}ms ...,opacity ${fadeMs}ms ease-in ${moveMs}ms` — opacity 트랜지션에 `moveMs`만큼의 지연(delay)을 걸어, 카드가 완전히 불투명한 채로 먼저 중앙까지 이동+축소(`moveMs`≈269ms)를 마치고, **그 다음에야** 빠르게 페이드아웃(`fadeMs`≈120ms)하도록 재설계. 이전 허브 자신의 페이드도 동일한 지연을 걸어 자식들과 같은 타이밍에 함께 사라지도록 맞춤. `closeDone` 판정 시각도 이 늘어난 총 시간(`moveMs+fadeMs`)에 맞춰 조정. Playwright로 시간대별 불투명도를 샘플링해 t=50~250ms 구간은 완전 불투명(이동 중, "거둬들이는" 단계가 눈에 보임) 유지, t=300~400ms 구간에 걸쳐서만 실제로 사라지는(숨기는 단계) 것을 확인 — 사용자가 요청한 정확한 2단계 시퀀스가 이제 실제로 구현됨.

### 같은 날 열두 번째 라운드 — 발광 전구 완전 제거(테두리 실선 꼬리만 유지), 자식 수렴 목표를 "중앙"에서 "그룹 허브의 실제 위치"로 일반화

두 차례의 각도 기준계 수정에도 불구하고 사용자가 "실선과 위치가 안 맞는다, 제대로 구현을 못 하는 것 같다"고 재차 지적 — 이 기법(전구+실선 두 레이어를 서로 다른 곡선으로 동기화)을 더 다듬기보다 **전구 자체를 완전히 폐기**하고 이미 검증된 테두리 실선(conic-gradient+mask 꼬리)만 남기기로 결정.

- **전구 제거**: `.torus-bulb` DOM 요소(카드 생성 시 주입되던 `<span class="torus-bulb">`), 그 CSS 두 규칙(`.torus-bulb`, `.torus-card.front .torus-bulb`), 그리고 매 프레임 전구 좌표를 계산하던 `updateTorusBulbPosition()`과 그 `requestAnimationFrame` 기동 코드를 전부 삭제. `.torus-card.front:before`(실선 꼬리, conic-gradient+mask)는 손대지 않고 그대로 유지 — 이제 이것이 유일한 발광 효과다. Playwright로 `.torus-bulb` 엘리먼트 0개, `updateTorusBulbPosition` 함수 미정의를 확인했고, 토러스 전면 카드 스크린샷에서 테두리를 따라 도는 파란 실선 꼬리만 보이고 별도의 동그란 광점은 남아있지 않음을 확인.
- **"중앙이 말단 카드일 때도 그룹 카드 쪽으로 모여야 한다" — 원인은 `enterFromRing`/`backNetworkLevel`의 자식 수렴 목표가 항상 로컬 원점 `(0,0,0)`으로 하드코딩돼 있었던 것**: `networkMode==='items'` 상태에서 리프 카드 하나가 포커스되면(`focusNetworkNode`) 카메라가 그 리프를 따라가도록 `networkShiftX/Y/Z`가 리프의 위치 쪽으로 이즈(ease)되는데, 이때 실제 그룹 허브(`networkHubEl`)는 `translate3d(-shift...)`만큼 화면 중앙에서 벗어난 곳에 서 있다 — 즉 "로컬 원점"과 "허브가 실제로 보이는 위치"가 서로 다른 지점이 된다. 기존 코드는 리프 형제들을 무조건 로컬 원점(사실상 화면 정중앙)으로 수렴시켜, 사용자 눈에는 "그룹 카드 쪽이 아니라 그냥 중앙으로 모인다"로 보였던 것. 포커스가 없을 때(허브 시프트가 0에 가까울 때)는 두 지점이 사실상 같아서 문제가 드러나지 않았다.
- **수정**: `currentHubTranslate()` 헬퍼를 추가해 `networkHubEl.style.transform`에서 현재 `translate3d(...)` 값을 그대로 읽어오고, `enterFromRing`과 `backNetworkLevel` 양쪽의 자식 수렴 목표를 하드코딩된 `'translate3d(0px,0px,0px)'` 대신 `` `translate3d(${hubTranslate})` ``로 교체 — 포커스가 없을 때는 이 값이 사실상 `(0,0,0)`이라 기존 동작과 동일하고(회귀 없음, Playwright로 실측: 미포커스 상태에서 hubTranslate가 `-3.85px, 0.04px, 0.32px`로 원점에 근접함을 확인), 리프가 포커스된 상태에서는 허브의 실제 시프트된 위치로 정확히 수렴한다(Playwright로 실측: 리프 포커스 후 허브 위치가 `-154.8px, -2.8px, 13.5px`로 크게 이동해 있었고, 그 직후 `enterFromRing`이 설정한 형제 카드들의 수렴 목표 transform이 정확히 그 좌표와 일치함을 확인). 허브 카드 자신이 "물러난 뒤 그룹 궤적으로 복귀"하는 부분은 열한 번째 라운드에서 이미 확인한 `fromHub` 재사용 메커니즘이 그대로 처리하므로 추가 수정 불필요. `backNetworkLevel`에도 동일한 하드코딩 패턴이 있어 같은 시나리오(리프 포커스 중 뒤로가기)에서 동일 버그가 재현될 것으로 판단해 동일 수정을 적용.

### 같은 날 열세 번째 라운드 — 데이터 월 제목 문구 정리, 포트폴리오 분류명 이미지 오버레이로 이동, 그룹 카드 교체 시 진입 속도를 상황에 따라 다르게

- **데이터 월 제목**: `<h2>데이터 월에서</h2>` → `<h2>데이터 월</h2>`. 단순 텍스트 수정.
- **포트폴리오 카드 분류명 위치 이동**: 기존엔 `.work-kicker`(분류명)가 `.work-info`(이미지 아래 영역) 안에 제목·설명과 함께 있었는데, "이미지 아래의 분류명을 이미지 내부 오른쪽 하단으로 옮겨달라"는 요청에 따라 마크업을 `<span class="work-media">${이미지/영상}<span class="work-kicker">...</span></span><span class="work-info"><h3>...</h3><p>...</p></span>`로 재구성 — 분류명을 이미지를 감싸는 새 `.work-media`(`position:relative`) 안으로 옮기고 `position:absolute;right:10px;bottom:10px`인 반투명 알약형 배지로 스타일링. **주의해서 처리한 부분**: `.work-kicker` 클래스가 원본 보기 모달(`#modalCategory`)에서도 그대로 재사용되고 있어서, 절대 위치 스타일을 `.work-kicker`에 직접 걸면 모달 쪽 분류명까지 깨질 뻔했다 — 새 오버레이 스타일은 `.work-media .work-kicker`라는 후손 선택자로 범위를 좁혀 포트폴리오 카드 안에서만 적용되게 하고, 모달의 `.work-kicker`(일반 텍스트 스타일)는 전혀 손대지 않았다. 부수 효과: 분류명이 더 이상 `.work-info`의 자식이 아니므로, 4열(가장 좁은) 모드에서 `.work-info` 전체를 숨기는 기존 규칙(`#portfolioGrid[data-cols="4"] .work-info{display:none}`)의 영향을 받지 않게 되어 4열 모드에서도 분류명 배지가 계속 보인다 — 이미지 위에 얹는 취지에 맞는 자연스러운 결과로 판단해 그대로 두었다. Playwright로 배지가 실제로 이미지 영역 내부 우하단에 있는지(`position:absolute`, 이미지 박스 안쪽), 모달의 분류명은 여전히 `position:static`으로 정상 동작하는지 확인.
- **그룹 카드 교체 진입 속도 — 중앙에 이미 펼쳐진 그룹이 있을 때만 살짝 느리게**: "선택된 외곽 그룹 카드가 중앙에 조금 천천히 도착해야, 물러나는 중앙 그룹의 시나리오도 온전히 보인다. 중앙에 펼쳐진 그룹이 없으면 정상 속도로." `enterFromRing`(배경 링의 다른 그룹 카드를 선택해 중앙과 교체하는 함수)에 `hasCenteredGroup=!!(networkHubEl&&networkHub)` 판정을 추가하고, 새로 만든 상수 `NETWORK_RING_SWAP_SLOW_FACTOR=1.35`를 곱해 유령(ghost) 카드의 중앙 도착 시간만 `NETWORK_ENTER_MS`(480ms) 대신 `Math.round(NETWORK_ENTER_MS*1.35)`(648ms)로 늘렸다 — 중앙에서 물러나는 기존 그룹의 자식 수렴/숨김(`moveMs`/`fadeMs`, 총 ~389ms)은 원래 속도 그대로 두어, 새 카드가 조금 더 늦게 도착하는 동안 물러나는 쪽 시나리오가 먼저 차분히 끝나는 여유가 생기도록 했다. `enterFromRing`은 구조상 배경 링(ring>0) 카드가 존재할 때만 호출되므로 사실상 항상 허브가 존재하지만, "중앙에 그룹이 없으면 정상 속도"라는 명시적 요구를 그대로 반영해 방어적으로 조건 분기를 넣었다. Playwright로 확인: 허브가 있는 정상 시나리오에서 유령 카드의 트랜지션이 정확히 `648ms`, 허브를 인위적으로 비운 상태에서는 `480ms`로 정상 속도로 되돌아감을 확인.

### 같은 날 열네 번째 라운드 — 그룹 교체 속도 2배로 재조정, 포트폴리오 분류명 배지 제거(색상 텍스트로 환원), 데이터 월 분류명도 우하단으로 통일

- **그룹 카드 교체 속도 1.35배 → 2배**: "여전히 겹쳐서 알 수 없다"는 재지적에 따라 `NETWORK_RING_SWAP_SLOW_FACTOR`를 `1.35`→`2`로 상향(유령 카드 중앙 도착 480ms→960ms). 다른 로직은 열세 번째 라운드와 동일, 상수 값만 조정. Playwright로 유령 카드 트랜지션이 정확히 `960ms`임을 확인.
- **포트폴리오 분류명 배지 제거, 기존 색상 텍스트 스타일로 환원**: 지난 라운드에 추가한 반투명 배경 알약형 배지(`background:rgba(18,14,23,.72);backdrop-filter:blur(6px);color:#fff`)가 "너무 도드라져 보여서 보기 싫다"는 지적을 받아 제거 — `.work-media .work-kicker`에는 이제 `position:absolute;right:10px;bottom:10px;z-index:2`만 남기고, 배경/블러/패딩/둥근모서리/색상 오버라이드를 전부 삭제해 기존 `.work-kicker` 기본 스타일(`color:var(--dark)`, 배경 없는 굵은 색상 텍스트)을 그대로 물려받도록 했다. 위치(이미지 내부 우하단)는 유지, 장식만 제거.
- **데이터 월(아카이브 월) 분류명도 우하단으로 이동, 위치 일관성 확보**: "데이터 월에서도 분류명을 오른쪽 하단으로 넣어달라"는 요청에 `.archive-thumb span{left:6px...}`을 `right:6px`로 변경했는데, 처음엔 반영이 안 됐다 — **원인**: 같은 스타일시트 뒤쪽에 더 높은 특이도(specificity)를 가진 중복 규칙 `.archive-thumb>span:not(.video-frame){position:absolute;left:6px;...}`(`:not()`이 클래스 취급되어 특이도가 더 높음)가 별도로 존재해, 소스 순서와 무관하게 이 규칙이 항상 이겼다. 게다가 `left`와 `right`가 동시에 지정되면 절대위치 요소가 두 값 사이를 채우도록 폭이 늘어나 버려(`width:auto`가 stretch로 바뀜), 라벨이 "우측 정렬"이 아니라 "카드 거의 전체 폭으로 늘어난 뒤 텍스트만 왼쪽에 렌더링"되는 상태가 됐다 — 스크린샷과 실측(`getBoundingClientRect`) 대조로 발견. 두 규칙 모두 `right:6px`로 통일해서 해결. **교훈**: 같은 선택자 패턴이 스타일시트 여러 곳에 중복 정의돼 있을 수 있으니, 위치류 속성을 수정할 때는 첫 번째로 찾은 규칙만 고치고 끝내지 말고 파일 전체에서 동일 속성값(`left:6px` 등)을 재검색해 중복 규칙이 없는지 확인할 것.

### 같은 날 열다섯 번째 라운드 — 선택된 새 그룹 카드의 이동 시작 시점을 "동시 시작"에서 "중앙 그룹이 물러나려는 시점"으로 재조정

지난 라운드까지는 유령(ghost) 카드의 이동 시간 자체를 늘렸지만(2배), "동시에 시작"이라는 기존 설계(아홉/열 번째 라운드에서 확정)는 그대로였다 — 두 애니메이션이 처음부터 끝까지 겹쳐 있어서 아무리 느리게 해도 "물러나는 중앙 그룹"과 "들어오는 새 카드"가 구분되어 보이지 않았다. 사용자: "선택된 새 그룹 카드가 움직이는 시점은 중앙의 그룹이 모이고 나서 뒤로 빠지려고 할 때, 그때 움직이게 해줘. 한 템포 늦게 시작하는 거야. 중앙에 카드가 없을 때는 관계없이 바로 시작해줘." — 즉 새 카드의 시작 시점 자체를 늦춰서, 중앙 그룹의 "자식 모으기" 단계가 끝나고 "물러나기(숨기기)" 단계로 넘어가려는 순간에 새 카드가 움직이기 시작하도록 재설계.

**수정**: `enterFromRing`에서 유령 이동을 호출하는 부분을 `startGhost` 함수로 감싸고, `hasCenteredGroup`이 참이면 `setTimeout(startGhost,moveMs)`로 자식 모으기 소요 시간(`moveMs`≈269ms)만큼 지연시킨 뒤 호출, 거짓(중앙에 카드 없음)이면 그대로 즉시 호출하도록 분기했다. **주의해서 처리한 부분**: `node.displayScale=NETWORK_HUB_SCALE`(유령의 확대 비율 계산에 쓰이는 "이전 스케일" 값을 덮어쓰는 코드)을 `startGhost` 함수 안에서도 반드시 `ghostRingCardToCenter` 호출 **다음**에 실행되도록 순서를 유지했다 — 처음 편집할 때 실수로 순서를 뒤집어서(덮어쓰기를 먼저 실행) `ghostRingCardToCenter`가 이미 덮어써진 스케일을 읽어 확대 비율이 무조건 1로 계산되는, 지난 라운드에 이미 한 번 고쳤던 것과 동일한 버그를 다시 만들 뻔했다 — 실제로 저장하기 전에 코드를 다시 읽다가 발견해서 바로 잡았다. Playwright로 확인: 중앙에 그룹이 있는 경우 유령 카드가 50ms 시점엔 존재하지 않다가 moveMs 직후(약 280~430ms)부터 생성되고 여전히 지난 라운드의 2배 속도(960ms)로 이동함, 중앙에 그룹이 없는 경우(허브를 인위적으로 비운 상태)엔 30ms 이내에 즉시 생성됨, 확대 비율도 여전히 정상적으로 1보다 큰 값(예: 시작 폭 99px → 목표 폭 259px, 원래 스케일 1.55 기준)으로 계산됨을 확인.

**같은 라운드, 곧바로 이어진 되돌림 요청**: "선택된 카드가 중앙으로 갈 때의 속도는 느리게 했었는데, 원래 속도로 해줘." — 시작 시점을 늦추는 위 수정으로 이미 "물러나는 시나리오를 온전히 본다"는 목적이 달성됐으니, 별도로 넣었던 이동 속도 자체의 2배 슬로우다운은 더 이상 필요 없다고 판단한 요청. `ghostMs`를 `hasCenteredGroup?NETWORK_ENTER_MS*NETWORK_RING_SWAP_SLOW_FACTOR:NETWORK_ENTER_MS`에서 항상 `NETWORK_ENTER_MS`(원래 속도, 480ms)로 되돌리고, 더 이상 쓰이지 않게 된 `NETWORK_RING_SWAP_SLOW_FACTOR` 상수 선언도 함께 제거했다(죽은 코드 방지). 시작 지연(`moveMs`만큼 늦게 시작)은 그대로 유지. Playwright로 확인: 유령 카드가 여전히 50ms 시점엔 없다가(지연 유지) 실제 생성 후 트랜지션 시간은 정확히 `480ms`(정상 속도로 복귀).

### 같은 날 열일곱 번째 라운드 — 물러나는 중앙 그룹 카드가 실제로 "작아지며 자기 궤도로 물러나는" 과정을 처음으로 시각화, 그 과정에서 발견한 새로운 렌더 루프 충돌 버그

사용자: "물러나는 그룹 카드가 그룹 궤도로 물러나는 과정이 보이지 않는다. 수정해줘. 작아지면서 본 궤도로 물러나야 한다." 지금까지 몇 라운드 동안 "허브(중앙 그룹 카드) 자신이 그룹 궤적으로 복귀"하는 부분은 전적으로 `pushNetworkLevel`의 `fromHub` 재사용 메커니즘(예전 허브 아이템을 새 배경 링 노드로 다시 그려서 옛 위치→새 위치로 트랜지션)에 맡겨져 있었는데, 이 메커니즘은 **`proceed()`가 실제로 호출된 뒤에야, 즉 유령 카드가 완전히 도착한 뒤에야** 발동한다 — 지난 라운드에 유령 카드의 시작 시점 자체를 `moveMs`만큼 늦추면서 이 발동 시점이 한층 더 늦춰져, "허브가 사라진 뒤 한참 동안 화면에 아무것도 없다가 뒤늦게 어디선가 다시 나타나는" 것처럼 보이는 상태였다.

**수정**: 허브 자신에게 별도의, 즉시 눈에 보이는 "축소 + 이동" 애니메이션을 새로 부여했다 — 자식들이 다 모이고 숨겨지는 시점(`closeMs`, 기존 자식 수렴+숨김 완료 시점)에 맞춰, 허브를 (a) 선택된 카드가 원래 있던 위치(`node.el`의 현재 실제 위치, 살아있는 값을 그 순간에 다시 읽음)로 이동시키고, (b) 선택된 카드의 원래(선택되기 전) 스케일(`originalNodeScale`, `enterFromRing` 시작 시점에 미리 캡처해둠 — 이 값을 나중에 다시 읽으면 이미 `NETWORK_HUB_SCALE`로 덮어써진 뒤라 반드시 시작 시점에 캡처해야 한다)로 축소시키는 CSS 트랜지션을 걸고, 완전히 불투명한 상태로 `retreatMoveMs`(240ms) 동안 이동+축소를 마친 뒤 `retreatFadeMs`(96ms) 동안만 빠르게 페이드아웃하도록 했다 — 지난 라운드들에서 이미 배운 "이동과 페이드가 겹치면 움직임이 안 보인다" 교훈을 여기에도 그대로 적용(`transition-delay`로 순차화). `closeDone`(전체 물러나기 완료 판정)도 이 새 단계까지 포함하도록 연장했고, 중앙에 그룹이 없는 경우(`hasCenteredGroup`가 거짓)엔 이 단계 자체가 없으므로 `closeDone`은 원래 시점(`closeMs+20`) 그대로 유지해 불필요한 지연이 생기지 않게 했다.

**작업 중 새로 발견한 렌더 루프 충돌 버그(이번 라운드에 넣은 코드 자체의 버그)**: 위 수정을 처음 넣고 Playwright로 확인했더니 허브의 transform이 전혀 바뀌지 않고 계속 중앙 근처에 그대로 있었다 — 원인은 `applyNetworkBillboard()`에 `if(networkHubEl)networkHubEl.style.transform=...`라는, **아무 조건 없이 매 프레임 허브의 transform을 무조건 카메라 기준 중앙 좌표로 다시 써버리는 줄**이 있었던 것 — 이번 세션 초반에 카드/노드별로 이미 고쳤던 "렌더 루프가 매 프레임 개별 애니메이션을 덮어쓴다"는 버그와 정확히 같은 부류인데, 그때는 `scatterActiveEls`로 카드별 쓰기를 막았지만 **허브 자신의 transform을 쓰는 이 한 줄은 그 가드에서 빠져 있었다**(애초에 허브를 이런 식으로 애니메이션시킬 일이 없었으니 발견되지 않았던 사각지대). `applyNetworkBillboard()`의 그 줄에 `&&!scatterActiveEls.has(networkHubEl)` 조건을 추가하고, `enterFromRing`의 새 은퇴 애니메이션이 시작될 때 그 순간의 허브 엘리먼트를 `scatterActiveEls`에 등록했다가 애니메이션이 끝나면 제거하도록 했다(엘리먼트 참조를 지역 변수로 미리 캡처해서, 그 사이 `networkHubEl`이 새 허브로 교체되어도 엉뚱한 엘리먼트를 지우지 않도록 방어). Playwright로 실측: 수정 전에는 550ms 시점에도 허브가 scale 1.85·중앙 근처에 그대로 있었지만, 수정 후에는 정확히 같은 시점에 scale 0.7(선택된 카드의 원래 스케일과 일치)·화면 우측 바깥쪽 위치로 옮겨가 있으면서도 여전히 불투명(opacity 1)함을 확인 — "작아지면서, 완전히 보이는 채로 물러나는" 요청 그대로 구현됨. 스크린샷으로도 중앙의 큰 카드 옆에 축소된 카드가 별도로 떠 있는 중간 상태를 직접 확인. 4회 연속 왕복 스트레스 테스트로 `scatterActiveEls` 누수 없음(항상 0으로 정리됨), 콘솔 에러 없음, 매번 정상적으로 정착함을 확인.

### 같은 날 열여덟 번째 라운드 — 물러나는 유령 카드의 도착지와 실제 배경 카드가 나타나는 위치를 일치시켜 "툭 튐" 제거

사용자: "물러나는 유령 그룹 카드의 도착지와 실제 그룹 카드가 나타나는 위치가 다르다. 통일해줘. 자연스럽게 움직이게 해줘. 툭 튀거나 하면 안 돼." 지난 라운드에 넣은 허브 은퇴 애니메이션은 목적지로 "방금 선택한 카드가 있던 자리"를 대충 가져다 썼는데, 이 자리는 그 아이템이 나중에 실제로 정착하게 될 배경 링 좌표(`buildNetworkData()`가 계산하는 진짜 궤도 위치)와는 전혀 무관한 값이었다 — 그래서 은퇴 애니메이션이 끝나는 지점(A)과, 나중에 `pushNetworkLevel`의 `fromHub` 재사용 메커니즘이 실제로 만들어내는 배경 카드의 진짜 도착 지점(B)이 서로 달라서, 두 애니메이션 사이에 눈에 보이는 불연속(팝)이 생겼다.

**핵심 통찰**: `enterFromRing`에서 선택한 배경 카드의 `targetRing`이 정확히 `1`일 때만(바로 인접한 형제 그룹을 선택한 경우) 기존 중앙 허브 아이템이 새 장면에서 다시 등장하며, 이때 **반드시 새 장면의 ring=1 자리에 정착한다**는 사실을 스택 인덱스 수식으로 역산해 증명했다(`li_selected=old_total-1-targetRing`이 허브 자신의 원래 부모 레벨 인덱스인 `old_total-2`와 같아지는 조건이 정확히 `targetRing=1`). 게다가 `buildNetworkData()`가 `fromHub` 아이템에는 항상 `orbitAngle=0,tilt=0`을 고정 배정하기 때문에(무작위가 아님), 그 최종 좌표는 사실상 결정론적으로 미리 계산 가능하다는 것도 확인했다(`targetRing>1`인 경우는 반대로, 예전 허브 아이템이 새 스택 범위 밖으로 잘려나가 아예 다시 나타나지 않는다는 것도 함께 증명 — 이 경우는 애초에 "맞춰야 할 실제 도착점" 자체가 없음).

**수정**: `predictRingRestState(ring)` 헬퍼를 새로 만들어 `buildNetworkData`/`advanceNetworkOrbits`/`applyNetworkBillboard`와 **완전히 동일한 궤도-투영 공식**(반지름 `.5*(1+ring*NETWORK_RING_GAP)`, 현재 카메라 시프트·요·피치 반영)으로 "이 아이템이 ring=1에 정착했을 때의 정확한 화면 좌표"를 미리 계산 — `enterFromRing`의 은퇴 애니메이션이 `targetRing===1`일 때는 이 예측값을 목적지로 쓰고, `targetRing>1`(다시 나타나지 않는 경우)일 때는 정밀도가 필요 없으므로 그냥 현재 위치에서 Z축으로 살짝 밀려나며 축소되는 단순한 대체 동작을 쓴다. Playwright로 예측값과 실제 최종 정착 좌표를 직접 대조: 예측 `(356.3, -0.02, -0.06)` vs 실제 최종 `(357.5, -0.001, -16.7)`, scale은 `0.7`로 정확히 일치 — X·Y는 1px 이내로 거의 완벽히 맞고 Z만 약 17px 정도 차이 나는데(무시한 `sub`/랜덤 지터 성분 때문), 이 정도는 화면상 거의 지각되지 않는 수준이라 "툭 튀는" 느낌이 사라졌다. 핸드오프 전후(약 700ms/950ms/1550ms) 스크린샷으로도 우측 카드의 위치가 시간에 따라 자연스럽게 이어지고 순간이동하듯 튀지 않음을 직접 확인. `targetRing>1` 대체 경로는 현재 실제 사이트 데이터(모든 최상위 그룹이 `direct:true`라 3단계 스택 자체가 생기지 않음)로는 실사용 테스트가 불가능해 코드 리뷰로만 안전성을 확인했다 — 추후 하위 그룹 구조가 추가되면 재검증이 필요하다는 점을 기록해둔다.

### 같은 날 열아홉 번째 라운드 — "두 번 물러난다" 버그(경쟁 조건)와 리프 포커스 상태에서의 예측 오차, 두 가지 실제 버그를 실측으로 확정하고 수정

사용자가 두 가지를 동시에 보고: (1) "그룹이 중앙 중심에 있는 상태에서 외곽 그룹 카드를 선택 시 유령 카드가 한번 물러나고, 다음에 실제 그룹 카드도 다시 물러난다. 두 번 물러난다." (2) "중심에 말단 카드가 존재할 시, 외곽 그룹 카드를 선택하면 말단 카드의 그룹카드가 물러나는 위치가 실제 위치와 달라서 튀는 현상이 나타난다." 이번엔 추측으로 고치지 않고, Playwright로 전체 시퀀스 동안 허브 엘리먼트의 transform을 100ms 간격으로 촘촘히 실측해 정확한 원인을 먼저 확정했다.

- **버그 1(두 번 물러남)의 진짜 원인 — 경쟁 조건(race condition)**: 지난 라운드에 넣은 "은퇴 애니메이션이 끝나면 `scatterActiveEls`에서 제거"하는 정리용 `setTimeout`이 **고정된 추정 시간**(`retreatMoveMs+retreatFadeMs+40`)만 기다렸는데, 실측해보니 `pushNetworkLevel`이 실제로 실행되는 시점(유령 카드의 도착이 `transitionend`가 아니라 안전장치용 타임아웃으로 늦게 발동하는 경우 등)이 이 추정 시간보다 늦어질 수 있었다. 그 사이(경쟁 조건이 실제로 발생하는 구간)에는: 은퇴 애니메이션은 이미 끝나 `scatterActiveEls`에서 제외됐는데, `pushNetworkLevel`은 아직 실행 전이라 허브 엘리먼트(`networkHubEl`)가 여전히 옛날 그 엘리먼트를 가리키고 있다 — 이 타이밍에 `applyNetworkBillboard()`의 매 프레임 무조건 실행 줄이 **더 이상 제외되지 않은** 이 허브를 다시 화면 중앙·원래 스케일(1.85)로 덮어써버려, 이미 물러났던 카드가 순간적으로 중앙으로 되돌아갔다가(스크린샷/실측으로 t=800ms에 확인) 그 직후 진짜 `fromHub` 엘리먼트가 나타나며 또 한 번 물러나는 것처럼 보였다 — 이게 "두 번 물러난다"의 정체였다. **수정**: 추측 시간에 기반한 정리용 타이머를 완전히 제거했다 — `pushNetworkLevel`이 실행되면 `networkHubEl`(모듈 전역 변수) 자체가 새 허브로 교체되므로, `applyNetworkBillboard()`의 가드는 더 이상 옛 엘리먼트를 건드리지 않게 되고, 옛 엘리먼트는 곧 DOM에서 완전히 제거되므로 `scatterActiveEls`에 영원히 남아있어도(죽은 참조 하나) 실질적으로 아무 문제가 없다 — 정리를 아예 안 하는 게 경쟁 조건 자체를 구조적으로 없애는 가장 안전한 방법이었다.
- **버그 2(리프 포커스 시 위치 어긋남)의 진짜 원인 — 카메라 시프트가 아직 다 안 풀린 상태에서 예측함**: 리프 카드가 포커스된 상태(`networkFollowIndex>=0`)에서는 카메라가 그 리프 쪽으로 계속 당겨져(`networkShiftX/Y/Z`) 있는데, `enterFromRing`은 이 값을 리셋하지 않았다 — 그래서 지난 라운드의 예측 함수가 "은퇴 시작 시점(closeMs, 389ms)의 현재 시프트 값"을 그대로 읽어 계산했는데, 실측해보니 이 시프트는 그 시점에 아직 다 풀리지 않은 상태였다(예: 389ms 시점 시프트 x=27.2, 그런데 실제 `fromHub` 요소가 생성되는 900ms 이후 시점엔 x=0.03까지 거의 완전히 풀림) — 두 시점 사이에 시프트가 계속 줄어들고 있었으니 예측값과 실제값이 어긋날 수밖에 없었다. **수정**: `enterFromRing` 시작 시점에 `networkFollowIndex=-1`을 추가해 카메라가 더 이상 리프를 쫓지 않고 즉시 원점(0,0,0)으로 되돌아가기 시작하도록 했고, 예측 함수(`predictRingRestState`) 자체도 "현재 시프트 값을 읽어서 빼는" 방식 대신 **"어차피 나중엔 항상 0으로 수렴한다"는 사실을 활용해 시프트를 아예 0으로 가정**하도록 단순화했다(0.14의 프레임당 감쇠율로 700~900ms면 원래 값의 0.03% 미만만 남으므로 실질적으로 항상 참인 가정). 곁들여 무시하고 있던 `sub`(그룹 내 순번에 따른 반지름 미세 보정, `idx%3*.12`) 항도 이번엔 대충 0으로 넘기지 않고, 다음 장면에서 이 아이템이 실제로 위치할 배열 순번을 미리 정확히 계산해 반영했다 — 유일하게 남는 오차는 진짜 무작위인 `Math.random()*.05` 지터뿐이다. Playwright 실측: 리프 포커스 후 예측 목표 `(357, 0, 0)` vs 실제 최종 위치 `(371.0, -0.002, -31.6)` — 무작위 지터 범위(최대 약 17px) 안에 들어오는 수준으로, 더 이상 눈에 띄는 "튐"이 아니다. 일반(포커스 없는) 시나리오에서도 예측-실제 오차가 지난 라운드 대비 더 줄어듦(약 3px)을 확인 — `sub` 정밀 계산 덕분.
- 4회 연속(짝수 회차마다 리프 포커스를 섞은) 왕복 스트레스 테스트로 매번 `transitioning:false`, 시프트가 거의 0으로 수렴, 콘솔 에러 없음을 확인. `scatterActiveEls.size`가 회차마다 1씩 계속 늘어나는 것(정리를 안 하기로 한 설계 그대로)을 확인했는데, 이는 의도된 트레이드오프다 — 죽은 DOM 참조 몇 개가 세션 내내 누적되는 것은 실사용 환경에서 무시 가능한 수준이고, 경쟁 조건을 완전히 없애는 이득이 훨씬 크다고 판단했다.

### 같은 날 스무 번째 라운드 — 포커스/선택 카드 교체 중 나머지 카드들의 운행이 멈추던 문제 수정

사용자: "포커스된 카드 그룹과 선택된 카드가 교체 시 나머지 카드들의 운행이 멈춘다. 멈추지 않게 해줘." 이번에도 추측 없이 Playwright로 `networkYaw`(카메라 자체의 요 회전값)와 배경 카드 하나의 `orbitAngle`을 100~400ms 간격으로 실측해 정확히 무엇이 멈추는지부터 확인했다.

- **원인**: `networkFrame()`의 카메라 유휴 자동 회전 블록(`if(!networkDragging&&!networkTransitioning){...else if(now-networkLastInput>1200){networkYaw+=.05;...}}`)이 `networkTransitioning`(교체/드릴다운/뒤로가기 등 어떤 장면 전환이든 진행 중이면 true)이 참인 동안엔 완전히 건너뛰어지고 있었다 — 즉 **개별 카드 자신의 공전(`advanceNetworkOrbits`)은 매 프레임 계속 진행되고 있었지만**, 화면 전체를 은은하게 돌리는 카메라 자체의 회전만 장면 전환 내내(약 1~2초) 멈춰 있어서, 배경의 "관계없는" 카드들이 상대적으로 훨씬 덜 움직이는 것처럼(사실상 멈춘 것처럼) 보였던 것 — 실측으로 확인: 전환 시작부터 끝(약 1700ms)까지 `networkYaw`가 21.3도에서 단 한 번도 바뀌지 않다가, 전환이 완전히 끝난 뒤에야 다시 증가하기 시작함.
- **수정**: `!networkTransitioning` 조건을 그 블록에서 제거해 카메라 자동 회전이 장면 전환 중에도 계속 진행되도록 했다 — 안전한 이유: (1) 관성 회전 분기(`networkVelocityYaw/Pitch`)는 모든 전환 함수(`enterFromRing`/`enterNetworkNode`/`backNetworkLevel`) 시작 시 이미 0으로 리셋되고, 드래그 자체도 `networkTransitioning`일 때 막혀 있어 전환 중엔 절대 0이 아닌 값이 될 수 없으므로 이 분기는 항상 무해하게 통과만 함, (2) 각 카드/허브의 개별 트랜지션 목표 좌표는 모두 카메라 회전과 무관한 로컬 좌표계라서(회전은 `networkScene` 전체에 나중에 한 번에 적용됨), 카메라가 계속 돌아도 진행 중인 개별 애니메이션의 목표값 자체는 전혀 어긋나지 않음. 추가로, 유휴 자동 회전은 원래 "1.2초간 조작이 없으면" 시작하는 조건이라 이 가드 제거만으로는 전환 시작 후 1.2초 동안은 여전히 카메라가 멈춰 있었다 — 이건 "최근 드래그한 각도를 유지해준다"는 원래 취지의 유휴 타이머일 뿐, 사용자가 방금 카드를 선택해 전환이 시작된 상황과는 무관하므로, 세 전환 함수 모두 시작 시 `networkLastInput=performance.now()-1300`으로 미리 앞당겨(1.2초 문턱을 즉시 통과하도록) 설정해 전환이 시작되자마자 카메라 회전도 같이 시작되게 했다.
- Playwright 실측: 수정 후 세 가지 전환(`enterFromRing`/`enterNetworkNode`/`backNetworkLevel`) 모두에서 전환 시작 직후(150ms 이내)와 전환 도중(1000ms 이상 경과) 양쪽에서 `networkYaw`가 계속 변화함을 확인, 모두 정상적으로 `transitioning:false`로 정착. 4회 연속 왕복 스트레스 테스트로도 회귀 없음(안정적인 노드 수, 콘솔 에러 없음)을 확인.

### 같은 날 스물한 번째 라운드 — 배경 그룹 전환 시 허브가 튀는 버그 최종 수정(카드 위치 이동 금지 규칙 재적용), 그리드 잔류 버그, 사방 드래그 안내 화살표 추가

- **버그 재보고**: "리프 카드가 중앙에 있을 때 외곽 그룹 카드를 선택하면 화면이 어색하다" — 조사 결과 `enterFromRing`/`backNetworkLevel`이 자식 카드들의 수렴 목표를 `currentHubTranslate()`(클릭 순간 허브의 실제 렌더 위치)로 스냅샷하는데, 리프가 포커스돼 카메라 원점이 그 리프 쪽으로 이동해 있던 상태라 이 스냅샷 자체가 화면 중앙이 아니라 치우친 값이었고, `networkFollowIndex`를 그 직후 0으로 되돌려도 허브 자신은(아직 `scatterActiveEls`에 등록되지 않아) 매 프레임 살아있는 카메라 보정에 의해 뒤늦게 중앙으로 흘러가 버려 — 자식들이 쫓아가는 목표(스냅샷, 고정)와 허브의 실제 위치(계속 이동)가 어긋나는 것이 근본 원인이었다.
- **1차 수정(오판)**: "허브는 포커스가 없으면 항상 로컬 원점(0,0,0)에 정착한다"는 불변식을 근거로 목표값 자체를 `'0px,0px,0px'` 고정값으로 바꿨더니, 이번엔 허브가 실제로 화면 중앙까지 미끄러져 이동해버렸다 — 사용자가 정확히 지적: "펼쳐진 그룹 카드는 움직이면 안 되고, 리프 카드들이 그 쪽으로 움직여야 한다."
- **최종 수정**: 목표값을 다시 `currentHubTranslate()`(클릭 순간의 실제 위치)로 되돌리되, 그 값을 읽는 즉시 허브 엘리먼트를 `scatterActiveEls`에 등록해 매 프레임 카메라 보정 렌더링에서 완전히 제외시켰다 — 이제 허브는 클릭 시점 위치에 그대로 고정된 채로만 존재하고(중앙이든 치우친 자리든 무관), 자식 카드들은 정확히 그 고정된 자리를 향해 수렴한다. 같은 요청에서 "모일 때 작아지지 않고 현재 크기 그대로 모여야 한다"는 지적에 따라 수렴 애니메이션의 `.replace(/scale\([^)]*\)/,'scale(.12)')` 강제 축소도 제거해 각 카드가 포커스 중이었으면 그 확대 배율을, 아니었으면 원래 배율을 그대로 유지한 채 이동만 하도록 정리. Playwright로 리프 카드를 포커스(배율 1.85)한 뒤 외곽 그룹을 클릭해 실측: 허브가 클릭 시점 좌표에 전 구간 고정됨(중앙으로 흐르지 않음), 리프 카드는 배율 1.85를 유지한 채 정확히 그 고정 좌표로 이동, 이후 허브가 배경 링 위치로 예측 좌표까지 물러나고 새 허브가 중앙에서 자연 배율로 자라나는 전체 시퀀스를 확인.
- **그리드(연결선) 잔류 버그**: "네트워크→도넛 전환 시 흩어져 안 보이는 카드의 연결선이 도넛이 다 만들어진 뒤에도 남아있다"는 지적 — 원인은 `scatterOutNetwork()`에서 `.network-edge`의 투명도 전환 지속시간이 카드 자신의 스타일(60%는 짧은 `ms`, 40%는 긴 `SPATIAL_BOUNCE_MS`)과 무관하게 항상 고정 `SPATIAL_BOUNCE_MS`(2600ms)였던 것 — 대부분(60%)의 카드는 훨씬 일찍 사라지는데 그 카드에 연결된 선만 2600ms까지 계속 남아있었다. 각 선도 카드와 동일하게 `pickPathStyle()`로 스타일을 뽑아 너비·투명도 지속시간을 그 값 하나로 통일하도록 수정. Playwright로 `getComputedStyle` 투명도를 60ms 간격 실측해, 수정 후 카드 평균과 선 평균이 거의 같은 곡선으로 함께 감소해 동일 구간(~2.4초)에 함께 0에 도달함을 확인.
- **사방 드래그 안내 화살표 추가**: "도넛 최전면 카드, 네트워크 중앙 카드 위에 상하좌우로 드래그 가능함을 나타내는 사방 화살표를 옅고 두껍게, 흰색으로 표시해달라" — 공유 SVG 마크업(`dragHintMarkup`, 십자형 4방향 화살표, `stroke-width:8`로 두껍게, svg 자체 opacity `.55`로 옅게)을 만들어 도넛 카드와 네트워크 허브·노드 생성 코드 양쪽에 동일하게 삽입, 표시 여부는 JS 변경 없이 기존 클래스(`.torus-card.front`, `.network-hub`, `.network-node--current`)에 대응하는 CSS로만 제어(투명도 트랜지션으로 순간 팝업 없이 전환) — 포커스로 허브가 흐려지면 자식인 화살표도 부모 opacity를 따라 자연히 함께 흐려짐. Playwright 스크린샷으로 도넛 최전면 카드와 네트워크 포커스 리프 카드에 정상적으로 렌더링됨을 확인.

### 같은 날 스물두 번째 라운드 — 사방 화살표에 검은 테두리 추가·2단계 감량, 포트폴리오/데이터 월/원본보기 분류명 색상·위치 통일

- **화살표 검은 테두리 추가**: "가운데 사방 화살표에 검은 테두리를 가늘게 넣어달라" — 흰색 도형 뒤에 같은 모양을 검게 먼저 그리는 2겹 레이어 방식으로 구현: 십자선은 검은 획을 흰 획보다 두껍게 깔고(겹치는 부분은 흰 획이 덮어 가장자리만 테두리로 남음), 화살촉 삼각형은 `fill+stroke` 조합으로 원래 크기보다 바깥으로 살짝 번지게 그려 같은 효과를 냄. 이후 "더 가늘게, 반정도로"라는 요청이 두 차례 이어져 테두리 두께를 순차적으로 절반씩 감량(십자선 검은 획 14→11→9.5, 화살촉 테두리 6→3→1.5) — 매번 로컬 8000포트에서 Playwright 스크린샷으로 실제 렌더링을 확인.
- **포트폴리오 패널 분류명 색상을 데이터 월 그룹 색과 통일**: "포트폴리오의 각 패널 우하단 분류명 색깔을 데이터 월의 해당 그룹 색으로 해달라" — 포트폴리오(`works`)는 "광고 이미지"처럼 여러 데이터 월 그룹(뷰티/헬스/피트니스/스포티룩/아웃도어 광고)을 하나로 묶어 표시하는 반면 데이터 월(`archiveItems`)은 8개 그룹별 고유색(`archiveCatColors`)을 이미 쓰고 있어, 같은 라벨이라도 실제 소속 그룹은 항목마다 다름 — 포트폴리오 렌더링 시 각 작품의 `art` ID로 `viewerGroups`(도넛/데이터 월과 공유하는 그룹 계층)를 역조회해 실제 소속 그룹의 색을 `--cat-color`로 부여하도록 수정(클라이밍 사진은 초록=아웃도어, 스킨케어 사진은 분홍=뷰티 광고 등, 같은 "광고 이미지" 라벨이라도 항목마다 다른 색). 도넛 계층에 속하지 않는 포트폴리오 전용 항목(브랜드 키비주얼)은 대응 그룹이 없어 기존 기본색 유지. **작업 중 발견한 버그**: 색상 맵 `archiveCatColors`가 포트폴리오 렌더링 코드(`works.forEach`)보다 뒤에 `const`로 선언되어 있어 참조 시점에 TDZ(Temporal Dead Zone) `ReferenceError`가 발생, 포트폴리오 그리드 전체가 13개 중 1개만 그려지고 렌더링이 중단되는 심각한 회귀였음 — 선언 위치를 포트폴리오 렌더링보다 앞으로 옮겨 해결, Playwright로 13개 전 항목이 정상 렌더링되고 콘솔 에러가 없음을 재확인.
- **원본보기(모달) 분류명 색상·위치 통일**: "원본보기 창 내의 색깔도 통일하고, 문구 위치를 우하단으로 해달라" — `#modalCategory`가 기존엔 좌하단에 뜨는 정보 패널(`.modal-copy`, 제목·설명·태그를 담은 드래그 가능한 카드) 맨 위 줄에 얹혀 있었는데, 이를 `.modal-copy`에서 완전히 분리해 `.modal-card`의 독립적인 자식(`.modal-category`)으로 이동시키고 이미지 영역 우하단에 고정 배치 — 포트폴리오/데이터 월 썸네일과 동일하게 "분류 배지는 항상 이미지 우하단"이라는 규칙을 원본보기까지 확장. 색상은 매 작품 탐색(`showWork`)마다 `art` ID로 `viewerGroups`를 다시 조회해 실제 그룹 색을 `--cat-color`로 갱신(내비게이션 중 그룹이 바뀌어도 항상 정확). 정보 패널을 감추는 토글(`.modal.info-hidden`)에도 함께 반응하도록 유지. Playwright로 서로 다른 그룹(아웃도어/헬스 광고)의 항목을 열어 배지 텍스트·색상이 정확히 바뀜을 확인, 정보 패널을 실제로 드래그해도 배지 좌표가 픽셀 단위로 전혀 변하지 않아 완전히 독립적으로 고정돼 있음을 확인.

### 같은 날 스물세 번째 라운드 — 일부러 느리게 합류하는 카드 속도 2배 상향

"가상공간 전환시 일부러 엄청 느리게 합류하는 카드의 속도를 2배 빠르게 해달라" — bounce/orbit/wave/loop 네 경로 스타일이 공유하는 지속시간 상수 `SPATIAL_BOUNCE_MS`를 2600ms→1300ms로 정확히 2배(직전 값 기준, 최초값 기준 아님) 단축. 기본(`plain`) 카드의 `SPATIAL_SCATTER_OUT_MS`/`IN_MS`는 그대로 유지. Playwright로 상수 값과 실제 전환 완료(`spatial-overlap` 해제) 소요 시간을 실측해 반영을 확인.

## 원본 보기 화면

- 좌우 스와이프 및 화면 양끝 25% 클릭 이동을 지원한다.
- 좌우 화살표, 정보 보기 토글, 확대 슬라이더가 있다.
- 확대 상태와 무관하게 화면 양끝 이동 기능이 동작한다.
- 모바일 핀치 확대와 PC 확대 슬라이더가 연동된다.
- 더블클릭 시 1:1 배율로 돌아간다.
- 정보 패널은 드래그할 수 있다.
- 정보 패널 배경 투명도를 조절할 수 있으며 블러는 사용하지 않는다.
- 조작 버튼은 3초 후 사라지고 동작 시 다시 나타난다.

## 주요 화면 명칭

- 주메뉴 `대표작`은 `주요 장면`으로 변경됐다.
- 주메뉴 `전체 작업`은 `모두 보기`로 변경됐다.
- 아카이브 제목은 `데이터 월에서`다.
- Selected Works 설명 제목은 `비젠티브가 만든 장면들`이다.

## 데이터 월

최종 갱신: 2026-08-23. 아래는 여러 차례 수정을 거친 현재 확정 동작이다. 다시 손댈 때는 아래 "주의사항"부터 읽는다. 상세 구현(함수명, 정확한 상수 위치)은 Claude 메모리(`archive-wall-magnet-system`)에 압축 정리되어 있음 — 큰 수정 전에 그쪽도 같이 참고할 것.

- 홈 전용 이미지 폴더 `public/hero`는 데이터 월 수집 대상이 아니다.
- 카테고리 필터 칩(`전체`, `AI 모델`, `뷰티 광고` 등)이 제목 옆에 있다. `torusGroups`에서 자동으로 뽑은 목록이라 카테고리가 바뀌어도 따로 손볼 필요 없다.
- **마우스 오버 마그넷 효과**와 **슬립 모드(유휴 시 자동으로 카드가 옮겨 다니며 반짝이는 것)**는 같은 하나의 렌더 엔진(`renderArchiveMagnet`)을 공유하되, 슬립 모드의 "카드가 부풀어 오르는 것" 자체는 독립된 타이머(카드가 리드를 맡는 그 순간 한 번만 시작, 이후 리드가 다른 카드로 넘어가도 자기 타이밍대로 끝까지 진행)로 분리되어 있다. 절대 렌더 엔진 자체를 두 개로 다시 분리하지 않는다 — 분리할 건 "타이밍 소스"뿐이다.
- 포커스 카드는 제자리에서 위로 솟구치듯 커지기만 한다(중심 기준, 수직 이동·비틀림 없음). 크기 1.42배, 화면 쪽으로 카드폭의 0.7배만큼 튀어나옴.
- 포커스를 잃은 카드는 하단이 아니라 중심 기준으로 원래대로 돌아가며, **거리가 아니라 시간(840ms)** 기준으로 감쇠한다 — 마우스가 그 자리에 계속 머물러 있어도 시간이 지나면 무조건 원위치로 돌아간다.
- **(실제 마우스 호버 전용)** 포커스가 다른 카드로 넘어가면 새 카드는 즉시 반응하고, 주변 카드에는 거리에 비례한 지연(물결)을 준 뒤 역시 840ms에 걸쳐 원위치로 감쇠한다(이미 감쇠 중인 더 큰 값을 새 잔상값으로 깎아내리지 않음, 더 강할 때만 갱신). **슬립 모드에서는 이 주변 물결 효과가 꺼져 있다** — 포인터가 실제로 지나간 카드만 커졌다 꺼지고, 지나가지 않은 카드는 전혀 변화 없다(사용자 요청으로 명시적으로 끔).
- **슬립 모드 자동 이동은 세 가지 속도가 서로 독립적으로 조절된다**: ① 리드(포커스) 전환 주기 — 현재 248ms마다 다음 카드로(2026-08-23 사용자 요청으로 기존 124ms에서 절반 속도로 늦춤; PC/모바일이 같은 `setInterval` 한 줄을 공유하고 기기별 분기가 전혀 없어 두 화면에 동일하게 적용됨). ② 카드가 부풀어 오르는 속도 — 현재 500ms(리드가 이미 다음 카드로 넘어간 뒤에도 자기 타이밍대로 끝까지 부풂 → 여러 카드가 동시에 겹쳐서 물결처럼 보임, 리드 주기가 늘어난 만큼 동시 확인되는 카드 수는 기존보다 줄어듦). ③ 잦아드는 속도 — 840ms(마우스 호버와 공유, 이번 조정에서 건드리지 않음). 다시 조정 요청이 오면 이 세 숫자만 건드리면 되고 서로 맞출 필요 없다.
- 카드를 클릭하면 원본 보기로 들어가기 전에 **현재 화면에 보이는 카드만** 클릭한 카드 위치로 모여든다. 크기는 줄어들지 않고(클릭 직전 마그넷으로 커져 있었다면 그 크기 그대로) 위치만 이동하며, 클릭한 카드가 항상 맨 앞·맨 위(z-index와 실제 3D 앞쪽 돌출 모두)에 남고 나머지는 그 뒤로 겹쳐 들어간다. 가까운 카드가 먼저 도착하고 먼 카드는 거리 비례로 늦게 도착한다.
- 모두 모인 뒤에는 클릭한 카드 하나가 화면을 거의 채울 만큼(뷰포트 높이의 85%·너비의 56% 중 작은 쪽 기준) 커지고 나서야 실제 원본 보기가 열린다 — 마치 그 썸네일이 그대로 원본 화면이 되는 것처럼 보이게 하기 위함이다. 화면 밖·필터로 숨겨진 카드는 건드리지 않는다.
- 원본 보기를 닫으면 반대 순서로 진행된다: 커졌던 카드가 먼저 원래 썸네일 크기로 줄어들고, 그 다음에야 모여있던 카드들이 원위치로 흩어진다(동시에 일어나지 않는다).
- 필터 버튼은 제목("데이터 월에서") 바로 옆에 배치한다(PC는 한 줄, 모바일은 폭이 부족하면 제목 아래로 자동 줄바꿈). "선택 시 원본 이미지로 보입니다" 안내 문구는 사용자 요청으로 완전히 제거했다 — 되살리지 않는다.
- 필터 칩과 각 카드 좌하단 마커는 카테고리별 고유 색상을 갖는다(`archiveCatColors`, 라벨→hex 맵 하나로 관리). **배경은 절대 채우지 않고 완전 투명이 확정 사양이다** — 테두리(필터 버튼)와 글자색으로만 구분한다. 텍스트 그림자 같은 배경 대체 효과도 넣지 않는다(옅게라도 어두운 배경이 보이면 안 된다는 게 사용자의 명확한 요구사항이었고, 실제로 배경 채움→반투명→그림자 순으로 여러 번 되돌린 이력이 있다).
- 모바일에서는 공간 절약을 위해 필터 버튼 텍스트를 축약한다(각 분류 첫 글자, 단 "AI 모델"은 예외로 "AI"). PC는 축약 없이 전체 명칭을 그대로 쓴다 — 색상 자체는 PC/모바일 공통이고, 축약 여부만 `@media(max-width:640px)`로 전환된다.
- 모바일 원반 메뉴 "모두 보기" → 데이터 월 스크롤 이동은, 포트폴리오 섹션(`#portfolio`, 데이터 월 바로 위)의 카드 이미지 실제 가로세로 픽셀 크기를 `mediaMarkup()`의 `artAspect` 맵을 통해 `width`/`height` 속성으로 미리 박아 넣어 해결했다(자세한 근본 원인은 아래 주의사항 참고).

### 주의사항 (다시 걸리기 쉬운 함정)

- `.archive-thumb:hover{...}` 같은 예전 CSS 규칙을 되살리지 않는다 — 인라인 스타일(JS)이 항상 CSS 클래스보다 우선하는데, 그 반대로 착각하기 쉽다.
- `resetArchiveMagnet()`은 모든 카드의 인라인 스타일을 즉시 지운다. 데이터 월의 `pointerdown`/`pointerup`/`pointercancel` 리스너는 반드시 `pointerType==='touch'`일 때만 이 함수를 불러야 한다 — 아니면 마우스 클릭 시 `click` 이벤트가 뜨기 전에 `pointerup`이 먼저 커진 크기를 지워버린다.
- `.archive-wall`에 `transform-style:preserve-3d`가 걸려 있어서, 카드를 맨 앞에 두려면 z-index만으로는 부족하다. 실제로 카드를 화면 쪽으로 더 튀어나오게(`--mag-z`)도 해야 하고, 그 값은 항상 `Math.max(현재값, 최소값)`으로 올리기만 해야 한다(내리면 원근 계산 때문에 크기도 같이 작아 보인다).
- `measureArchiveMagnet()`(위치 재측정)은 항상 `resetArchiveMagnet()`/`freezeArchiveMagnet()`보다 **뒤에** 호출한다. 반대로 하면 방금 측정한 값을 스스로 지워버린다.
- 검증 시 Playwright로 헤드리스 크롬을 직접 띄워 테스트했는데, `mouse.move`/`click` 같은 호출 자체에 90~170ms 이상의 지연이 있어서 `waitForTimeout` + 스크린샷 방식은 타이밍을 오판하기 쉽다. `performance.now()` 기반의 페이지 내부 프레임 대기와 `--mag-*` 커스텀 프로퍼티 직접 읽기가 더 정확했다.
- 모바일 "모두 보기" 스크롤이 데이터 월 대신 포트폴리오 중간쯤에 도착하는 버그는 스크롤 오프셋 계산이 아니라 **레이아웃 자체가 아직 확정되지 않은 상태에서 위치를 쟀기 때문**이었다. `.work img`가 `loading="lazy"`이고 예약된 크기(`width`/`height` 속성이나 CSS `aspect-ratio`)가 전혀 없으면, 이미지 바이트가 도착하기 전엔 브라우저가 그 카드의 실제 높이를 모른다 — 그 상태에서 목표 지점을 계산해 스크롤하면 이미지가 나중에 로드되며 섹션 키가 늘어나 목표가 더 아래로 밀린다. `ResizeObserver`로 사후에 쫓아가며 보정하는 방식은 실제 페이지 로드 직후 클릭 같은 상황에서 여전히 실패했다(네트워크가 느리면 보정 시간 안에 다운로드 자체가 안 끝남). **근본 해결은 이미지가 도착하기도 전에 정확한 박스 크기를 브라우저에 알려주는 것**(실제 픽셀 크기를 `width`/`height` 속성으로 미리 지정) — 목표 지점을 스크롤 시작 전에 이미 정확하게 만들어서, 사후 보정 자체가 필요 없어지게 하는 게 핵심이었다. 비슷한 증상(스크롤 목표가 lazy 이미지 그리드 바로 다음 섹션인데 자꾸 짧게 도착)을 다시 보면 스크롤 오프셋 계산보다 이미지 박스 크기 예약 여부부터 의심할 것.
- 새 art 번호를 포트폴리오/데이터 월에 추가할 때는 `mediaMarkup()`의 `artAspect` 맵에도 실제 원본 가로세로 픽셀 크기(`sharp`로 읽으면 됨)를 같이 추가해야 한다. 빠뜨리면 그 이미지만 다시 레이아웃-밀림(CLS) 문제가 재발한다.
- 원본 보기를 닫을 때 접근성을 위해 포커스를 "열기 전 요소"로 되돌리는데, 그게 방금 클릭한 카드 자신이라 데이터 월의 `focusin` 리스너가 다시 발동해 "사용자가 상호작용 중"이라는 상태가 영구히 켜진 채로 남아 슬립 모드가 다시는 안 도는 버그가 있었다(수정 완료). 비슷하게 "닫을 때 포커스를 되돌리는" 코드를 다시 만질 일이 있으면, 그 포커스 대상이 데이터 월 안쪽 요소일 수 있다는 점을 염두에 둘 것.

## 연락처

- 이메일: `gtkim1000@naver.com`
- 이메일 기본 제목: `프로젝트 문의`
- 전화: `010-8294-7823`
- 카카오톡 오픈채팅: `https://open.kakao.com/o/ss9NyOGi`

## 검증 및 로컬 실행 주의사항

- 원본 수정 파일은 `index.html`이다.
- 로컬에서 `dist/index.html`을 보고 있을 수 있으므로 수정 후 반드시 빌드해야 한다.
- 검증 및 빌드 명령:

```powershell
python tools/validate_site.py
```

- 위 명령 실행 후 `index.html`과 `dist/index.html`에 변경이 모두 반영됐는지 확인한다.
- 과거에 원본만 수정하고 `dist`를 빌드하지 않아 화면에서 변경이 보이지 않았던 문제가 있었다. 같은 실수를 반복하지 않는다.

## 백업 및 배포 기록

- 브라우징 4차 백업: `backups/vizentive-browsing-v4.zip`
- 비젠티브 버전 5 백업: `backups/vizentive-version5.zip`
- 비젠티브 버전 5 커밋: `eead95c`
- 비젠티브 버전 6 백업: `backups/vizentive-version6.zip`
- 비젠티브 버전 7 도넛 브라우저 안정화 백업: `backups/vizentive-version7-donut-stabilization.zip`
- 모바일 슬로건 및 브라우징 위치 조정: `a464c41`
- 모바일 브라우징 위치 미세 조정: `65efff2`
- 버튼 기준 모바일 브라우징 간격 조정: `ce1928b`

## 배포 절차

1. `python tools/validate_site.py`
2. `git diff --check`
3. 변경 파일만 `git add`
4. 한글 제목으로 `git commit`
5. `git push origin main`
6. `https://www.vizentive.co.kr/` 응답 코드 200 확인
7. 운영 HTML에서 새 기능을 나타내는 고유 문자열 확인

## 작업 원칙

- 사용자가 배포를 요청하지 않은 수정은 로컬에만 반영한다.
- 배포 요청 시 원본 수정, `dist` 재빌드, Git 푸시, 운영 반영 확인까지 완료한다.
- 확정 슬로건과 서비스 범주는 임의로 바꾸지 않는다.
- 홈 전용 이미지는 포트폴리오 및 데이터 월 자산과 분리한다.
